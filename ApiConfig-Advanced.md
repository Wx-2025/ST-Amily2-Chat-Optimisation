# API 连接 · 进阶操作

## 连接页

| 操作 | 说明 |
|---|---|
| 添加 | 新建一条连接：选用途（对话/向量/重排）→ 填地址、密钥、模型 → 保存 |
| 编辑 | 点已有连接改参数，再保存 |
| 测试 | 对该条连接发试探请求，确认通不通 |
| 删除 | 删掉不用的连接（已分配的功能会变「未分配」） |
| 类型筛选 | 只看对话 / 向量 / 重排，方便找 |

## 分配页

| 操作 | 说明 |
|---|---|
| 功能开关 | 开=启用该槽；关=功能不走 API |
| 下拉选连接 | 把某条连接挂到对应功能 |
| 一键关 | 关掉某槽后该功能立刻停 |

常见槽：聊天总结、表格填表/重整、独立API填表、智能检索·向量化/重排、剧情优化、术语表、角色世界书、一键生卡…

## 防超时（流式收完再拼）

对话模型连接的“高级 → 防超时”是连接级设置。开启后，凡是分配到这条连接的文本生成槽都会向上游发送 `stream: true`，收到连续分片后再在本地还原成原有的完整结果；功能模块仍拿到完整文本，不需要改提示词或输出格式。

覆盖正文/剧情优化、并发优化、聊天总结、独立 API 填表、表格 Tool Call 与严格文本回退、术语表、角色世界书、一键生卡、旧卡兼容任务和 shujuku Agent 工作负载。Tool Call 的名称与参数分片也会完整重组。

- 适合模型生成时间超过 Cloudflare、Nginx 或其它反代“长时间无响应”限制的连接。
- 它不能延长上游模型自身的超时，也不能挽救已经断开的网络；作用是让生成期间持续有数据通过反代。
- 向量化与重排不是文本生成流，不使用该开关。
- “ST 预设转发”通过 SillyTavern 原生连接管理器请求流式结果，Amily 在所选 Profile 锁释放前收完整条流并恢复成普通响应；不会绕过宿主去手工拼接预设参数。
- 损坏的 SSE、错误 JSON、超大响应或不完整 Tool Call 会报错停止，不会把缺失分片当作成功结果。

## 更多页

| 操作 | 说明 |
|---|---|
| 导出私钥包 | 换机前导出密钥 |
| 导入私钥包 | 新机器导入后才能用已有连接 |
| 本机存储 | API Key 只保存在当前浏览器，不上传 |
| 加密云同步（手动私钥） | API Key 密文随酒馆设置同步；换设备时手动导入私钥 |
| 授权码云同步（Type2+） | 在手动云同步基础上，用当前服务器授权码加密备份私钥并按需恢复 |

授权码云同步只在启用、授权成功、页面就绪后的恢复检查或用户点击按钮时请求服务器，不做定时轮询。断网、服务异常或修订冲突不会删除本机现有密钥；本机与云端指纹不同会停下来要求人工选择。

手动云同步和授权码云同步切回本机时都只设置当前设备的本机偏好，不删除 `extension_settings` 中的共享公钥或密文。从本机重新启用手动云同步会先检查共享镜像；缺少匹配私钥、镜像损坏或本机与云端内容不同时，必须恢复私钥或明确选择采用哪一份，不能静默覆盖。

手动云同步的共享镜像仍由 SillyTavern 设置同步，服务端没有独立修订号。插件会把检查时的 SHA-256 快照与本次确认绑定，并在提交前复核；但两个离线设备真正同时写入时，底层仍是 SillyTavern 的最后写入覆盖语义。请避免在多台设备上同时执行“用本机覆盖共享备份”。

服务器密钥库只保存公钥和加密后的私钥，不保存 API Key 密文本身。完整换机恢复仍要求新设备加载同一份 SillyTavern `extension_settings`；不同酒馆实例只有授权码和私钥备份时，没有可供解密的 API Key 密文。

### 授权码加密的安全边界

私钥包使用当前服务器授权码作为 HKDF 输入，再通过 AES-GCM 加密。该设计可避免私钥以明文形式出现在 Vault 记录、酒馆设置或普通网络响应中，但它不是零知识或端到端加密：授权服务仍需保存可用于校验的授权码，而当前授权记录与 Vault 记录位于同一服务端数据库。能够同时读取授权码与加密私钥包的服务端管理员、应用进程或整库泄露者，可以派生同一把包装密钥；本模式不承诺抵御这一威胁。

如果攻击者只取得 Vault 记录而没有授权码，离线猜测难度取决于授权码自身的熵。短码、带可预测时间片的码、人工口令以及随机部分过短的历史码，不应被视为独立的加密密钥。平台后续生成或轮换用于 Vault 的授权码时，应使用密码学安全随机源，并提供至少 128 bit 的不可预测随机量；提高 HKDF 或其他 KDF 的计算成本只能增加猜测成本，不能解决授权码与密文同库泄露的问题。

## 仍解决不了

回首页 **作者留言 / 公告板**，点 **【01】群** 或 **【02】群** 加群求助。
