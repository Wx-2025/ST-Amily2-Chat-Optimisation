# 数据库与 Agent 兼容：进阶操作

> 适用版本：Amily 2.3.1
> 最低权限：Type2。Type1、授权过期或未授权状态会停止扫描、桥接、模型任务和 Agent 注入，但不会删除已经迁移的数据。

## 三级配置

| 层级 | 保存什么 | 不会做什么 |
| --- | --- | --- |
| 全局 | 当前浏览器允许使用的兼容能力上限 | 不给任何聊天自动授权 |
| 角色卡 | 按卡内容指纹保存新聊天的默认预选 | 不写回角色卡，不保存 Key，不复制聊天凭据 |
| 聊天 | 真正生效的精确 grant 与本机 receipt | 不自动扩展到更新后新增的能力 |

安全顺序是：**全局允许 → 建立档案 → 选择能力 → 聊天确认**。关闭全局开关只会停桥；要删除聊天授权，请明确点击“撤销当前聊天授权”。

## 建档和迁移

- 扫描只读取角色卡静态数据，不执行卡内 JavaScript。
- 确认摘要后，迁移档案写入当前聊天；原 PNG/JSON 不会被修改。
- 档案协议或卡片内容指纹变化后必须重扫，旧 receipt 不会越过新档案继续生效。
- 现有 Amily 用户表不会被静默覆盖；冲突会中止整次提交。

## B3：模板和剧情资源

B3 只暴露迁移清单中已经登记并通过摘要校验的资源：

- 可预览表格模板和剧情预设；
- 可显式绑定到当前聊天；
- 不创建全局预设，也不执行第三方剧情任务；
- 更新资源或换卡后要重新核对绑定。

## B4：受限模型任务

B4 使用独立功能开关、聊天授权和专用 API 槽。诊断只发送固定的短请求；页面只显示脱敏后的 provider、model 和 token 上限，不显示 URL、Key、headers 或其他槽位。

建议为 B4 / Agent 设置合理 RPM，避免错误脚本或异常循环造成高频调用。RPM 为 `0` 表示不限制，不适合不可信兼容任务。

## Agent 一键兼容

完整顺序：

1. 当前必须是单角色聊天，并已建立最新版兼容档案。
2. 停用已识别的旧 Agent 伴侣脚本，然后完整刷新 SillyTavern。
3. 开启受管影子以及卡片请求的 Agent 能力。
4. 逐项确认本聊天授权。
5. 在 API 连接配置中分配 **shujuku Agent 原生任务**专用 Profile。
6. 执行 **准备影子档案**。
7. 执行 **构建索引**，等待索引和语义前置条件全部就绪。
8. 执行 **激活**，再开始聊天测试。

Amily 不修改原角色世界书，而是创建未绑定角色的受管影子并从副本构建索引与注入。兼容层不是 JavaScript 沙箱；同页面的第三方脚本仍可能绕过 Amily，因此只对可信卡启用。

## RP 与填表

- RP 模型只扮演自己的角色，不替玩家决定行为，也不预写完整剧情结果。
- Tool Call 填表只接受唯一目标工具、非空参数、合法 JSON 和严格行边界。
- 格式错误或空回会在有限重试前提示，不会无限循环。
- 模块自有表继续受 owner 边界保护，兼容卡不能借 AI 填表修改其他模块的私有数据。

## 停用与恢复

- 临时停用：先停 Agent 注入，再关闭相应全局能力。
- 永久撤销当前聊天：点击“撤销当前聊天授权”。
- 清除新聊天默认：点击“清除本卡默认”。
- 降为 Type1 或授权失效：运行时立即失败关闭，迁移档案和本地偏好保留；恢复 Type2 后仍会重新核验档案、receipt 和聊天作用域。

错误排查时优先记录页面显示的安全错误码，不要公开 API Key、完整请求头、角色卡私密正文或聊天内容。
