# 表格模块（照着点就行）

## 模型

| 需要 | 不要 |
|---|---|
| **对话**（DeepSeek / Gemini / GPT / Claude） | embedding、rerank |

## 怎么开

```
API连接：加对话
  → 分配「表格填表/重整」（主填表）
  → 要用单独通道：分配「独立API填表」并开开关
本页：总开关开 → 要 AI 看见表再开「表格注入」
提取规则：下拉选一条（规则配置中心先建好）
填表模式：新手用「分步」或「兼容优化」
```

## 翻车

| 现象 | 你做 |
|---|---|
| 填表没反应 | 总开关 + 分配页对话连接 |
| 提取规则是「未分配」 | 进 **规则配置** 建规则 → 本页下拉选上 |
| 类型不匹配 | 改成 **对话** |
| AI 不看表 | 开 **表格注入** |
| 独立API填表没反应 | 开「启用独立API填表」+ 分配「独立API填表」 |
