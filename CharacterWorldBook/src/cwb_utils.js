export function logDebug(...args){console.log("[CWB]",...args)}export function logError(...args){console.error("[CWB]",...args)}import{extensionName}from"../../utils/settings.js";export function isCwbEnabled(){try{const overrides=JSON.parse(localStorage.getItem("cwb_boolean_settings_override")||"{}");if(void 0!==overrides.cwb_master_enabled)return!0===overrides.cwb_master_enabled;const settingsString=localStorage.getItem(`extensions_settings_${extensionName}`);if(settingsString){const settings=JSON.parse(settingsString);if(void 0!==settings?.cwb_master_enabled)return!0===settings.cwb_master_enabled}return!0}catch(error){return console.error("[CWB] Error reading master switch state:",error),!0}}export function checkCwbEnabled(operation="操作"){return!!isCwbEnabled()||(console.log(`[CWB] ${operation}被跳过 - CharacterWorldBook总开关已关闭`),!1)}export function showToastr(type,message,options={}){isCwbEnabled()&&(window.toastr?(window.toastr.clear(),window.toastr[type](message,"角色世界书",options)):logDebug(`Toastr (${type}): ${message}`))}export function escapeHtml(unsafe){return"string"!=typeof unsafe?"":unsafe.replace(/&/g,"&").replace(/</g,"<").replace(/>/g,">").replace(/"/g,'"').replace(/'/g,"&#039;")}export function cleanChatName(fileName){if(!fileName||"string"!=typeof fileName)return"unknown_chat_source";let cleanedName=fileName;if(fileName.includes("/")||fileName.includes("\\")){const parts=fileName.split(/[\\/]/);cleanedName=parts[parts.length-1]}return cleanedName.replace(/\.jsonl$/,"").replace(/\.json$/,"")}export function compareVersions(v1,v2){const parts1=String(v1).split(".").map(Number),parts2=String(v2).split(".").map(Number);for(let i=0;i<Math.max(parts1.length,parts2.length);i++){const p1=parts1[i]||0,p2=parts2[i]||0;if(p1>p2)return 1;if(p1<p2)return-1}return 0}export function parseCustomFormat(text){const data={};if("string"!=typeof text)return data;const coreDataMatch=text.match(/\[--Amily2::CHAR_START--\]([\s\S]*?)\[--Amily2::CHAR_END--\]/);if(!coreDataMatch||!coreDataMatch[1])return data;const coreData=coreDataMatch[1],setNestedValue=(obj,path,value)=>{const keys=path.split(".");let current=obj;for(let i=0;i<keys.length-1;i++){const key=keys[i],nextKey=keys[i+1],isNextKeyNumeric=/^\d+$/.test(nextKey);if(current[key]||(current[key]=isNextKeyNumeric?[]:{}),"object"!=typeof current[key]||null===current[key])return void logError(`Path conflict in worldbook entry for path: ${path}. Expected object/array at key '${key}', but found ${typeof current[key]}.`);current=current[key]}const finalKey=keys[keys.length-1];/^\d+$/.test(finalKey)&&Array.isArray(current)?current[parseInt(finalKey,10)]=value:"object"!=typeof current||Array.isArray(current)||(current[finalKey]=value)};return coreData.split("\n").filter(line=>""!==line.trim()).forEach(line=>{const match=line.match(/^\[{1,2}(.*?)\]{1,2}:([\s\S]*)$/);if(match){const path=match[1],value=match[2].trim();setNestedValue(data,path,value)}}),data}function buildCustomFormatRecursive(obj,prefix=""){let result="";for(const key in obj)if(Object.prototype.hasOwnProperty.call(obj,key)){const newPrefix=prefix?`${prefix}.${key}`:key,value=obj[key];if(null==value)continue;"object"!=typeof value||Array.isArray(value)?Array.isArray(value)?value.length>0&&"object"==typeof value[0]&&null!==value[0]?value.forEach((item,index)=>{result+=buildCustomFormatRecursive(item,`${newPrefix}.${index}`)}):value.forEach((item,index)=>{result+=`[${newPrefix}.${index}]:${item}\n`}):result+=`[${newPrefix}]:${value}\n`:result+=buildCustomFormatRecursive(value,newPrefix)}return result}export function buildCustomFormat(data){let content=buildCustomFormatRecursive(data);return content=content.split("\n").filter(line=>line.match(/^\[.*?]:.+/)).join("\n"),`[--Amily2::CHAR_START--]\n${content.trim()}\n[--Amily2::CHAR_END--]`}