# 规则配置 · 进阶操作

## 面板

| 区 | 干啥 |
|---|---|
| 规则列表 | 新建 / 选中 / 删除 |
| 规则名称 | 自己好认的名字 |
| 只提取指定标签 | 只保留列出的标签内文本 |
| 标签列表 | 逗号分隔，如 `time,content` |
| 排除段落 | 起止标记之间整段丢掉 |
| 跳过用户消息 | 不处理用户楼 |

## 配法示例（对照小铅笔）

消息：

```text
<time>2026/7/25</time>
<content>
<!--我今天不高兴-->
我今天真高兴。
</content>
```

| 设置 | 值 |
|---|---|
| 只提取指定标签 | 开 |
| 标签 | `time,content` |
| 排除一行 | 左 `<!--` 右 `-->` |

效果：保留日期和「我今天真高兴」，丢掉注释里的 CoT。

## 分配到功能

规则建好后，在这些页的下拉里选用：

- 总结模块
- 表格模块
- 智能检索（浓缩/预处理）

未选 = 可能「没反应」或走默认行为。

## 技巧

| 点 | 说明 |
|---|---|
| 先看再写 | 永远先用小铅笔确认真实标签 |
| 标签不要写 `<>` | 填 `content` 不要填 `<content>` |
| 多套规则 | 不同卡/预设可各建一套 |
| 排除可多条 | think、details、html 注释等 |

## 仍解决不了

回首页 **作者留言 / 公告板**，点 **【01】群** 或 **【02】群** 加群求助。
