# 时间河使用指南

时间河把本地小说建立成按原文顺序排列的连续故事轨，并让每个聊天分别绑定到明确的 Story Segment。它提供“沿原作河道前进”的参考窗口，不替用户决定剧情，也不会自动移动游标。

> 当前版本为时间河 v1 本地连续轨内测，要求有效的 **Type2 授权或更高权限**。
>
> v1 基础流程不需要 Embedding，不会在导入、发布、绑定、游标保存或本地预览时调用模型或向量服务。

## 开始前

- 小说文件需为本地 `.txt` 或 `.md`；
- 小说正本、目录、Revision 和故事轨保存在当前浏览器的时间河 IndexedDB 中，不随角色卡或聊天导出；
- 清理站点数据、换浏览器配置或换设备后，本地语料不会自动同步；
- 只导入来源可信且你有权使用的文本。

## 六步完成首次配置

入口：`Amily 面板 → 功能模块 → 时间河`

### 第一步：确认基础模式

v1 本地连续轨不依赖 Embedding。普通对话模型也不能替代 Embedding，但这不影响当前基础故事轨。

只有以下情况才需要在 `API 连接配置 → 智能检索 · 向量化` 中配置 Embedding：

- 继续使用旧版 `legacy-vector` 语料；
- 以后主动开启“已历范围语义回忆”等可选增强。

这些可选能力失败时只应关闭对应增强，不得阻断 v1 本地连续窗口。

### 第二步：导入并检查目录

1. 在“01 导入与目录预览”点击“选择小说文件”；
2. 选择 `.txt` 或 `.md`；
3. 核对 UTF-8 / GB18030 编码与作品标题；
4. 点击“导入小说并预览目录”；
5. 检查识别出的卷、章、一级块、二级块和空间保守估算；
6. 必要时修正目录标题。

这一步只在浏览器本地解析，不联网、不调用模型，也不产生向量费用。

### 第三步：确认并发布 active Revision

目录无误后：

1. 勾选“我已核对目录与空间保守估算……”；
2. 点击“确认并持久化”；
3. 等待本地完整性校验结束。

时间河会在本地保存不可变正本、建立有序 Story Segment、验证相邻关系与来源范围，然后直接发布为 `active` Revision。整个 v1 本地流程网络调用为 `0`。

`staging` 只用于旧版向量语料或未完成的兼容任务，不能绑定聊天；v1 本地轨不需要先构建向量索引。

### 第四步：选择语料并绑定聊天

1. 在“02 语料与版本”选择目标 Corpus 与 `active` Revision；
2. 在“04 当前聊天绑定与游标”选择当前卷、当前章和当前 Story Segment；
3. 选择“允许前导至”的同章连续 horizon；
4. 点击“将所选 Revision 绑定到当前聊天”。

绑定只保存语料身份、精细游标、前导硬边界和预算，不把整本小说写进聊天或角色卡。每个聊天独立维护自己的绑定。

> **绑定不等于开启注入。** 新绑定默认不会自动把原文送进模型上下文。

### 第五步：设置窗口并本地预览

v1 把连续窗口分成三段：

- 历史：当前段之前、仍在预算内的连续原文；
- 当前：游标所在 Story Segment；
- 前导：从当前段向后连续读取，但绝不能越过明确选择的 horizon。

分别设置“历史 Token 预算”“当前段 Token 预算”“前导 Token 预算”和“本地连续窗口总预算”，再点击“预览本地连续窗口”。

本地预览不会调用 Embedding、向量查询或对话模型。预览中应重点确认：

- 原文顺序连续；
- 当前 Story Segment 正确；
- 前导内容没有越过 horizon；
- 总文本没有超过预算；
- 没有出现其他章节或未来未授权段落。

旧版兼容模式仍把语义“未来窗口”固定为 `0`；v1 的少量前导不是自由向量召回，而是用户明确选择、连续且可预览的本地硬边界。

### 第六步：开启生成前注入

1. 勾选“启用生成前原文窗口注入”；
2. 点击“保存游标与注入设置”；
3. 再次预览确认；
4. 正常发送消息。

实际生成时，时间河只把当前绑定对应的有界连续窗口加入模型上下文。基础 v1 不会为每轮聊天额外调用 Embedding，但注入原文本身会占用当前对话模型的提示词 Token。

剧情推进后需手动移动 Story Segment、重新确认 horizon 并保存。当前版本不会让 LLM 无确认地推进游标。

## 状态流程

```text
本地选择 TXT / MD
  → 本地目录与分块预览
  → 确认持久化
  → 校验 Story Segment
  → 直接发布 active
  → 绑定当前聊天
  → 预览连续窗口
  → 开启并保存注入
```

## 网络与费用边界

| 操作 | v1 本地连续轨 |
| --- | --- |
| 选择文件、解析目录、编辑标题 | 仅本地 |
| 确认持久化、校验并发布 active | 仅本地 |
| 绑定聊天、移动游标、修改 horizon | 仅本地 |
| 预览本地连续窗口 | 仅本地，不调用 Embedding |
| 开启注入后的聊天生成 | 不额外调用 Embedding；原文会占用对话模型上下文 |
| 旧版向量摄取 / 语义增强 | 可调用 Embedding，必须另行明确确认 |

## 常见问题

### 页面不可用

- 确认账号具有有效的 Type2 授权或更高权限；
- 授权失效会停止新任务、读取、绑定与注入，但不会删除已有本地语料；
- 恢复授权后重新打开页面并手动刷新状态。

### 选择文件后按钮仍不可用

- 确认文件扩展名为 `.txt` 或 `.md`；
- 核对编码；
- 重新选择文件后，旧预览与旧确认会失效，这是防止错绑正本的预期行为。

### 持久化后没有 active Revision

- 查看页面状态中的完整性错误；
- 不要用旧预览继续提交，重新选择文件并生成新预览；
- 若是浏览器存储额度不足，请先释放站点空间；
- v1 本地轨不应要求配置 Embedding，若出现该要求请作为 bug 提交。

### 无法绑定

- 确认当前有已保存的聊天；
- 确认选择的是可读的 `active` Revision；
- 选择有效的 Story Segment；
- horizon 必须是当前段或同章内可连续到达的后续段。

### 勾选注入后保存不了

- 先完成聊天绑定；
- 重新选择 Story Segment 和 horizon；
- 检查各段预算之和与总预算；
- 切换聊天后旧操作会失败关闭，需要在新聊天重新读取绑定。

### 已绑定但回复没有时间河效果

- 确认注入开关已开启并成功保存；
- 先点击“预览本地连续窗口”，确认预览中确实有正文；
- 绑定和游标按聊天隔离，新聊天不会继承另一个聊天的注入状态；
- 如果旧提示词无法确认清除，时间河会阻止本轮生成，避免串入其他聊天的原文。

### 需要提交问题

使用“导出脱敏诊断”。诊断只应包含状态、错误分类与计数，不应包含 API Key、小说正文或 Embedding 数组；发送前仍建议自行检查。

## 当前边界

当前版本提供：

- TXT / Markdown 本地导入；
- 卷章解析与弹性分块；
- 不可变 Corpus / Revision；
- 连续 Story Segment；
- 按聊天隔离的精细游标与 horizon；
- 有界本地连续原文预览和生成前注入；
- 旧版向量语料的兼容读取与任务恢复底座。

尚未开放：

- 自动判断并推进 Story Segment；
- WorldBuilder 结构化世界构建主链；
- 偏离检测、世界线重算与推进建议；
- 语料跨设备自动同步；
- 将小说正本随角色卡分发。
