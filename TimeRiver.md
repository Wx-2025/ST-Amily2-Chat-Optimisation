# 时间河使用指南

时间河让当前聊天沿着小说的卷章进度检索原文参考。它会限制检索范围，只允许当前章节和已经经过的章节参与生成；它不会自动决定剧情走向，也不会替你推进章节。

> 当前版本要求 **Type2 及以上授权**，只支持从本地导入 `.txt` 或 `.md` 文件。

## 开始前

### 1. 配置 Embedding 连接

先在：

`Amily 面板 → API 连接配置 → 智能检索 · 向量化`

创建并分配一个可用的 **Embedding（向量化）连接**。

Embedding 会把小说分块和聊天查询转换成可检索的向量。普通对话模型不能替代它；没有可用的 Embedding 连接时，仍可在本地预览小说目录，但无法完成索引、发布 `active` Revision 或进行原文检索。

Embedding 服务是否收费取决于服务商。构建索引、重试失败的索引块，以及启用注入后的实际聊天检索，都可能调用该服务。

### 2. 确认本地存储环境

小说正本、分块、任务记录和 Revision 保存在当前浏览器的时间河专用存储中，不会随角色卡或聊天导出。清理站点数据、更换浏览器配置或换设备后，这些本地数据不会自动同步。

## 六步完成首次配置

入口：

`Amily 面板 → 功能模块 → 时间河`

### 第一步：导入并检查目录（仅本地）

1. 在“01 导入与目录预览”中点击“选择小说文件”；
2. 选择本地 `.txt` 或 `.md`；
3. 确认文本编码和作品标题；
4. 点击“导入小说并预览目录”；
5. 检查识别出的卷、章、标题、分块数量和空间估算。

这一步只在当前浏览器读取和解析文件，不联网、不调用模型，也不会产生模型费用。

### 第二步：持久化为 staging（仅本地）

确认目录无误后，勾选确认项并点击“确认并持久化”。

时间河会把小说正本、目录和待执行任务写入本地存储，并创建一个 `staging` Revision：

- `staging` 表示索引尚在构建，或仍有任务等待执行、恢复或重试；
- `staging` 可以检查任务状态，但不能绑定到聊天；
- 持久化本身不会自动启动可能计费的任务。

### 第三步：构建索引并等待 active（可能调用外部服务）

1. 在“02 语料与版本”中选择刚创建的 Corpus 和 `staging` Revision；
2. 再次核对付费提示并勾选确认；
3. 在“03 可靠索引任务”中点击“开始 / 继续”；
4. 等待待处理项全部完成；失败项可在确认后单独重试。

这一步会逐块调用当前配置的 Embedding 服务并写入向量索引，可能产生费用。暂停只会停止后续工作，已经发往远端的请求可能已经计费。

全部必需任务成功后，Revision 会自动变为 `active`：

- `active` 表示索引已经可以供聊天使用；
- 只有 `active` Revision 能绑定到聊天；
- 刷新页面不会自动继续或重试可能计费的任务。

### 第四步：绑定当前聊天（仅本地）

打开或新建一个已经保存的聊天，然后在“04 当前聊天绑定与游标”中点击“将所选 Revision 绑定到当前聊天”。

绑定只保存：

- Corpus 与 Revision；
- 当前卷章游标；
- 回看窗口、Top K 和 Token 预算；
- 是否启用生成前注入。

整本小说和 Embedding 不会写入聊天或角色卡。不同聊天分别保存自己的绑定和游标。

> **绑定不等于开启注入。** 新绑定默认关闭生成前注入，不会因为完成绑定就自动改变模型回复。

### 第五步：设置游标并本地预览（仅本地）

1. 选择剧情当前所在的卷和章；
2. 设置回看窗口、Top K 和注入 Token 预算；
3. 点击“本地预览检索范围”，检查允许参与检索的章节；
4. 确认无误后保存游标。

“本地预览检索范围”只展示白名单和预算，不调用 Embedding，也不执行向量查询。

未来窗口固定为 `0`，时间河不会检索当前章节之后的原文。回看窗口只决定允许向前回看多少章。

### 第六步：按需开启生成前注入（实际生成可能调用外部服务）

需要让原文参考真正参与回复时：

1. 勾选“启用生成前原文窗口注入”；
2. 点击“保存游标与注入设置”；
3. 正常发送消息。

启用后，每次生成前会：

1. 把当前聊天内容转换为查询向量；
2. 只查询当前章和回看窗口内的章节；
3. 在 Top K 与 Token 预算内选取相关原文片段；
4. 将这些片段作为参考加入当前模型上下文。

这可能产生 Embedding 调用费用，并占用对话模型的提示词 Token。时间河不会把整本小说一次性发送给对话模型。

剧情推进到下一章后，需要手动移动章节游标并保存；当前版本不会自动推进。

## 状态关系

```text
本地导入与预览
  → 持久化为 staging
  → 构建向量索引
  → 自动发布为 active
  → 绑定当前聊天
  → 开启并保存注入
  → 生成时检索当前章与过去窗口
```

## 哪些操作可能产生费用

| 操作 | 是否联网或调用模型 |
| --- | --- |
| 选择文件、解析目录、编辑标题 | 仅本地 |
| 确认并持久化 | 仅本地 |
| 开始 / 继续构建索引 | 可能调用 Embedding，并写入向量服务 |
| 重试失败项 | 可能再次调用 Embedding；已有逐块缓存时会尽量复用 |
| 本地预览检索范围 | 仅本地，不调用 Embedding 或向量查询 |
| 绑定聊天、调整并保存游标 | 仅本地 |
| 开启注入后的实际生成 | 会进行查询向量化和向量检索，并把命中的片段送入对话模型上下文 |

## 常见问题与排错

### 时间河页面不可用或所有操作被禁用

- 确认当前账号具有有效的 Type2 或 Type3 授权；
- 授权失效后，时间河会停止新任务、检索和注入，但不会自动删除已有本地数据；
- 恢复授权后重新打开页面并手动继续任务。

### 可以导入，但索引立即失败

优先检查“API 连接配置 → 智能检索 · 向量化”：

- 是否已经创建 Embedding 连接；
- 是否已把该连接分配给“智能检索 · 向量化”；
- 模型名称、接口地址、密钥和向量维度是否正确；
- 不要把普通对话连接当作 Embedding 连接。

修复配置后，重新选择 `staging` Revision，核对费用提示，再点击“重试失败项”或“开始 / 继续”。

### “确认并持久化”按钮不可用

- 先选择文件并完成“导入小说并预览目录”；
- 检查目录和空间估算；
- 勾选当前导入目标对应的确认项；
- 修改文件、目录或目标后，旧确认会失效，需要重新核对并勾选。

### “开始 / 继续”按钮不可用

- 确认当前选择的是 `staging` Revision；
- 重新勾选与当前 Corpus、Revision、任务数和模型配置对应的费用确认；
- 如果已有任务正在其他标签页执行，本页只能观察；
- 如果 Revision 已是 `active`，无需再次构建。

### “绑定到当前聊天”按钮不可用

- 只有 `active` Revision 可以绑定；
- 先打开或创建一个已经保存的聊天；
- 确认页面当前选择的 Corpus 和 Revision 正是需要绑定的版本。

### 已绑定，但回复里看不到时间河效果

- 确认“启用生成前原文窗口注入”已经勾选并保存；
- 确认当前 Embedding 配置与构建该 `active` Revision 时一致；
- 确认当前卷章游标正确，且允许的章节中确实存在相关内容；
- 先用“本地预览检索范围”检查章节白名单；
- Top K 或 Token 预算过小时，可在合理范围内提高后再保存。

### 检索内容太旧或剧情位置不对

- 手动把游标移动到当前剧情所在章节并保存；
- 减小回看窗口可降低较早章节参与检索的机会；
- 未来窗口固定为 `0`，不能通过设置放开未来章节。

### 刷新或中断后任务没有自动继续

这是预期行为。为避免在用户不知情时继续产生费用，刷新后不会自动续跑：

1. 重新选择对应的 `staging` Revision；
2. 检查任务统计和错误信息；
3. 重新确认费用；
4. 点击“开始 / 继续”或“重试失败项”。

### 更换 Embedding 模型后无法检索

时间河会阻止使用新 Embedding 配置查询旧模型构建的索引。请恢复原配置，或使用当前配置重新导入并构建新的 Revision；不要混用不同模型的向量索引。

### 需要提交问题报告

点击“导出脱敏诊断”。诊断用于记录任务状态和错误分类，不应包含 API Key、小说正文或 Embedding 数组。提交前仍建议自行检查文件内容。

## 当前版本边界

当前版本提供本地小说导入、章级故事轨索引、手动章节游标和生成前受控检索注入。以下能力尚未开放：

- 自动判断并推进剧情章节；
- 结构化世界构建；
- 偏离检测、世界线重算与推进建议；
- 自动把时间河语料同步到其他浏览器或设备。
