# 表格模块 · 进阶操作

## 总览

| 区 | 能干什么 |
|---|---|
| 总开关 / 表格注入 | 系统开 + AI 能否看见表 |
| 填表模式 | 分步 / 兼容 / 批量等 |
| 提取规则 | 分步与批量用哪条规则 |
| 独立API填表 | 另开一条对话通道（分配「独立API填表」） |
| 每批处理表数 | 同一正文分多次只处理少量表，降低多表注意力干扰 |
| 缓存预热并行填表 | 第 1 个真实子批完成后，并行执行其余子批 |
| 移除填表随机头 | 让相同正文与规则的跨轮提示前缀更稳定 |
| 楼层填表 | 当前楼 / 选定楼 / 填表记录回滚 |
| 重整 / 模板 | 整理表结构、导入导出主题 |

## 操作表

| 操作 | 步骤 |
|---|---|
| 主填表 | 分配「表格填表/重整」对话 → 总开关开 |
| 独立通道 | 分配「独立API填表」→ 开「启用独立API填表」 |
| 让 AI 看见表 | 开「表格注入」 |
| 选规则 | 规则配置建规则 → 本页「提取规则」下拉 |
| 填当前楼 | 「填当前楼层」 |
| 填一段 | 填起始/结束楼 →「选定楼层填表」 |
| 救回表 | 「填表记录」→ 选楼层快照恢复 |
| 批量 | 按页内批量设置跑多楼 |
| 测试独立 API | 独立区块里「测试连接 / 获取模型」 |

## 多表少量多次

`每批处理表数` 控制一次模型请求最多以多少张表为装箱目标：

| 值 | 行为 |
|---|---|
| `0` | 兼容旧行为，一次发送全部可填用户表 |
| `1-128` | 正文、历史和规则保持不变，分多次发送不同的表格子集 |

建议从 `3-6` 开始测试。设置越小，模型每次更聚焦，但请求次数、延迟和 RPM 占用越高；设置越大则更接近旧版行为。

已声明外键的用户表会自动作为一个关系组件同批处理，不会为了满足数字而强行拆开。整轮最多 `16` 个表格子批；若提示超过上限，请调大该参数。只有所有子批都通过校验后，当前逻辑填表才会提交，任一子批损坏或越界都不会留下半批数据。

开启“缓存预热并行填表”后，插件先正常完成第 1 个真实子批，让服务商有机会缓存共同的正文/规则前缀；随后按“并行请求上限”（默认 `4`，范围 `1-16`；非法值回退为 `4`，超上限截断为 `16`）同时执行其余子批。这里不会额外发送要求空回的请求，因此相较串行子批不会增加独立预热调用；第 1 批也不会提前写表，仍等全部子批通过后一次提交。

“移除填表随机头”省略的是消息链首条随机 `system` 提示，不是 HTTP Header。默认关闭以保持旧行为；开启后相同正文、规则和世界书在不同填表轮次之间更容易形成一致前缀。

提示词缓存、并发能力与计费都由服务商决定。RPM 较低时保持串行或把并发设为 `1-2`；SillyTavern 预设模式因需要切换宿主全局 Profile，会由插件内部锁强制串行。直接 API 也仍受 Profile RPM 限流器约束。

完整边界、请求预算及与两阶段填表的区别见：[v2.3.2 按表子批填充](docs/table-database/table-sub-batch-filling.md)。
## 模式怎么选

| 模式 | 适合 |
|---|---|
| 分步 | 新手、稳一点 |
| 兼容优化 | 多数中转 |
| 批量 | 大段回填，注意 token |

## 翻车加细

| 现象 | 再查 |
|---|---|
| 没反应 | 总开关、分配、提取规则、独立开关 |
| 表被清空 | 用「填表记录」回滚到好版本 |
| AI 不看表 | 表格注入 |
| 类型不匹配 | 槽必须挂 **对话** |
| 公益站 tools 报错 | 关 tools / 换站（页内有提示） |

## 仍解决不了

回首页 **作者留言 / 公告板**，点 **【01】群** 或 **【02】群** 加群求助。
