# 智能检索（照着点就行）

## 模型

| 需要 | 认法 | 别用 |
|---|---|---|
| **向量**（必配） | 名字带 `embedding` / `embed` / `bge` | DeepSeek/Gemini **聊天** |
| **重排**（可选） | 名字带 `rerank` | 聊天、embedding |

| 商家写的 | 这里选 |
|---|---|
| Chat / 对话 / Gemini / DeepSeek-Chat | **对话**（给别的模块） |
| Embedding / 向量 | **向量** |
| Rerank | **重排** |

## 怎么开

```
API连接：加向量(embedding) → 可选再加重排
分配：「智能检索·向量化」挂向量并开开关
本页：开「启用智能检索」→ 录入知识 → 聊天自动搜
```

## 翻车

| 现象 | 你做 |
|---|---|
| 没效果/报错 | 向量槽是不是挂成聊天模型了 |
| 类型不匹配 | 向量槽只挂 **向量** |
| 不准 | 再配 **重排** |
