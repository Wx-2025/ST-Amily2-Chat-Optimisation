import{normalizeCharacterId}from"../../core/table-system/character-profile.js";import{captureChatScope,chatScopesMatch}from"../../core/table-system/infra/chat-scope.js";export const IMPORT_SYNC_CHARACTER_PROFILE_SETTING="table_import_sync_character_profile";export function bindImportSyncCharacterProfileSetting(checkbox,getSettings,saveSettings){if(!checkbox)return()=>{};checkbox.checked=!0===getSettings?.()?.table_import_sync_character_profile;const persist=()=>{const settings=getSettings?.();settings&&"object"==typeof settings&&(settings.table_import_sync_character_profile=!0===checkbox.checked,saveSettings?.())};return checkbox.addEventListener("change",persist),()=>checkbox.removeEventListener("change",persist)}export function captureCharacterProfileTarget(context){const characterId=normalizeCharacterId(context?.characterId),character=null===characterId?null:context?.characters?.[characterId];return character?Object.freeze({characterId:characterId,character:character,identity:readCharacterIdentity(character),chatScope:captureChatScope(context)}):null}export function isSameCharacterProfileTarget(context,target){if(!target)return!1;const characterId=normalizeCharacterId(context?.characterId);if(null===characterId||characterId!==target.characterId)return!1;const character=context?.characters?.[characterId];if(!character)return!1;const identity=readCharacterIdentity(character);return!(character!==target.character&&!Boolean(identity&&target.identity&&identity===target.identity))&&characterProfileChatScopesMatch(target.chatScope,captureChatScope(context))}function readCharacterIdentity(character){const avatar=character?.avatar??character?.data?.avatar;return"string"==typeof avatar&&avatar.length>0?`avatar:${avatar}`:null}function characterProfileChatScopesMatch(expected,actual){return expected?.chatId||actual?.chatId?chatScopesMatch(expected,actual):expected?.chatId===actual?.chatId&&expected?.epoch===actual?.epoch&&expected?.chatRef===actual?.chatRef&&expected?.metadataRef===actual?.metadataRef}