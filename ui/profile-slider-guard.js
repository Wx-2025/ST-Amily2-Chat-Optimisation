import{apiProfileManager}from"../utils/config/ApiProfileManager.js";import{escapeHTML}from"../utils/utils.js";export function applyProfileSliderGuard(slot,selectors){const $els=$(selectors.join(", "));if(0===$els.length)return;const profileId=apiProfileManager.getAssignment(slot),profile=profileId?apiProfileManager.getProfile(profileId):null,$anchor=$els.first().closest("div, label");$anchor.parent().find(".amily2-profile-guard-hint").remove(),profile?($els.prop("disabled",!0).css("opacity","0.5"),$anchor.after(`<div class="amily2-profile-guard-hint" style="font-size: 0.85em; opacity: 0.75; margin: 4px 0;"><i class="fa-solid fa-lock" style="margin-right: 4px;"></i>以上参数当前由连接配置「${escapeHTML(profile.name||profileId)}」控制，请在 API 连接配置面板修改。</div>`)):$els.prop("disabled",!1).css("opacity","")}export function watchProfileSliderGuard(slot,selectors){applyProfileSliderGuard(slot,selectors),document.addEventListener("amily2-profile-assignment-changed",e=>{e.detail?.slot===slot&&applyProfileSliderGuard(slot,selectors)})}