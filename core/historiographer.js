import{getContext,extension_settings}from"/scripts/extensions.js";import{characters}from"/script.js";import{extractBlocksByTags,applyExclusionRules}from"./utils/rag-tag-extractor.js";import{world_names,loadWorldInfo,createNewWorldInfo,createWorldInfoEntry}from"/scripts/world-info.js";import{saveBook as loreSaveBook,saveBookStrictUnlocked,mutateBookStrict,withLoreLock}from"./lore-service.js";import{extensionName}from"../utils/settings.js";import{getChatIdentifier,refreshWorldbookListOnly}from"./lore.js";import{ingestTextToHanlinyuan}from"./rag-processor.js";import{showSummaryModal,showHtmlModal}from"../ui/page-window.js";import{getPresetPrompts,getMixedOrder}from"../PresetSettings/index.js";import{generateRandomSeed}from"./api.js";import{callNgmsAI}from"./api/Ngms_api.js";import{executeAutoHide}from"./autoHideManager.js";import{resolveHistoriographyRuleConfig}from"../utils/config/RuleProfileManager.js";import{PROGRESS_SEAL_REGEX,appendMicroSummaryBlock,buildRollingRefinementInput,containsReservedLedgerStructure,estimateHistoriographyMessagesTokens,estimateHistoriographyTokens,normalizeRefinementLimits,parseLedgerForRefinement,rollingLedgerFitsBudget}from"./historiography-ledger.js";let reloadEditor=()=>{console.warn("[大史官] reloadEditor 函数不可用，可能是旧版本。已使用空函数代替。")};(async()=>{try{const{reloadEditor:importedReloadEditor}=await import("/scripts/world-info.js");importedReloadEditor&&(reloadEditor=importedReloadEditor,console.log("[大史官] 已成功动态导入 reloadEditor。"))}catch(error){console.warn("[大史官] 动态导入 reloadEditor 失败，将使用空函数。错误信息：",error.message)}})();let isExpeditionRunning=!1,manualStopRequested=!1;export const RUNNING_LOG_COMMENT="【敕史局】对话流水总帐";export async function readGoldenLedgerProgress(targetLorebookName){if(!targetLorebookName)return 0;try{const bookData=await loadWorldInfo(targetLorebookName);if(!bookData||!bookData.entries)return 0;const ledgerEntry=Object.values(bookData.entries).find(e=>"【敕史局】对话流水总帐"===e.comment&&!e.disable);if(!ledgerEntry)return 0;const match=ledgerEntry.content.match(PROGRESS_SEAL_REGEX);return match?parseInt(match[1],10):0}catch(error){return console.error(`[大史官] 阅览《${targetLorebookName}》天机时出错:`,error),0}}const refinementReminderKeys=new Set,vectorizedRollingContentFingerprints=new Set;async function runHistoriographyPostCommitEffect(label,effect){try{await effect()}catch(error){console.warn(`[大史官] 史册已提交，但后续动作“${label}”失败:`,error)}}async function fingerprintText(text){const value=String(text??""),subtle=globalThis.crypto?.subtle;if(subtle&&"function"==typeof TextEncoder){const digest=await subtle.digest("SHA-256",(new TextEncoder).encode(value));return`sha256:${Array.from(new Uint8Array(digest),byte=>byte.toString(16).padStart(2,"0")).join("")}`}let hash=2166136261;for(let index=0;index<value.length;index++)hash^=value.charCodeAt(index),hash=Math.imul(hash,16777619);return`fnv1a:${(hash>>>0).toString(16).padStart(8,"0")}`}export async function getActiveLedgerRefinementStatus(){const targetLorebookName=await getTargetLorebookName();if(!targetLorebookName)return{available:!1,targetLorebookName:null,loreKey:null,parsed:null};const bookData=await loadWorldInfo(targetLorebookName),loreKey=Object.keys(bookData?.entries||{}).find(key=>"【敕史局】对话流水总帐"===bookData.entries[key].comment&&!bookData.entries[key].disable);if(!loreKey)return{available:!1,targetLorebookName:targetLorebookName,loreKey:null,parsed:null};const entry=bookData.entries[loreKey];return{available:!0,targetLorebookName:targetLorebookName,loreKey:loreKey,parsed:parseLedgerForRefinement(entry.content)}}async function maybeNotifyRefinementThreshold(){try{const settings=extension_settings[extensionName]||{},limits=normalizeRefinementLimits(settings),status=await getActiveLedgerRefinementStatus(),pendingBlocks=status.parsed?.pendingBlockCount||0;if(!status.available||pendingBlocks<limits.reminderBlocks)return;const reminderBand=Math.floor(pendingBlocks/limits.reminderBlocks),reminderKey=`${status.targetLorebookName}:${status.loreKey}:${status.parsed?.compiledFloor||0}:${reminderBand}`;if(refinementReminderKeys.has(reminderKey))return;refinementReminderKeys.add(reminderKey),toastr.warning(`当前活动史册已有 ${pendingBlocks} 个尚未合并的微言录块。建议打开“总结模块 → 大总结（合并精炼）”，点击“合并当前活动史册”。这里只提醒，不会在后台自动调用模型。`,"宏史卷待重铸",{timeOut:1e4})}catch(error){console.warn("[大史官] 检查宏史卷重铸阈值失败，已跳过提醒:",error)}}export async function checkAndTriggerAutoSummary(){if(isExpeditionRunning)return;const settings=extension_settings[extensionName];if(!settings.historiographySmallAutoEnable)return;const context=getContext();let targetLorebookName=null;switch(settings.lorebookTarget){case"character_main":targetLorebookName=characters[context.characterId]?.data?.extensions?.world;break;case"dedicated":targetLorebookName=`Amily2-Lore-${await getChatIdentifier()}`;break;default:return}if(!targetLorebookName)return;const characterCount=await readGoldenLedgerProgress(targetLorebookName),summarizableLength=context.chat.length-(settings.historiographyRetentionCount??5);if(summarizableLength-characterCount>=settings.historiographySmallTriggerThreshold){const batchSize=settings.historiographySmallTriggerThreshold,startFloor=characterCount+1,endFloor=Math.min(characterCount+batchSize,summarizableLength);console.log(`[大史官] 自动微言录已触发，处理 ${startFloor} 至 ${endFloor} 楼。`);const isInteractive=settings.historiographyAutoSummaryInteractive??!1;await executeManualSummary(startFloor,endFloor,!isInteractive)}}export async function getAvailableWorldbooks(){return[...world_names]}export async function getLoresForWorldbook(bookName){if(!bookName)return[];try{const bookData=await loadWorldInfo(bookName);return bookData&&bookData.entries?Object.entries(bookData.entries).filter(([,entry])=>!entry.disable).map(([key,entry])=>({key:key,comment:entry.comment||"无标题条目"})):[]}catch(error){return console.error(`[大史官] 检阅《${bookName}》时出错:`,error),[]}}function escapeHtml(text){return text?text.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#039;"):""}export async function executeManualSummary(startFloor,endFloor,isAuto=!1){return new Promise(async resolve=>{const toastTitle=isAuto?"微言录 (自动)":"微言录 (手动)",context=getContext();if(isAuto){const messages=getRawMessagesForSummary(startFloor,endFloor);if(!messages||0===messages.length)return toastr.warning("自动巡录：未找到符合条件的消息。",toastTitle),resolve(!1);const textToSummarize=messages.map(m=>`【第 ${m.floor} 楼】 ${m.author}: ${m.content}`).join("\n"),summary=await getSummary(textToSummarize,toastTitle);return void(summary?showSummaryModal(summary,{onConfirm:async finalSummary=>{const success=await writeSummary(finalSummary,startFloor,endFloor,toastTitle);resolve(success)},onRegenerate:async summaryDialog=>{summaryDialog.find("textarea").prop("disabled",!0).val("正在重新生成，请稍候...");const newSummary=await getSummary(textToSummarize,toastTitle);summaryDialog.find("textarea").prop("disabled",!1).val(newSummary||summary),summaryDialog[0].showModal(),newSummary||toastr.error("重新生成失败，已恢复原始内容。","模型召唤失败")},onCancel:()=>{toastr.info("本批次总结已取消。",toastTitle),resolve(!1)}}):resolve(!1))}const messages=getRawMessagesForSummary(startFloor,endFloor);if(!messages||0===messages.length)return toastr.warning("选定的楼层范围内无有效对话或内容被规则排除。","圣谕有误"),resolve(!1);const modalHtml=(msgList=>{const messageHtml=msgList.map(msg=>`\n                <details class="historiography-message-item" data-author-type="${msg.authorType}">\n                    <summary>【第 ${msg.floor} 楼】 ${escapeHtml(msg.author)}</summary>\n                    <div class="historiography-editor-container">\n                        <textarea class="text_pole" data-floor="${msg.floor}">${escapeHtml(msg.content)}</textarea>\n                    </div>\n                </details>\n            `).join("");return`\n                <div id="historiography-preview-controls">\n                    <label><input type="checkbox" id="hist-include-user" checked> ${context.name1||"用户"}</label>\n                    <label><input type="checkbox" id="hist-include-char" checked> ${context.name2||"角色"}</label>\n                </div>\n                <div id="historiography-preview-container">${messageHtml}</div>\n                <style>\n                    #historiography-preview-controls { margin-bottom: 10px; display: flex; gap: 15px; }\n                    #historiography-preview-container { height: 65vh; overflow-y: auto; border: 1px solid #444; padding: 5px; }\n                    .historiography-message-item { margin-bottom: 5px; }\n                    .historiography-message-item[hidden] { display: none; }\n                    .historiography-message-item summary { cursor: pointer; padding: 5px; background-color: #333; }\n                    .historiography-editor-container { padding: 10px; border: 1px solid #444; border-top: none; }\n                    .historiography-editor-container textarea { height: 150px; resize: vertical; }\n                </style>\n            `})(messages);showHtmlModal("原文预览与编辑",modalHtml,{okText:"确认原文并总结",cancelText:"取消",onOpen:dialog=>{const userCheckbox=dialog.find("#hist-include-user"),charCheckbox=dialog.find("#hist-include-char"),container=dialog.find("#historiography-preview-container"),updateVisibility=()=>{const includeUser=userCheckbox.is(":checked"),includeChar=charCheckbox.is(":checked");container.find(".historiography-message-item").each(function(){const item=$(this),authorType=item.data("author-type"),shouldBeHidden="user"===authorType&&!includeUser||"char"===authorType&&!includeChar;item.toggle(!shouldBeHidden)})};userCheckbox.on("change",updateVisibility),charCheckbox.on("change",updateVisibility)},onOk:async dialog=>{const includeUser=dialog.find("#hist-include-user").is(":checked"),includeChar=dialog.find("#hist-include-char").is(":checked"),textToSummarize=dialog.find(".historiography-message-item").filter(function(){const authorType=$(this).data("author-type");return!("user"===authorType&&!includeUser)&&!("char"===authorType&&!includeChar)}).find("textarea").map(function(){const floor=$(this).data("floor");return`【第 ${floor} 楼】 ${$(this).closest(".historiography-message-item").find("summary").text().replace(`【第 ${floor} 楼】 `,"")}: ${$(this).val()}`}).get().join("\n");if(!textToSummarize.trim())return void toastr.error("请至少选择一条消息进行总结！","圣谕有误");const dialogElement=dialog[0];dialogElement&&"function"==typeof dialogElement.close&&dialogElement.close(),dialog.remove();const summary=await getSummary(textToSummarize,toastTitle);summary?showSummaryModal(summary,{onConfirm:async finalSummary=>{const success=await writeSummary(finalSummary,startFloor,endFloor,toastTitle);resolve(success)},onRegenerate:async summaryDialog=>{summaryDialog.find("textarea").prop("disabled",!0).val("正在重新生成，请稍候...");const newSummary=await getSummary(textToSummarize,toastTitle);summaryDialog.find("textarea").prop("disabled",!1).val(newSummary||summary),summaryDialog[0].showModal(),newSummary||toastr.error("重新生成失败，已恢复原始内容。","模型召唤失败")},onCancel:()=>{toastr.info("本批次总结已取消。","操作已取消"),resolve(!1)}}):resolve(!1)},onCancel:()=>{toastr.info("操作已取消。",toastTitle),resolve(!1)}})})}function getRawMessagesForSummary(startFloor,endFloor){const context=getContext(),chat=context.chat,settings=extension_settings[extensionName],historySlice=chat.slice(startFloor-1,endFloor);if(0===historySlice.length)return null;const userName=context.name1||"用户",characterName=context.name2||"角色",historiographyRuleConfig=resolveHistoriographyRuleConfig(settings),useTagExtraction=historiographyRuleConfig.tagExtractionEnabled??!1,tagsToExtract=useTagExtraction?(historiographyRuleConfig.tags||"").split(",").map(t=>t.trim()).filter(Boolean):[],exclusionRules=historiographyRuleConfig.exclusionRules||[],excludeUserMessages=historiographyRuleConfig.excludeUserMessages??!1;return historySlice.map((msg,index)=>{if(excludeUserMessages&&msg.is_user)return null;let content=msg.mes;if(useTagExtraction&&tagsToExtract.length>0){const blocks=extractBlocksByTags(content,tagsToExtract);blocks.length>0&&(content=blocks.join("\n\n"))}return content=applyExclusionRules(content,exclusionRules),content.trim()?{floor:startFloor+index,author:msg.is_user?userName:characterName,authorType:msg.is_user?"user":"char",content:content.trim()}:null}).filter(Boolean)}async function getSummary(formattedHistory,toastTitle,retryCount=0){toastr.info("正在为您熔铸对话历史...",toastTitle);const settings=extension_settings[extensionName],presetPrompts=await getPresetPrompts("small_summary");let mixedOrder;try{const savedOrder=localStorage.getItem("amily2_prompt_presets_v2_mixed_order");savedOrder&&(mixedOrder=JSON.parse(savedOrder))}catch(e){console.error("[大史官] 加载混合顺序失败:",e)}const order=getMixedOrder("small_summary")||[],messages=[{role:"system",content:generateRandomSeed()}];let promptCounter=0;for(const item of order)if("prompt"===item.type)presetPrompts&&presetPrompts[promptCounter]&&(messages.push(presetPrompts[promptCounter]),promptCounter++);else if("conditional"===item.type)switch(item.id){case"jailbreakPrompt":settings.historiographySmallJailbreakPrompt&&messages.push({role:"system",content:settings.historiographySmallJailbreakPrompt});break;case"summaryPrompt":settings.historiographySmallSummaryPrompt&&messages.push({role:"system",content:settings.historiographySmallSummaryPrompt});break;case"coreContent":messages.push({role:"user",content:`请严格根据以下"对话记录"中的内容进行总结，不要添加任何额外信息。\n\n<对话记录>\n${formattedHistory}\n</对话记录>`})}const summary=await callNgmsAI(messages);if(console.log("[大史官-微言录] AI回复的全部内容:",summary),!summary||!summary.trim()){const maxRetries=settings.historiographyMaxRetries??2;return retryCount<maxRetries?(console.warn(`[大史官-微言录] AI返回空内容，正在进行第 ${retryCount+1}/${maxRetries} 次重试...`),toastr.warning(`AI返回空内容，正在进行第 ${retryCount+1}/${maxRetries} 次重试...`,toastTitle),await new Promise(resolve=>setTimeout(resolve,3e3)),await getSummary(formattedHistory,toastTitle,retryCount+1)):(console.error(`[大史官-微言录] 达到最大重试次数 (${maxRetries})，总结失败。`),toastr.error(`达到最大重试次数 (${maxRetries})，总结失败。`,toastTitle),null)}return summary}async function writeSummary(summary,startFloor,endFloor,toastTitle){const settings=extension_settings[extensionName],context=getContext(),shouldWriteToLorebook=settings.historiographyWriteToLorebook??!0,shouldIngestToRag=settings.historiographyIngestToRag??!1,refinementLimits=normalizeRefinementLimits(settings);if(!shouldWriteToLorebook&&!shouldIngestToRag)return toastr.warning("“写入史册”和“存入翰林院”均未启用，总结任务已完成但未保存。",toastTitle),!0;let targetLorebookName;try{switch(settings.lorebookTarget){case"character_main":if(targetLorebookName=characters[context.characterId]?.data?.extensions?.world,!targetLorebookName)throw new Error("当前角色未绑定主世界书。");break;case"dedicated":{const chatIdentifier=await getChatIdentifier();targetLorebookName=`Amily2-Lore-${chatIdentifier}`;break}default:throw new Error("未知的史册写入指令。")}}catch(error){return toastr.error(`无法确定史册目标：${error.message}`,"国史馆"),!1}const ingestMicroSummary=async()=>{try{toastr.info("正在将此份“微言录”送往翰林院...","翰林院");const metadata={bookName:targetLorebookName,entryName:`微言录总结: ${startFloor}-${endFloor}楼`},result=await ingestTextToHanlinyuan(summary,"lorebook",metadata);if(!result.success)throw new Error(result.error);return toastr.success("翰林院已成功接收记忆碎片！","翰林院"),!0}catch(ragError){return console.error("[翰林院] 向量化处理失败:",ragError),toastr.error(`送往翰林院的文书处理失败: ${ragError.message}`,"翰林院"),!1}};if(shouldWriteToLorebook){const firstLedgerAppend=appendMicroSummaryBlock("",summary,startFloor,endFloor,refinementLimits.inputMaxTokens);if(!firstLedgerAppend.fits)return toastr.error(`单批微言录写入后约 ${firstLedgerAppend.estimatedTokens} Token，已经超过活动史册硬上限 ${firstLedgerAppend.maxTokens}。本批次未写入史册，进度未前移；请缩小每次总结层数或提高上限。`,"微言录过长",{timeOut:12e3}),!1;try{let ledgerBudgetExceeded=null;console.log("[大史官-调试] 读取到的原始设置:",{loreActivationMode:settings.loreActivationMode,loreInsertionPosition:settings.loreInsertionPosition,loreDepth:settings.loreDepth,loreKeywords:settings.loreKeywords});const optionsForNewEntry={keys:settings.loreKeywords.split(",").map(k=>k.trim()).filter(Boolean),isConstant:"keyed"!==settings.loreActivationMode,insertion_position:settings.loreInsertionPosition,depth:settings.loreDepth};console.log("[大史官-调试] 构建并传递的选项:",optionsForNewEntry);const targetWasListed=world_names.includes(targetLorebookName),persistence=await mutateBookStrict(targetLorebookName,candidateBookData=>{if(!candidateBookData.entries||"object"!=typeof candidateBookData.entries||Array.isArray(candidateBookData.entries))throw new Error("目标世界书缺少有效的 entries 结构");const existingEntry=Object.values(candidateBookData.entries).find(entry=>"【敕史局】对话流水总帐"===entry?.comment&&!entry.disable),appendResult=appendMicroSummaryBlock(existingEntry?.content||"",summary,startFloor,endFloor,refinementLimits.inputMaxTokens);if(!appendResult.fits)return ledgerBudgetExceeded={estimatedTokens:appendResult.estimatedTokens,maxTokens:appendResult.maxTokens},{changed:!1,reason:"ledger-budget"};let targetEntry=existingEntry;if(!targetEntry){if(targetEntry=createWorldInfoEntry(targetLorebookName,candidateBookData),!targetEntry)throw new Error("无法创建活动史册条目");const positionMap={before_char:0,after_char:1,before_an:2,after_an:3,at_depth:4};Object.assign(targetEntry,{comment:"【敕史局】对话流水总帐",key:optionsForNewEntry.keys,constant:optionsForNewEntry.isConstant,position:positionMap[optionsForNewEntry.insertion_position]??4,depth:Number.parseInt(optionsForNewEntry.depth,10)||998,disable:!1})}return targetEntry.content=appendResult.content,{changed:!0,createdBook:!targetWasListed}});return ledgerBudgetExceeded?(toastr.error(`追加后活动史册约 ${ledgerBudgetExceeded.estimatedTokens} Token，将超过硬上限 ${ledgerBudgetExceeded.maxTokens}，因此本批次尚未写入、总结进度也没有前移。请先在“大总结”中合并当前活动史册，再继续补全；原聊天与原史册均保持不变。`,"活动史册已达上限",{timeOut:14e3}),!1):persistence.committed?(persistence.mutation?.createdBook&&await runHistoriographyPostCommitEffect("刷新世界书列表",()=>refreshWorldbookListOnly(targetLorebookName)),await runHistoriographyPostCommitEffect("刷新世界书编辑器",()=>reloadEditor(targetLorebookName)),await runHistoriographyPostCommitEffect("发布世界书更新通知",()=>document.dispatchEvent(new CustomEvent("amily-lorebook-created",{detail:{bookName:targetLorebookName}}))),await runHistoriographyPostCommitEffect("显示写入成功提示",()=>toastr.success("编年史已成功更新！",`${toastTitle} - 国史馆`)),await runHistoriographyPostCommitEffect("自动隐藏已总结消息",()=>executeAutoHide()),await runHistoriographyPostCommitEffect("检查宏史卷合并提醒",()=>maybeNotifyRefinementThreshold()),shouldIngestToRag&&await ingestMicroSummary(),!0):(console.warn("[大史官] 严格史册写入未产生持久化提交。"),!1)}catch(error){return console.error(`[大史官] ${toastTitle}写入国史馆失败:`,error),toastr.error(`写入国史馆时发生错误: ${error.message}`,"国史馆"),!1}}return!shouldIngestToRag||ingestMicroSummary()}export async function executeRefinement(worldbook,loreKey,options={}){toastr.info(`遵旨！正在为您重铸《${worldbook}》中的【微言录合集】...`,"宏史卷重铸");try{const bookData=await loadWorldInfo(worldbook),entry=bookData?.entries[loreKey];if(!entry)return void toastr.error("找不到指定的史册条目，重铸任务中止。","圣谕有误");const originalContent=String(entry.content||""),originalEntrySnapshot=Object.freeze({loreKey:String(loreKey),key:JSON.stringify(entry.key??[]),uid:String(entry.uid??loreKey),comment:String(entry.comment??""),disable:Boolean(entry.disable),content:originalContent}),settings=extension_settings[extensionName],limits=normalizeRefinementLimits(settings),parsedLedger=parseLedgerForRefinement(originalContent);if("legacy-vector-placeholder"===parsedLedger.reason)return void toastr.error("该史册仍使用“旧宏史卷已由翰林院向量化注入”的占位格式，当前条目并不包含早期剧情正文。请先从禁用的旧宏史卷正本恢复，或从翰林院导出并恢复早期正文；在正文恢复前不会生成虚假的 1-N 楼滚动宏史卷。","需要先恢复早期正文",{timeOut:16e3});if(!parsedLedger.valid)return void toastr.error("史册缺少【流水金印】，无法执行重铸。","结构异常");if(!parsedLedger.pendingMicroContent.trim()||parsedLedger.pendingBlockCount<=0)return void toastr.warning("史册条目中没有新的内容可供重铸。","国库无新事");const presetPrompts=await getPresetPrompts("large_summary"),order=getMixedOrder("large_summary")||[],requestSeed=generateRandomSeed(),buildRefinementMessages=(refinementInput,selectedBlockCount)=>{const selectedEndFloor=parsedLedger.pendingBlocks[selectedBlockCount-1]?.endFloor||parsedLedger.compiledFloor||0,messages=[{role:"system",content:requestSeed}];let promptCounter=0,coreContentInserted=!1;for(const item of order)if("prompt"!==item.type){if("conditional"===item.type)switch(item.id){case"jailbreakPrompt":settings.historiographyLargeJailbreakPrompt&&messages.push({role:"system",content:settings.historiographyLargeJailbreakPrompt});break;case"summaryPrompt":settings.historiographyLargeRefinePrompt&&messages.push({role:"system",content:settings.historiographyLargeRefinePrompt});break;case"coreContent":coreContentInserted||(messages.push({role:"user",content:`<核心处理内容>\n\n${refinementInput}\n\n</核心处理内容>`}),coreContentInserted=!0)}}else presetPrompts&&presetPrompts[promptCounter]&&(messages.push(presetPrompts[promptCounter]),promptCounter++);return coreContentInserted||messages.push({role:"user",content:`<核心处理内容>\n\n${refinementInput}\n\n</核心处理内容>`}),messages.push({role:"system",content:`必须把“既有宏史卷”和本批“待合并微言录”合并成一份覆盖 1-${selectedEndFloor} 楼的滚动宏史卷。不得只总结新增部分，不得遗漏既有宏史卷中的关键事实。最终输出不得超过约 ${limits.activeMaxTokens} Token；仅输出可直接写入宏史卷的正文，不得输出流水金印、篇章封印或微言录标题。`}),messages},buildCandidate=selectedBlockCount=>{const refinementInput=buildRollingRefinementInput(parsedLedger,selectedBlockCount),messages=buildRefinementMessages(refinementInput,selectedBlockCount);return{selectedBlockCount:selectedBlockCount,refinementInput:refinementInput,messages:messages,estimatedTokens:estimateHistoriographyMessagesTokens(messages)}},fixedCandidate=buildCandidate(0);if(fixedCandidate.estimatedTokens>limits.inputMaxTokens)return void toastr.error(`既有宏史卷与完整固定提示约 ${fixedCandidate.estimatedTokens} Token，已经超过输入上限 ${limits.inputMaxTokens}。本次没有调用模型；请先手动缩短宏史卷或提高输入上限。`,"宏史卷固定输入超限",{timeOut:14e3});let low=1,high=parsedLedger.pendingBlockCount,selectedCandidate=null;for(;low<=high;){const middle=Math.floor((low+high)/2),candidate=buildCandidate(middle);candidate.estimatedTokens<=limits.inputMaxTokens?(selectedCandidate=candidate,low=middle+1):high=middle-1}if(!selectedCandidate){const oneBlockCandidate=buildCandidate(1);return void toastr.error(`完整固定提示、既有宏史卷与最旧一块微言录约 ${oneBlockCandidate.estimatedTokens} Token，超过输入上限 ${limits.inputMaxTokens}。本次没有调用模型；请提高输入上限或手动缩短既有宏史卷。`,"单块仍无法安全重铸",{timeOut:14e3})}const selectedBlockCount=selectedCandidate.selectedBlockCount,remainingBlockCount=parsedLedger.pendingBlockCount-selectedBlockCount,messages=selectedCandidate.messages;remainingBlockCount>0&&toastr.info(`本轮将在输入上限内合并最旧的 ${selectedBlockCount} 块；其余 ${remainingBlockCount} 块会原样保留。完成后可由您手动继续下一轮。`,"分段重铸",{timeOut:1e4});const getRefinedContent=async(retryCount=0)=>{toastr.info("正在召唤模型进行内容精炼...","宏史卷重铸");const content=await callNgmsAI(messages,{maxTokens:limits.activeMaxTokens});if(!content||!content.trim()){const maxRetries=settings.historiographyMaxRetries??2;return retryCount<maxRetries?(console.warn(`[大史官-宏史卷重铸] AI返回空内容，正在进行第 ${retryCount+1}/${maxRetries} 次重试...`),toastr.warning(`AI返回空内容，正在进行第 ${retryCount+1}/${maxRetries} 次重试...`,"宏史卷重铸"),await new Promise(resolve=>setTimeout(resolve,3e3)),await getRefinedContent(retryCount+1)):(console.error(`[大史官-宏史卷重铸] 达到最大重试次数 (${maxRetries})，重铸失败。`),toastr.error(`达到最大重试次数 (${maxRetries})，重铸失败。`,"宏史卷重铸失败"),null)}return content},initialRefinedContent=await getRefinedContent();if(!initialRefinedContent)return;const processLoop=async currentRefinedContent=>{showSummaryModal(currentRefinedContent,{onConfirm:async editedText=>{if(containsReservedLedgerStructure(editedText))return toastr.error("输出中包含史册保留结构标记（流水金印、篇章封印或微言录标题）。为避免破坏后续解析，本次没有写入；请在重新打开的预览中删除这些标记。","宏史卷结构冲突",{timeOut:12e3}),void setTimeout(()=>processLoop(editedText),0);const budgetResult=rollingLedgerFitsBudget(editedText,parsedLedger,limits.activeMaxTokens,selectedBlockCount);if(!budgetResult.fits)return toastr.error(`当前滚动宏史卷约 ${budgetResult.estimatedTokens} Token，超过宏史卷上限 ${budgetResult.maxTokens}。为避免不可控上下文膨胀，本次没有写入；请在重新打开的预览中继续缩减。`,"宏史卷仍过长",{timeOut:12e3}),void setTimeout(()=>processLoop(editedText),0);const finalContent=budgetResult.content;if(finalContent)try{await withLoreLock(`rollingRefinement(${worldbook}:${loreKey})`,async()=>{if(options.requireCurrentTarget){if(await getTargetLorebookName()!==worldbook){const staleError=new Error("当前聊天的目标世界书已经变化");throw staleError.code="HISTORIOGRAPHY_STALE_LEDGER",staleError}}const latestBookData=await loadWorldInfo(worldbook),latestEntry=latestBookData?.entries?.[loreKey],latestActiveLoreKey=Object.keys(latestBookData?.entries||{}).find(key=>"【敕史局】对话流水总帐"===latestBookData.entries[key].comment&&!latestBookData.entries[key].disable);if(!latestEntry||String(loreKey)!==originalEntrySnapshot.loreKey||options.requireCurrentTarget&&String(latestActiveLoreKey??"")!==originalEntrySnapshot.loreKey||JSON.stringify(latestEntry.key??[])!==originalEntrySnapshot.key||String(latestEntry.uid??loreKey)!==originalEntrySnapshot.uid||String(latestEntry.comment??"")!==originalEntrySnapshot.comment||Boolean(latestEntry.disable)!==originalEntrySnapshot.disable||String(latestEntry.content||"")!==originalEntrySnapshot.content){const staleError=new Error("预览期间活动史册已发生变化");throw staleError.code="HISTORIOGRAPHY_STALE_LEDGER",staleError}const candidateBookData=structuredClone(latestBookData),candidateEntry=candidateBookData?.entries?.[loreKey];if(!candidateEntry)throw new Error("无法建立独立的史册候选草稿");const archiveEntry=createWorldInfoEntry(worldbook,candidateBookData);if(!archiveEntry)throw new Error("无法创建重铸前正本存档条目");const oldFloorLabel=parsedLedger.compiledFloor>0?`原宏史卷至${parsedLedger.compiledFloor}楼`:"首次重铸",archiveTimestamp=(new Date).toISOString().replace(/[:.]/g,"-").slice(0,19);archiveEntry.comment=`【敕史局】对话流水总帐_归档_${archiveTimestamp}_重铸前正本_1-${parsedLedger.totalFloors}楼（${oldFloorLabel}）`,archiveEntry.content=originalContent,archiveEntry.disable=!0,archiveEntry.constant=!1,archiveEntry.key=[],candidateEntry.content=finalContent,await saveBookStrictUnlocked(worldbook,candidateBookData)}),reloadEditor(worldbook),toastr.success(`史册已重铸为一份滚动宏史卷；重铸前完整正本已禁用归档于《${worldbook}》。`+(remainingBlockCount>0?` 尚余 ${remainingBlockCount} 块微言录，请按需手动继续合并。`:" 本轮已合并全部待处理微言录。"),"宏史卷重铸完毕",{timeOut:1e4});if((document.getElementById("amily2_vectorize_summary_content")?.checked??!1)&&parsedLedger.existingRollingContent)try{const fingerprint=await fingerprintText(parsedLedger.existingRollingContent);if(vectorizedRollingContentFingerprints.has(fingerprint))return void toastr.info("同一旧宏史卷本次会话已向量化，已跳过重复入库。","翰林院");toastr.info(`正在将前 ${parsedLedger.compiledFloor} 楼的旧宏史卷副本送往翰林院...`,"翰林院");const ingestResult=await ingestTextToHanlinyuan(parsedLedger.existingRollingContent,"lorebook",{bookName:worldbook,entryName:`宏史卷总结: 1-${parsedLedger.compiledFloor}楼 [${fingerprint}]`});if(!(ingestResult.success&&ingestResult.count>0))throw new Error(ingestResult.error||`向量化未产生条目（count=${ingestResult.count??0}）`);vectorizedRollingContentFingerprints.add(fingerprint),toastr.success(`旧宏史卷副本已进入翰林院，共 ${ingestResult.count} 条。`,"翰林院")}catch(error){console.error("[大史官-宏史卷向量化] 失败:",error),toastr.warning(`滚动宏史卷已安全保存，但可选向量化失败：${error.message}`,"翰林院",{timeOut:1e4})}}catch(error){if("HISTORIOGRAPHY_STALE_LEDGER"===error?.code)return void toastr.warning("预览期间活动史册已发生变化，本次旧结果已安全丢弃。请重新开始合并。","史册已更新",{timeOut:1e4});console.error("[大史官-宏史卷存档] 写入失败:",error),toastr.error(`无法同时保存滚动宏史卷与重铸前正本：${error.message}。原活动史册保持不变。`,"宏史卷保存失败",{timeOut:12e3})}else toastr.error("无法构建滚动宏史卷，原始史册保持不变。","重铸失败")},onRegenerate:async dialog=>{dialog.find("textarea").prop("disabled",!0).val("正在重新生成，请稍候...");const newContent=await getRefinedContent();dialog.find("textarea").prop("disabled",!1).val(newContent||currentRefinedContent),dialog[0].showModal(),newContent||toastr.error("重新生成失败，已恢复原始内容。","模型召唤失败")},onCancel:()=>{toastr.info("宏史卷重铸操作已取消。","操作已取消")}})};await processLoop(initialRefinedContent)}catch(error){console.error("[大史官] 重铸任务失败:",error),toastr.error(`重铸史册时发生严重错误: ${error.message}`,"国史馆")}}export async function executeActiveLedgerRefinement(){try{const status=await getActiveLedgerRefinementStatus();return status.available&&status.targetLorebookName&&status.loreKey?"legacy-vector-placeholder"===status.parsed?.reason?(toastr.error("该活动史册仍使用“旧宏史卷已由翰林院向量化注入”的占位格式，条目内没有可供重铸的早期剧情正文。请先恢复旧宏史卷正文再重铸。","需要先恢复早期正文",{timeOut:16e3}),!1):status.parsed?.valid?status.parsed.pendingMicroContent.trim()?(await executeRefinement(status.targetLorebookName,status.loreKey,{requireCurrentTarget:!0}),!0):(toastr.info("活动史册没有新的微言录块需要合并。","宏史卷重铸"),!1):(toastr.error("活动史册缺少流水金印，无法安全重铸。","结构异常"),!1):(toastr.warning("当前聊天没有可重铸的活动【对话流水总帐】。","宏史卷重铸"),!1)}catch(error){return console.error("[大史官] 无法打开当前活动史册重铸:",error),toastr.error(`无法读取当前活动史册：${error.message}`,"宏史卷重铸"),!1}}export async function executeExpedition(){if(isExpeditionRunning)toastr.info("补全总结正在进行中，请稍候。","总结");else{isExpeditionRunning=!0,manualStopRequested=!1,document.dispatchEvent(new CustomEvent("amily2-expedition-state-change",{detail:{isRunning:!0}}));try{const settings=extension_settings[extensionName],context=getContext();let targetLorebookName=null;switch(settings.lorebookTarget){case"character_main":if(targetLorebookName=characters[context.characterId]?.data?.extensions?.world,!targetLorebookName)return toastr.error("当前角色未绑定主世界书，无法写入总结。","总结"),isExpeditionRunning=!1,void document.dispatchEvent(new CustomEvent("amily2-expedition-state-change",{detail:{isRunning:!1,manualStop:!1}}));break;case"dedicated":targetLorebookName=`Amily2-Lore-${await getChatIdentifier()}`;break;default:return toastr.error("写入目标无效，请检查世界书设置。","总结"),isExpeditionRunning=!1,void document.dispatchEvent(new CustomEvent("amily2-expedition-state-change",{detail:{isRunning:!1,manualStop:!1}}))}const summarizedCount=await readGoldenLedgerProgress(targetLorebookName),retentionCount=settings.historiographyRetentionCount??5,summarizableLength=context.chat.length-retentionCount,remainingHistory=summarizableLength-summarizedCount;if(remainingHistory<=0)return toastr.info("没有需要补全的历史，已经是最新。","总结"),isExpeditionRunning=!1,void document.dispatchEvent(new CustomEvent("amily2-expedition-state-change",{detail:{isRunning:!1,manualStop:!1}}));const batchSize=settings.historiographySmallTriggerThreshold,totalBatches=Math.ceil(remainingHistory/batchSize);toastr.info(`开始补全：还有 ${remainingHistory} 层，分 ${totalBatches} 批处理。`,"开始总结");let currentProgress=summarizedCount;for(let i=0;i<totalBatches;i++){if(manualStopRequested){toastr.warning("已暂停。点「继续补全」可接着做。","总结");break}const startFloor=currentProgress+1,endFloor=Math.min(currentProgress+batchSize,summarizableLength),toastTitle=`补全进度 (${i+1}/${totalBatches})`,delay=2e3;if(i>0&&(toastr.info(`准备第 ${i+1} 批…（${delay/1e3} 秒后开始）`,toastTitle),await new Promise(resolve=>setTimeout(resolve,delay))),manualStopRequested){toastr.warning("已在准备阶段暂停。","总结");break}if(!await executeManualSummary(startFloor,endFloor,!1)){toastr.warning(`第 ${i+1} 批失败，补全已中止。`,"总结"),manualStopRequested=!0;break}currentProgress=endFloor}manualStopRequested||toastr.success("补全完成，未总结的历史已全部处理。","开始总结")}catch(error){console.error("[大史官-补全失败]",error),toastr.error("补全过程出错已中止，可点「继续补全」重试。","总结")}finally{isExpeditionRunning=!1,document.dispatchEvent(new CustomEvent("amily2-expedition-state-change",{detail:{isRunning:!1,manualStop:manualStopRequested}}))}}}export function stopExpedition(){isExpeditionRunning?(manualStopRequested=!0,toastr.info("已请求停止，当前这一批结束后会停下。","总结")):toastr.warning("当前没有正在进行的补全。","总结")}export async function executeCompilation(worldbook,loreKeys){if(!Array.isArray(loreKeys)||0===loreKeys.length)return toastr.warning("未选择任何条目进行编纂。","圣谕不明"),{success:!1,error:"No lore keys provided."};toastr.info(`遵旨！开始对《${worldbook}》中的 ${loreKeys.length} 个条目进行批量编纂...`,"翰林院入库");let totalSuccessCount=0,totalVectorCount=0,errors=[];try{const bookData=await loadWorldInfo(worldbook);if(!bookData||!bookData.entries)throw new Error(`无法加载书库《${worldbook}》的数据。`);for(const loreKey of loreKeys){const entry=bookData.entries[loreKey];if(!entry){errors.push(`条目【${loreKey}】未找到。`);continue}const contentToIngest=entry.content;if(!contentToIngest.trim()){errors.push(`条目【${entry.comment||loreKey}】内容为空。`);continue}const metadata={bookName:worldbook,entryName:entry.comment||loreKey};try{const ingestResult=await ingestTextToHanlinyuan(contentToIngest,"lorebook",metadata);ingestResult.success?(totalSuccessCount++,totalVectorCount+=ingestResult.count):errors.push(`条目【${entry.comment||loreKey}】处理失败: ${ingestResult.error}`)}catch(ingestError){errors.push(`条目【${entry.comment||loreKey}】处理时发生严重错误: ${ingestError.message}`)}}let finalMessage=`批量编纂完成！\n成功处理 ${totalSuccessCount} / ${loreKeys.length} 个条目，共新增 ${totalVectorCount} 条忆识。`;return errors.length>0?(finalMessage+=`\n\n发生以下错误:\n- ${errors.join("\n- ")}`,toastr.warning("批量编纂期间发生部分错误，详情请查看控制台。","翰林院"),console.warn("[翰林院] 批量编纂错误详情:",errors)):toastr.success(`批量编纂大功告成！新增 ${totalVectorCount} 条忆识。`,"翰林院"),{success:0===errors.length,content:finalMessage,totalSuccess:totalSuccessCount,totalVectors:totalVectorCount,errors:errors}}catch(error){return console.error("[翰林院] 批量条目入库失败:",error),toastr.error(`批量入库失败: ${error.message}`,"翰林院"),{success:!1,error:error.message}}}export async function getTargetLorebookName(){const settings=extension_settings[extensionName],context=getContext();let targetLorebookName=null;switch(settings.lorebookTarget){case"character_main":targetLorebookName=characters[context.characterId]?.data?.extensions?.world;break;case"dedicated":targetLorebookName=`Amily2-Lore-${await getChatIdentifier()}`}return targetLorebookName}export async function archiveCurrentLedger(){try{const targetLorebookName=await getTargetLorebookName();if(!targetLorebookName)return toastr.error("无法确定目标世界书，归档失败。","圣谕不明"),!1;const bookData=await loadWorldInfo(targetLorebookName);if(!bookData||!bookData.entries)return toastr.error(`无法读取世界书《${targetLorebookName}》。`,"国史馆"),!1;const ledgerEntryKey=Object.keys(bookData.entries).find(key=>"【敕史局】对话流水总帐"===bookData.entries[key].comment&&!bookData.entries[key].disable);if(!ledgerEntryKey)return toastr.info("当前没有活跃的【对话流水总帐】，无需归档。","国史馆"),!1;const entry=bookData.entries[ledgerEntryKey],newComment=`【敕史局】对话流水总帐_归档_${(new Date).toISOString().replace(/[:.]/g,"-").slice(0,19)}`;return entry.comment=newComment,entry.disable=!0,await loreSaveBook(targetLorebookName,bookData),reloadEditor(targetLorebookName),toastr.success(`已将当前流水总帐归档为：\n${newComment}`,"归档成功"),!0}catch(error){return console.error("[大史官] 归档失败:",error),toastr.error(`归档失败: ${error.message}`,"国史馆"),!1}}export async function getArchivedLedgers(){try{const targetLorebookName=await getTargetLorebookName();if(!targetLorebookName)return[];const bookData=await loadWorldInfo(targetLorebookName);if(!bookData||!bookData.entries)return[];return Object.entries(bookData.entries).filter(([,entry])=>entry.comment&&entry.comment.startsWith("【敕史局】对话流水总帐_归档_")).map(([key,entry])=>({key:key,comment:entry.comment})).sort((a,b)=>b.comment.localeCompare(a.comment))}catch(error){return console.error("[大史官] 获取归档列表失败:",error),[]}}export async function restoreArchivedLedger(targetLoreKey){try{const targetLorebookName=await getTargetLorebookName();if(!targetLorebookName)return toastr.error("无法确定目标世界书，回溯失败。","圣谕不明"),!1;const bookData=await loadWorldInfo(targetLorebookName);if(!bookData||!bookData.entries)return toastr.error(`无法读取世界书《${targetLorebookName}》。`,"国史馆"),!1;const targetEntry=bookData.entries[targetLoreKey];if(!targetEntry)return toastr.error("找不到指定的归档史册。","圣谕有误"),!1;const currentActiveKey=Object.keys(bookData.entries).find(key=>"【敕史局】对话流水总帐"===bookData.entries[key].comment&&!bookData.entries[key].disable);if(currentActiveKey&&currentActiveKey!==targetLoreKey){const activeEntry=bookData.entries[currentActiveKey],timestamp=(new Date).toISOString().replace(/[:.]/g,"-").slice(0,19);activeEntry.comment=`【敕史局】对话流水总帐_归档_${timestamp}`,activeEntry.disable=!0,toastr.info(`已自动归档原有的活跃史册为: ${activeEntry.comment}`,"自动归档")}return targetEntry.comment="【敕史局】对话流水总帐",targetEntry.disable=!1,await loreSaveBook(targetLorebookName,bookData),reloadEditor(targetLorebookName),toastr.success("史册回溯成功！时光已倒流，旧史重现。","回溯成功"),!0}catch(error){return console.error("[大史官] 回溯失败:",error),toastr.error(`回溯失败: ${error.message}`,"国史馆"),!1}}