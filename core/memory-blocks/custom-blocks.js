import{extension_settings}from"/scripts/extensions.js";import{saveSettingsDebounced}from"/script.js";import{extensionName}from"../../utils/settings.js";import{register,unregister,listAll}from"./registry.js";const STORAGE_KEY="memoryBlocks_customBlocks";export const CUSTOM_ID_PREFIX="custom.";export function isCustomBlockId(id){return"string"==typeof id&&id.startsWith("custom.")}function getStore(){extension_settings[extensionName]||(extension_settings[extensionName]={});const s=extension_settings[extensionName];return Array.isArray(s[STORAGE_KEY])||(s[STORAGE_KEY]=[]),s[STORAGE_KEY]}function persist(){saveSettingsDebounced()}function newCustomId(){return`custom.${Date.now().toString(36)}${Math.random().toString(36).slice(2,7)}`}export function syncCustomBlocksFromSettings(){for(const b of listAll())isCustomBlockId(b.id)&&unregister(b.id);for(const def of getStore())try{register(def)}catch(error){console.warn("[MemoryBlocks] 自定义块定义损坏，已跳过:",def,error)}}export function listCustomBlocks(context){const store=getStore();return context?store.filter(b=>b.context===context):[...store]}export function getCustomBlock(id){return getStore().find(b=>b.id===id)??null}export function addCustomBlock(def){const full={enabled:!0,...def,id:newCustomId()};return register(full),getStore().push(full),persist(),full}export function updateCustomBlock(id,patch){if(!isCustomBlockId(id))throw new Error(`[MemoryBlocks] 仅自定义块可修改: ${id}`);const store=getStore(),idx=store.findIndex(b=>b.id===id);if(-1===idx)throw new Error(`[MemoryBlocks] 自定义块不存在: ${id}`);const merged={...store[idx],...patch,id:id};return register(merged),store[idx]=merged,persist(),merged}export function deleteCustomBlock(id){if(!isCustomBlockId(id))return!1;const store=getStore(),idx=store.findIndex(b=>b.id===id);return-1!==idx&&(store.splice(idx,1),unregister(id),persist(),!0)}