import{extension_settings}from"/scripts/extensions.js";import{saveSettingsDebounced}from"/script.js";import{extensionName}from"../../utils/settings.js";import{register,unregister,listAll}from"./registry.js";const STORAGE_KEY="memoryBlocks_customBlocks";export const CUSTOM_ID_PREFIX="custom.";export function isCustomBlockId(id){return typeof id==="string"&&id.startsWith(CUSTOM_ID_PREFIX)}function getStore(){if(!extension_settings[extensionName])extension_settings[extensionName]={};const s=extension_settings[extensionName];if(!Array.isArray(s[STORAGE_KEY]))s[STORAGE_KEY]=[];return s[STORAGE_KEY]}function persist(){saveSettingsDebounced()}function newCustomId(){return`${CUSTOM_ID_PREFIX}${Date.now().toString(36)}${Math.random().toString(36).slice(2,7)}`}export function syncCustomBlocksFromSettings(){for(const b of listAll()){if(isCustomBlockId(b.id))unregister(b.id)}for(const def of getStore()){try{register(def)}catch(error){console.warn(`[MemoryBlocks] 自定义块定义损坏，已跳过:`,def,error)}}}export function listCustomBlocks(context){const store=getStore();return context?store.filter(b=>b.context===context):[...store]}export function getCustomBlock(id){return getStore().find(b=>b.id===id)??null}export function addCustomBlock(def){const full={enabled:true,...def,id:newCustomId()};register(full);getStore().push(full);persist();return full}export function updateCustomBlock(id,patch){if(!isCustomBlockId(id))throw new Error(`[MemoryBlocks] 仅自定义块可修改: ${id}`);const store=getStore();const idx=store.findIndex(b=>b.id===id);if(idx===-1)throw new Error(`[MemoryBlocks] 自定义块不存在: ${id}`);const merged={...store[idx],...patch,id:id};register(merged);store[idx]=merged;persist();return merged}export function deleteCustomBlock(id){if(!isCustomBlockId(id))return false;const store=getStore();const idx=store.findIndex(b=>b.id===id);if(idx===-1)return false;store.splice(idx,1);unregister(id);persist();return true}