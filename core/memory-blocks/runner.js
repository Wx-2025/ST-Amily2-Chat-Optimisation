import{getHandler}from"./generator-handlers.js";import{listByContext}from"./registry.js";export async function runBlock(block,ctx){const handler=getHandler(block.generator?.type);if(!handler)return console.warn(`[MemoryBlocks] 未注册的 generator 类型 "${block.generator?.type}"，块 ${block.id} 已跳过。`),null;try{const value=await handler(block,ctx);return null==value?null:{block:block,value:String(value)}}catch(error){if("AbortError"===error?.name)throw error;return console.error(`[MemoryBlocks] 块 ${block.id} 生成失败:`,error),null}}export async function executeContext({context:context,settings:settings,signal:signal,extras:extras}={}){const blocks=listByContext(context);if(0===blocks.length)return[];const ctx={settings:settings??{},signal:signal,context:context,extras:extras};return await Promise.all(blocks.map(b=>runBlock(b,ctx)))}