const VALID_MODES=new Set(["main-api","secondary-api","batch","floor-range"]),subscribers=new Set;export function dispatchTableFillStart(event){const snapshot=normalizeTableFillStart(event),currentSubscribers=[...subscribers];for(const subscriber of currentSubscribers)try{const result=subscriber(snapshot);result&&"function"==typeof result.then&&Promise.resolve(result).catch(error=>{console.error("[TableFillLifecycle] async subscriber failed:",error)})}catch(error){console.error("[TableFillLifecycle] subscriber failed:",error)}return currentSubscribers.length}export function subscribeTableFillStarts(subscriber){if("function"!=typeof subscriber)throw new TypeError("[TableFillLifecycle] subscriber must be a function.");return subscribers.add(subscriber),()=>subscribers.delete(subscriber)}function normalizeTableFillStart(event){if(!isPlainObject(event)||!VALID_MODES.has(event.mode))throw new TypeError("[TableFillLifecycle] event mode is invalid.");const hasStart=Object.hasOwn(event,"startFloor");if(hasStart!==Object.hasOwn(event,"endFloor"))throw new TypeError("[TableFillLifecycle] floor bounds must be supplied together.");if(!hasStart)return Object.freeze({mode:event.mode});if(!Number.isSafeInteger(event.startFloor)||!Number.isSafeInteger(event.endFloor)||event.startFloor<1||event.endFloor<event.startFloor)throw new TypeError("[TableFillLifecycle] floor bounds are invalid.");return Object.freeze({mode:event.mode,startFloor:event.startFloor,endFloor:event.endFloor})}function isPlainObject(value){if(!value||"object"!=typeof value||Array.isArray(value))return!1;const prototype=Object.getPrototypeOf(value);return prototype===Object.prototype||null===prototype}