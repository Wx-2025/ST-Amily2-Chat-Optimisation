import{extension_settings,getContext}from"/scripts/extensions.js";import{characters,eventSource,event_types}from"/script.js";import{loadWorldInfo,createNewWorldInfo,createWorldInfoEntry,saveWorldInfo,world_names,updateWorldInfoList}from"/scripts/world-info.js";import{compatibleWriteToLorebook,safeLorebooks,safeCharLorebooks,safeLorebookEntries}from"./tavernhelper-compatibility.js";import{extensionName}from"../utils/settings.js";import{isActiveChatContext}from"./utils/chat-context-state.js";document.addEventListener("amily-lorebook-created",event=>{event.detail&&event.detail.bookName&&(console.log(`[Amily2-国史馆] 监听到史书《${event.detail.bookName}》变更，即刻通报工部刷新宫殿。`),refreshWorldbookListOnly(event.detail.bookName))});export const LOREBOOK_PREFIX="Amily2档案-";export const DEDICATED_LOREBOOK_NAME="Amily2号-国史馆";export const INTRODUCTORY_TEXT="【Amily2号自动档案】\n此卷宗由Amily2号优化助手自动生成并维护，记录核心事件脉络。\n---\n";export async function getChatIdentifier(){let attempts=0;if(!isActiveChatContext(getContext()))return"unknown_chat_inactive";for(;attempts<50;){try{const context=getContext();if(!isActiveChatContext(context))return"unknown_chat_inactive";if(context&&void 0!==context.characterId&&null!==context.characterId){const character=characters[context.characterId];return character&&character.avatar?`char-${character.avatar.replace(/\.(png|webp|jpg|jpeg|gif)$/,"")}`:`char-${context.characterId}`}if(context&&context.chat_filename){return context.chat_filename.split(/[\\/]/).pop().replace(/\.jsonl?$/,"")}}catch(error){isActiveChatContext(getContext())&&console.warn(`[Amily2-户籍管理处] 等待上下文时发生轻微错误 (尝试次数 ${attempts+1}):`,error.message)}await new Promise(resolve=>setTimeout(resolve,100)),attempts++}return isActiveChatContext(getContext())&&(console.error("[Amily2-国史馆] 已进入聊天，但户籍管理处仍无法确定户籍。"),toastr.warning("Amily2号无法确定当前聊天身份，世界书功能将受影响。","上下文错误")),"unknown_chat_timeout"}export async function findLatestSummaryLore(lorebookName,chatIdentifier){try{const bookData=await loadWorldInfo(lorebookName);if(!bookData||!bookData.entries)return null;const entriesArray=Object.values(bookData.entries),uniqueLoreName=`Amily2档案-${chatIdentifier}`;return entriesArray.find(entry=>entry.comment===uniqueLoreName&&!entry.disable)||null}catch(error){return console.error(`[Amily2-国史馆] 钦差大臣在 '${lorebookName}' 检索时发生错误:`,error),null}}export async function getCombinedWorldbookContent(lorebookName){if(!lorebookName)return"";try{const bookData=await loadWorldInfo(lorebookName);if(!bookData||!bookData.entries)return"";return Object.values(bookData.entries).filter(entry=>!entry.disable).map(entry=>`[条目: ${entry.comment||"无标题"}]\n${entry.content}`).join("\n\n---\n\n")}catch(error){return console.error(`[Amily2-国史馆] 钦差大臣在整合 '${lorebookName}' 时发生错误:`,error),toastr.error(`读取世界书 '${lorebookName}' 失败!`,"档案整合错误"),""}}export async function refreshWorldbookListOnly(newBookName=null){console.log("[Amily2号-工部-v2.0] 执行SillyTavern核心UI刷新...");try{await updateWorldInfoList(),console.log("[Amily2号-工部] SillyTavern核心刷新函数 (updateWorldInfoList) 调用成功。")}catch(error){console.error("[Amily2号-工部] 调用核心刷新函数时出错:",error),toastr.error("Amily2号调用核心UI刷新函数时失败。","核心刷新失败")}}export async function writeSummaryToLorebook(pendingData){if(!(pendingData&&pendingData.summary&&pendingData.sourceAiMessageTimestamp&&pendingData.settings))return void console.warn("[Amily助手-国史馆] 接到一份残缺的待办文书，写入任务已中止。",pendingData);const context=getContext(),chat=context.chat;let isSourceMessageValid=!1,sourceMessageCandidate=null;for(let i=chat.length-1;i>=0;i--)if(!chat[i].is_user){sourceMessageCandidate=chat[i];break}if(sourceMessageCandidate&&sourceMessageCandidate.send_date===pendingData.sourceAiMessageTimestamp&&(isSourceMessageValid=!0),!isSourceMessageValid)return void console.log("[Amily助手-逆时寻踪] 裁决: 源消息已被修改或删除，遵旨废黜过时总结。");const{summary:summaryToCommit,settings:settings}=pendingData;console.groupCollapsed(`[Amily助手-存档任务] ${(new Date).toLocaleTimeString()}`),console.time("总结写入总耗时");try{const chatIdentifier=await getChatIdentifier(),character=characters[context.characterId];let targetLorebookName=null;switch(settings.target){case"character_main":if(targetLorebookName=character?.data?.extensions?.world,!targetLorebookName)return toastr.warning("角色未绑定主世界书，总结写入任务已中止。","Amily助手"),void console.groupEnd();break;case"dedicated":targetLorebookName=`Amily2号-国史馆-${chatIdentifier}`;break;default:return toastr.error(`收到未知的写入指令: "${settings.target}"`,"Amily助手"),void console.groupEnd()}const uniqueLoreName=`Amily2档案-${chatIdentifier}`,contentUpdateCallback=existingContent=>{if(existingContent){const cleanedContent=existingContent.replace(INTRODUCTORY_TEXT,"").trim();return`${existingContent}\n${(cleanedContent?cleanedContent.split("\n"):[]).length+1}. ${summaryToCommit}`}return`${INTRODUCTORY_TEXT}1. ${summaryToCommit}`},options={keys:settings.keywords.split(",").map(k=>k.trim()).filter(Boolean),isConstant:"always"===settings.activationMode,insertion_position:settings.insertionPosition,depth:settings.depth};await compatibleWriteToLorebook(targetLorebookName,uniqueLoreName,contentUpdateCallback,options)?toastr.success(`总结已成功写入《${targetLorebookName}》！`,"Amily助手"):toastr.error(`总结写入《${targetLorebookName}》时失败。`,"Amily助手")}catch(error){console.error("[Amily助手-写入失败] 写入流程发生意外错误:",error),toastr.error("后台写入总结时发生错误。","Amily助手")}finally{console.timeEnd("总结写入总耗时"),console.groupEnd()}}export async function getOptimizationWorldbookContent(){const settings=extension_settings[extensionName];if(!settings||!settings.modal_wbEnabled)return"";try{let bookNames=[];if("manual"===settings.modal_wbSource)bookNames=settings.modal_amily2_wb_selected_worldbooks||[];else{const charLorebooks=await safeCharLorebooks({type:"all"});charLorebooks.primary&&bookNames.push(charLorebooks.primary),charLorebooks.additional?.length&&bookNames.push(...charLorebooks.additional)}if(0===bookNames.length)return console.log("[Amily2-正文优化] No world books selected or linked for optimization."),"";let allEntries=[];for(const bookName of bookNames)if(bookName){const entries=await safeLorebookEntries(bookName);entries?.length&&entries.forEach(entry=>allEntries.push({...entry,bookName:bookName}))}const selectedEntriesConfig=settings.modal_amily2_wb_selected_entries||{},userEnabledEntries=allEntries.filter(entry=>{if(!entry.enabled)return!1;const bookConfig=selectedEntriesConfig[entry.bookName];return!!bookConfig&&bookConfig.includes(String(entry.uid))});if(0===userEnabledEntries.length)return console.log("[Amily2-正文优化] No entries are selected for optimization in the chosen world books."),"";const combinedContent=userEnabledEntries.map(entry=>entry.content).filter(Boolean).join("\n\n---\n\n");return console.log(`[Amily2-正文优化] Loaded ${userEnabledEntries.length} world book entries, total length: ${combinedContent.length}`),combinedContent}catch(error){return console.error("[Amily2-正文优化] Processing world book content failed:",error),""}}export async function getPlotOptimizedWorldbookContent(context,apiSettings,isConcurrent=!1){const panel=$("#amily2_plot_optimization_panel");let liveSettings={};const isPanelReady=panel.length>0&&panel.find('#amily2_opt_worldbook_entry_list_container input[type="checkbox"]').length>0;if(isConcurrent)console.log("[剧情优化大师] 检测到并发调用，强制使用传入的并发世界书设置。"),liveSettings={worldbookEnabled:apiSettings.plotOpt_worldbook_enabled,worldbookSource:apiSettings.plotOpt_worldbook_source||"character",selectedWorldbooks:apiSettings.plotOpt_selectedWorldbooks||[],autoSelectWorldbooks:apiSettings.plotOpt_autoSelectWorldbooks||[],worldbookCharLimit:apiSettings.plotOpt_worldbookCharLimit,enabledWorldbookEntries:null};else if(isPanelReady){liveSettings.worldbookEnabled=panel.find("#amily2_opt_worldbook_enabled").is(":checked"),liveSettings.newMemoryLogicEnabled=panel.find("#amily2_opt_new_memory_logic_enabled").is(":checked"),liveSettings.worldbookSource=panel.find('input[name="amily2_opt_worldbook_source"]:checked').val()||"character",liveSettings.selectedWorldbooks=[],"manual"===liveSettings.worldbookSource&&panel.find('#amily2_opt_worldbook_checkbox_list input[type="checkbox"]:not(.amily2_opt_wb_auto_check):checked').each(function(){liveSettings.selectedWorldbooks.push($(this).val())}),liveSettings.autoSelectWorldbooks=[],panel.find("#amily2_opt_worldbook_checkbox_list input.amily2_opt_wb_auto_check:checked").each(function(){liveSettings.autoSelectWorldbooks.push($(this).data("book"))}),liveSettings.worldbookCharLimit=parseInt(panel.find("#amily2_opt_worldbook_char_limit").val(),10)||6e4,liveSettings.contextLimit=parseInt(panel.find("#amily2_opt_context_limit").val(),10)||5;let enabledEntries={};panel.find('#amily2_opt_worldbook_entry_list_container input[type="checkbox"]:checked').each(function(){const bookName=$(this).data("book"),uid=parseInt($(this).data("uid"));enabledEntries[bookName]||(enabledEntries[bookName]=[]),enabledEntries[bookName].push(uid)}),liveSettings.enabledWorldbookEntries=enabledEntries}else panel.length>0?console.warn("[剧情优化大师] 检测到UI面板但内容未完全加载，回退到使用已保存的设置。"):console.warn("[剧情优化大师] 未找到设置面板，世界书功能将使用已保存的设置。"),liveSettings={worldbookEnabled:apiSettings.plotOpt_worldbookEnabled,newMemoryLogicEnabled:apiSettings.plotOpt_newMemoryLogicEnabled,worldbookSource:apiSettings.plotOpt_worldbookSource||"character",selectedWorldbooks:apiSettings.plotOpt_selectedWorldbooks,autoSelectWorldbooks:apiSettings.plotOpt_autoSelectWorldbooks||[],worldbookCharLimit:apiSettings.plotOpt_worldbookCharLimit,contextLimit:apiSettings.plotOpt_contextLimit??apiSettings.plotOpt_contextTurnCount??5,enabledWorldbookEntries:apiSettings.plotOpt_enabledWorldbookEntries};if(!liveSettings.worldbookEnabled)return"";if(!context)return console.warn("[剧情优化大师] context 未提供，无法获取世界书内容。"),"";try{let bookNames=[];if("manual"===liveSettings.worldbookSource){if(bookNames=liveSettings.selectedWorldbooks,0===bookNames.length)return""}else{const charLorebooks=await safeCharLorebooks({type:"all"});if(charLorebooks.primary&&bookNames.push(charLorebooks.primary),charLorebooks.additional?.length&&bookNames.push(...charLorebooks.additional),0===bookNames.length)return""}let allEntries=[];for(const bookName of bookNames)if(bookName){const entries=await safeLorebookEntries(bookName);entries?.length&&entries.forEach(entry=>allEntries.push({...entry,bookName:bookName}))}if(0===allEntries.length)return"";const enabledEntriesMap=liveSettings.enabledWorldbookEntries,autoSelectedBooks=liveSettings.autoSelectWorldbooks||[],userEnabledEntries=allEntries.filter(entry=>{if(!entry.enabled)return!1;if(liveSettings.newMemoryLogicEnabled){const character=characters[context.characterId],charName=character?character.data?.name||character.name:null;if(charName&&entry.bookName===`Amily2_Memory_${charName}`){return[...new Set([...entry.key||[],...entry.keys||[]])].some(k=>k.includes("索引"))?(entry.constant=!0,entry.prevent_recursion=!0):entry.constant=!1,!0}}const isAuto=autoSelectedBooks.includes(entry.bookName);if(isConcurrent||isAuto)return entry.constant=!0,!0;if(enabledEntriesMap){const bookConfig=enabledEntriesMap[entry.bookName];return!!bookConfig&&(bookConfig.includes(entry.uid)||bookConfig.includes(String(entry.uid)))&&(entry.constant=!0),!0}return!1});if(0===userEnabledEntries.length)return"";let messagesToScan=context.chat;liveSettings.contextLimit>0&&(messagesToScan=context.chat.slice(-liveSettings.contextLimit));const chatHistory=messagesToScan.map(message=>message.mes).join("\n").toLowerCase(),getEntryKeywords=entry=>[...new Set([...entry.key||[],...entry.keys||[]])].filter(k=>k&&k.trim().length>0).map(k=>k.toLowerCase()),blueLightEntries=userEnabledEntries.filter(entry=>entry.constant);let pendingGreenLights=userEnabledEntries.filter(entry=>!entry.constant);const triggeredEntries=new Set([...blueLightEntries]);for(const entry of pendingGreenLights){const keywords=getEntryKeywords(entry),secondaryKeys=(entry.secondary_keys||[]).filter(k=>k&&k.trim().length>0).map(k=>k.toLowerCase()),selectiveKeys=(entry.selective||[]).filter(k=>k&&k.trim().length>0).map(k=>k.toLowerCase()),checkText=chatHistory,hasPrimary=keywords.length>0&&keywords.some(k=>checkText.includes(k)),hasSecondary=0===secondaryKeys.length||secondaryKeys.some(k=>checkText.includes(k)),hasSelective=selectiveKeys.length>0&&selectiveKeys.some(k=>checkText.includes(k));hasPrimary&&hasSecondary&&!hasSelective&&triggeredEntries.add(entry)}const finalEntries=Array.from(triggeredEntries);finalEntries.sort((a,b)=>{const isIndex=e=>e.constant&&e.prevent_recursion,isTriggered=e=>!e.constant,getPriority=e=>isIndex(e)?1:isTriggered(e)?2:3;return getPriority(a)-getPriority(b)});const finalContent=finalEntries.map(entry=>{const keys=[...new Set([...entry.key||[],...entry.keys||[]])].filter(Boolean).join("、");return`【世界书条目：${entry.comment||`Entry ${entry.uid}`}。绿灯触发关键词：${keys}】\n内容：${entry.content}`}).filter(Boolean);if(0===finalContent.length)return"";const combinedContent=finalContent.join("\n\n---\n\n"),limit=liveSettings.worldbookCharLimit;return combinedContent.length>limit?(console.log(`[剧情优化大师] 世界书内容 (${combinedContent.length} chars) 超出限制 (${limit} chars)，将被截断。`),combinedContent.substring(0,limit)):combinedContent}catch(error){return console.error("[剧情优化大师] 处理世界书逻辑时出错:",error),""}}export async function manageLorebookEntriesForChat(){try{const chatIdentifier=await getChatIdentifier();if(!chatIdentifier||chatIdentifier.startsWith("unknown_chat"))return void console.error("[Amily2-国史馆] 无法获取有效的聊天标识符，中止条目状态管理。");const context=getContext();if(!context||!context.characterId)return void console.log("[Amily2-国史馆] 未选择任何角色，跳过世界书管理。");const charLorebooks=await safeCharLorebooks({type:"all"}),bookNames=[];charLorebooks.primary&&bookNames.push(charLorebooks.primary),charLorebooks.additional?.length&&bookNames.push(...charLorebooks.additional);const dedicatedBookName=`Amily2号-国史馆-${chatIdentifier}`;bookNames.includes(dedicatedBookName)||bookNames.push(dedicatedBookName);for(const bookName of bookNames){if(!world_names.includes(bookName))continue;const entries=await safeLorebookEntries(bookName),entriesToUpdate=[];for(const entry of entries)if(entry.comment&&entry.comment.startsWith("Amily2档案-")){const isForCurrentChat=entry.comment.includes(chatIdentifier);isForCurrentChat&&entry.disable?entriesToUpdate.push({uid:entry.uid,enabled:!0}):isForCurrentChat||entry.disable||entriesToUpdate.push({uid:entry.uid,enabled:!1})}if(entriesToUpdate.length>0){await safeUpdateLorebookEntries(bookName,entriesToUpdate)&&console.log(`[Amily2-国史馆] 已为《${bookName}》更新了 ${entriesToUpdate.length} 个条目的状态以匹配当前聊天: ${chatIdentifier}`)}}}catch(error){console.error("[Amily2-国史馆] 管理世界书条目状态时发生错误:",error)}}