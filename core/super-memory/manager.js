import{extension_settings,getContext}from"/scripts/extensions.js";import{extensionName}from"../../utils/settings.js";import{amilyHelper}from"../tavern-helper/main.js";import{generateIndex}from"./smart-indexer.js";import{syncToLorebook,ensureMemoryBook,updateTransientHint,getMemoryBookName}from"./lorebook-bridge.js";import{getMemoryState}from"../table-system/manager.js";import{inferTableRole}from"../table-system/events-schema.js";import{normalizeTableUpdatePayload,subscribeTableUpdates}from"../internal/table-update-channel.js";import{eventSource,event_types}from"/script.js";let isInitialized=!1,updateQueue=[],isProcessing=!1,_syncPromise=null;export async function awaitSync(){_syncPromise&&await _syncPromise}export async function initializeSuperMemory(){if(parseInt(localStorage.getItem("plugin_user_type")||"0")<2)return console.warn("[Amily2-SuperMemory] 权限不足 (Type < 2)，拒绝初始化超级记忆系统。"),void(window.$&&$("#sm-system-status").text("未授权").css("color","red"));if(!1===(extension_settings[extensionName]||{}).super_memory_enabled)return console.log("[Amily2-SuperMemory] 功能已禁用 (super_memory_enabled = false)。"),void(window.$&&$("#sm-system-status").text("已禁用").css("color","gray"));isInitialized?window.$&&$("#sm-system-status").text("运行中").css("color","#4caf50"):(console.log("[Amily2-SuperMemory] 初始化核心管理器..."),amilyHelper?(subscribeTableUpdates(handleTableUpdate),eventSource.on(event_types.CHAT_CHANGED,async()=>{!1!==(extension_settings[extensionName]||{}).super_memory_enabled&&await checkWorldBookStatus()}),await checkWorldBookStatus(),await forceSyncAll(),isInitialized=!0,console.log("[Amily2-SuperMemory] 核心管理器初始化完成。"),window.$&&$("#sm-system-status").text("运行中").css("color","#4caf50")):console.error("[Amily2-SuperMemory] 致命错误：AmilyHelper 未就绪。"))}async function checkWorldBookStatus(){try{await ensureMemoryBook()}catch(error){console.error("[Amily2-SuperMemory] 检查世界书状态失败:",error)}}function enqueueTableUpdate(payload){const settings=extension_settings[extensionName]||{};if(!1===settings.super_memory_enabled)return;const safePayload=normalizeTableUpdatePayload(payload),minFloor=settings.superMemory_minTriggerFloor??0;if(minFloor>0){const chatLength=getContext()?.chat?.length??0;if(chatLength<minFloor)return void console.log(`[Amily2-SuperMemory] 当前楼层 ${chatLength} < 最低触发楼层 ${minFloor}，跳过同步。`)}const{tableName:tableName,data:data,role:role,headers:headers,rowStatuses:rowStatuses,hint:hint}=safePayload;console.log(`[Amily2-SuperMemory] 收到表格更新: ${tableName} (Role: ${role})`),updateQueue.push({tableName:tableName,data:data,role:role,headers:headers,rowStatuses:rowStatuses,hint:hint}),isProcessing||(_syncPromise=processQueue())}function handleTableUpdate(payload){enqueueTableUpdate(payload)}async function processQueue(){if(!isProcessing&&0!==updateQueue.length){isProcessing=!0;try{for(;updateQueue.length>0;){const consolidatedTasks=new Map,currentBatch=[...updateQueue];updateQueue.length=0;for(const task of currentBatch)consolidatedTasks.set(task.tableName,task);currentBatch.length>consolidatedTasks.size&&console.log(`[Amily2-SuperMemory] 队列优化: 将 ${currentBatch.length} 个事件合并为 ${consolidatedTasks.size} 个操作。`);for(const task of consolidatedTasks.values())await processUpdateTask(task)}}catch(error){console.error("[Amily2-SuperMemory] 处理更新队列失败:",error)}finally{isProcessing=!1,updateQueue.length>0&&(_syncPromise=processQueue())}}}async function processUpdateTask(task){const{tableName:tableName,data:data,role:role,hint:hint,headers:headers,rowStatuses:rowStatuses}=task,settings=extension_settings[extensionName]||{},tableSettings=settings.superMemory_tableSettings?.[tableName]||{};if(!1===tableSettings.sync)return void console.log(`[Amily2-SuperMemory] 表格 ${tableName} 已配置为不写入世界书，跳过同步。`);const isIndexConstant=!1!==tableSettings.constant,activeData=data.filter((_,i)=>!rowStatuses||"pending-deletion"!==rowStatuses[i]),indexText=generateIndex(activeData,headers,role,tableName),tableIndex=getMemoryState().findIndex(t=>t.name===tableName),depth=8001+(tableIndex>=0?tableIndex:99);await syncToLorebook(tableName,data,indexText,role,headers,rowStatuses,{depth:depth,isIndexConstant:isIndexConstant,pinFirstRow:!0===tableSettings.pinFirstRow}),hint&&(console.log(`[Amily2-SuperMemory] 应用主动记忆提示: ${hint}`),await updateTransientHint(hint)),console.log(`[Amily2-SuperMemory] 任务完成: ${tableName}`),updateDashboardCounters()}function updateDashboardCounters(){const tables=getMemoryState();if(tables&&window.$){$("#sm-index-count").text(`${tables.length} 个索引`);const totalRows=tables.reduce((acc,t)=>acc+(t.rows?t.rows.length:0),0);$("#sm-detail-count").text(`${totalRows} 个详情`)}}export async function forceSyncAll(){console.log("[Amily2-SuperMemory] 正在执行全量同步...");const minFloor=(extension_settings[extensionName]||{}).superMemory_minTriggerFloor??0;if(minFloor>0){const chatLength=getContext()?.chat?.length??0;if(chatLength<minFloor)return void console.log(`[Amily2-SuperMemory] 全量同步跳过：当前楼层 ${chatLength} < 最低触发楼层 ${minFloor}。`)}const tables=getMemoryState();if(tables&&0!==tables.length){for(const table of tables)try{updateQueue.push(normalizeTableUpdatePayload({tableId:table.id,tableName:table.name,data:table.rows,headers:table.headers,rowStatuses:table.rowStatuses||[],role:inferTableRole(table.name)}))}catch(error){console.error(`[Amily2-SuperMemory] 已跳过无效或超限表格 ${table?.name||"<unknown>"}:`,error)}isProcessing||(_syncPromise=processQueue()),await _syncPromise,console.log("[Amily2-SuperMemory] 全量同步完成。")}else console.warn("[Amily2-SuperMemory] 没有可同步的表格数据。")}export async function purgeSuperMemory(){try{console.log("[Amily2-SuperMemory] 开始清空记忆...");const bookName=getMemoryBookName(),entries=await amilyHelper.getLorebookEntries(bookName);if(!entries||0===entries.length)return void console.log("[Amily2-SuperMemory] 世界书为空，无需清理。");const entriesToDelete=[],prefixes=["[Amily2]","【Amily2"];for(const entry of entries)entry.comment&&prefixes.some(p=>entry.comment.startsWith(p))&&entriesToDelete.push(entry.uid);entriesToDelete.length>0?(await amilyHelper.deleteLorebookEntries(bookName,entriesToDelete),console.log(`[Amily2-SuperMemory] 已清空 ${entriesToDelete.length} 个条目。`),window.toastr&&toastr.success(`已清空 ${entriesToDelete.length} 条记忆数据`)):window.toastr&&toastr.info("没有发现需要清空的Amily2记忆数据"),updateDashboardCounters()}catch(error){console.error("[Amily2-SuperMemory] 清空失败:",error),window.toastr&&toastr.error("清空失败: "+error.message)}}