import{getContext,extension_settings}from"/scripts/extensions.js";import{extensionName}from"../utils/settings.js";import{processMessageUpdate}from"./table-system/TableSystemService.js";import{messagePipeline}from"./pipeline/MessagePipeline.js";export async function onMessageReceived(data){window.lastPreOptimizationResult=null,document.dispatchEvent(new CustomEvent("preOptimizationTextUpdated"));const context=getContext();if(data&&data.is_user||context.isWaitingForUserInput)return;const settings=extension_settings[extensionName],chat=context.chat;if(!chat||0===chat.length)return;const latestMessage=chat[chat.length-1];latestMessage.is_user||await messagePipeline.execute({messageId:chat.length-1,latestMessage:latestMessage,chat:chat,settings:settings,optimizationResult:null})}export async function handleTableUpdate(messageId){await processMessageUpdate(messageId)}