import{extension_prompt_roles,setExtensionPrompt,eventSource,event_types,saveSettingsDebounced}from"/script.js";import{extension_settings}from"/scripts/extensions.js";import*as ContextUtils from"./utils/context-utils.js";import{getCollectionIdInfo,getCharacterId,getCharacterStableId}from"./utils/context-utils.js";import{defaultSettings as ragDefaultSettings}from"./rag-settings.js";import{extractBlocksByTags,applyExclusionRules}from"./utils/rag-tag-extractor.js";import{resolveQueryPreprocessingRuleConfig}from"../utils/config/RuleProfileManager.js";import{extensionName}from"../utils/settings.js";import*as IngestionManager from"./ingestion-manager.js";import{getEmbeddings,fetchEmbeddingModels as apiFetchEmbeddingModels,fetchRerankModels as apiFetchRerankModels,executeRerank,getRerankSettings,testApiConnection as apiTestApiConnection}from"./rag-api.js";import{superSort}from"./super-sorter.js";import{executeGraphRetrieval}from"./relationship-graph/executor.js";import{initializeArchiveManager}from"./archive-manager.js";const MODULE_NAME="hanlinyuan-rag-core";let context=null,settings=null,lockedCollectionId=null;function filterWorldbooks(searchQuery,worldbooks){if(!searchQuery||!searchQuery.trim())return worldbooks;const query=searchQuery.toLowerCase().trim();return worldbooks.filter(bookName=>bookName.toLowerCase().includes(query)||containsPinyinMatch(bookName,query))}function filterWorldbookEntries(searchQuery,entries){if(!searchQuery||!searchQuery.trim())return entries;const query=searchQuery.toLowerCase().trim();return entries.filter(entry=>[entry.comment||"",entry.key||"",entry.content||""].join(" ").toLowerCase().includes(query)||containsPinyinMatch(entry.comment||"",query))}function containsPinyinMatch(text,query){const pinyin={"世界书":"sjshu","条目":"tiaomu","编纂":"bianzhuan","搜索":"sousuo"}[text];return pinyin&&pinyin.includes(query)}function highlightSearchMatch(text,query){const safeText=String(text??"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#39;");if(!query||!query.trim())return safeText;const safeQuery=String(query).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#39;"),regex=new RegExp(`(${safeQuery.replace(/[.*+?^${}()|[\]\\]/g,"\\$&")})`,"gi");return safeText.replace(regex,'<mark class="search-highlight">$1</mark>')}function debounce(func,wait){let timeout;return function executedFunction(...args){clearTimeout(timeout),timeout=setTimeout(()=>{clearTimeout(timeout),func(...args)},wait)}}export{initialize,getSettings,saveSettings,resetSettings,apiTestApiConnection as testApiConnection,apiFetchEmbeddingModels as fetchEmbeddingModels,apiFetchRerankModels as fetchRerankModels,getVectorCount,purgeStorage,getMessagesForCondensation,processCondensation,ingestTextToHanlinyuan,getCollectionId,toggleSessionLock,isSessionLocked,getLockedSessionInfo,addKnowledgeBase,removeKnowledgeBase,getLocalKnowledgeBases,getGlobalKnowledgeBases,toggleKnowledgeBase,moveKnowledgeBase,filterWorldbooks,filterWorldbookEntries,highlightSearchMatch,debounce,renameKnowledgeBase};function initialize(){context=SillyTavern.getContext(),context?(migrateLegacyRagSettings(),sanitizeProfilePollution(),settings=getSettings(),window.hanlinyuanRagProcessor||(window.hanlinyuanRagProcessor={}),window.hanlinyuanRagProcessor.rearrangeChat=rearrangeChat,window.hanlinyuanRagProcessor.initialized=!0,eventSource.on(event_types.MESSAGE_RECEIVED,handleAutoCondensation),initializeArchiveManager(),console.log("翰林院忆识核心已启动 (V5.3-归档版)，已注册到全局 hanlinyuanRagProcessor 对象。")):console.error("[翰林院] 未能获取SillyTavern上下文，初始化失败。")}async function ingestTextToHanlinyuan(text,source="manual",metadata={},progressCallback=()=>{},signal=null,logCallback=()=>{},batchCompleteCallback=()=>{},jobId=null,resumeFromIndex=0){if(!text||!text.trim())return{success:!1,error:"输入文本为空"};if(!settings)return{success:!1,error:"核心未初始化"};try{const idInfo=getCollectionIdInfo(),legacyCollectionId=await getDynamicCollectionId();if(idInfo.oldId&&idInfo.oldId===legacyCollectionId&&idInfo.oldId!==idInfo.newId){if(!confirm("检测到旧版数据。此操作将把旧数据迁移到新格式，过程不可逆，是否继续？"))return logCallback("[翰林院-迁移] 用户取消了迁移操作。","info"),toastr.info("操作已取消。"),{success:!1,error:"用户取消了迁移操作"};logCallback(`[翰林院-迁移] 用户确认迁移，正在处理旧宝库: ${idInfo.oldId}`,"warn"),await purgeStorage(idInfo.oldId),logCallback("[翰林院-迁移] 旧宝库已清空。","success")}let kbName,taskId;const timestamp=(new Date).toLocaleString("zh-CN",{hour12:!1}),charName=getCharacterName()||"未知角色";switch(source){case"chat_history":const range=metadata.range||{},start=range.start??"?",end=0===range.end?"末":range.end??"?";kbName=`${charName}: ${start}楼-${end}楼`;break;case"lorebook":const bookName=metadata.bookName||"未分类世界书";if(metadata.entryName&&metadata.entryName.includes("微言录总结"))metadata.entryName="对话记录小总结";else if(metadata.entryName&&metadata.entryName.includes("宏史卷总结")){const fingerprint=metadata.entryName.match(/\[(?:sha256:[0-9a-f]{64}|fnv1a:[0-9a-f]{8})\]/iu)?.[0];metadata.entryName=fingerprint?`对话记录大总结 ${fingerprint}`:"对话记录大总结"}const entryName=metadata.entryName||"未知条目";kbName=`${bookName}: ${entryName}`;break;case"novel":kbName=`小说: ${metadata.sourceName||"未知小说"}`;break;default:kbName=`手动录入: ${timestamp}`}const independentChatId="chat_history"===source&&settings.retrieval.independentChatMemoryEnabled?getChatId():null,foundKb=Object.values(getKnowledgeBases()).find(kb=>kb.name===kbName&&(kb.chatId??null)===independentChatId);if(foundKb)taskId=foundKb.id,logCallback(`[翰林院-核心] 检测到同名知识库 "${kbName}"，将数据合并入库。`,"info");else{logCallback(`[翰林院-核心] 准备为任务 "${kbName}" 创建专属知识库...`,"info");taskId=addKnowledgeBase(kbName,source,independentChatId).id}const charId=getCharacterStableId(),collectionId=independentChatId?`${independentChatId}_${taskId}`:`${charId}_${taskId}`;logCallback(`[翰林院-核心] 已创建并锁定知识库: ${kbName} (集合ID: ${collectionId})`,"success"),logCallback(`[翰林院-核心] 已锁定忆识宝库ID: ${collectionId}`,"info"),progressCallback({message:"正在智能分块...",processed:0,total:1});const chunks=splitIntoChunks(text,source,metadata),totalChunks=chunks.length;if(signal?.aborted)throw new Error("AbortError");if(logCallback(`[翰林院-核心] 将来源'${kbName}'的文本分割成 ${totalChunks} 个块。`,"info"),0===totalChunks)return{success:!0,count:0};const batchSize=settings.retrieval.batchSize||5;let processedCount=resumeFromIndex;for(let i=resumeFromIndex;i<totalChunks;i+=batchSize){if(signal?.aborted)throw new Error("AbortError");const batchChunks=chunks.slice(i,i+batchSize);progressCallback({message:`正在处理 ${i+1}-${i+batchChunks.length} 块`,processed:i,total:totalChunks});const batchTexts=batchChunks.map(c=>c.text),embeddings=await getEmbeddings(batchTexts,signal);if(signal?.aborted)throw new Error("AbortError");if(batchChunks.length!==embeddings.length)throw new Error("文本块和向量数量不匹配");const vectorItems=batchChunks.map((chunk,index)=>({...chunk,vector:embeddings[index]}));await insertVectors(vectorItems,signal,collectionId),processedCount+=batchChunks.length,jobId&&IngestionManager.saveProgress(jobId,processedCount,totalChunks),await batchCompleteCallback()}return jobId&&IngestionManager.clearJob(jobId),logCallback(`[翰林院-核心] 成功插入 ${processedCount} 个向量条目。`,"success"),{success:!0,count:processedCount}}catch(error){if("AbortError"===error.name)throw logCallback("[翰林院-核心] 文本录入任务被用户中止。","warn"),error;return console.error("[翰林院-核心] ingestTextToHanlinyuan 失败:",error),logCallback(`[翰林院-核心] 文本录入失败: ${error.message}`,"error"),{success:!1,error:error.message}}}function getSettings(){extension_settings[extensionName]||(extension_settings[extensionName]={});const root=extension_settings[extensionName];let s=root[MODULE_NAME];s||(s={},root[MODULE_NAME]=s),void 0===s.condensationHistory&&(s.condensationHistory={}),void 0===s.knowledgeBases&&(s.knowledgeBases={}),void 0===s.queryPreprocessing&&(s.queryPreprocessing={enabled:!1,tagExtractionEnabled:!1,tags:"content,details,摘要",exclusionRules:[]});for(const key in ragDefaultSettings)if(void 0===s[key])s[key]=structuredClone(ragDefaultSettings[key]);else if("object"==typeof ragDefaultSettings[key]&&!Array.isArray(ragDefaultSettings[key])&&null!==ragDefaultSettings[key])for(const subKey in ragDefaultSettings[key])void 0===s[key][subKey]&&(s[key][subKey]=ragDefaultSettings[key][subKey]);if(s.rerank?.priorityRetrieval&&!s.rerank.priorityRetrieval.sources&&(s.rerank.priorityRetrieval.sources=structuredClone(ragDefaultSettings.rerank.priorityRetrieval.sources)),s.rerank?.priorityRetrieval?.sources){const defaultSources=ragDefaultSettings.rerank.priorityRetrieval.sources;for(const sourceName in defaultSources)if(s.rerank.priorityRetrieval.sources[sourceName]){const existing=s.rerank.priorityRetrieval.sources[sourceName];for(const key in defaultSources[sourceName])void 0===existing[key]&&(existing[key]=defaultSources[sourceName][key])}else s.rerank.priorityRetrieval.sources[sourceName]=structuredClone(defaultSources[sourceName])}return s}function saveSettings(){saveSettingsDebounced()}function resetSettings(){extension_settings[extensionName]||(extension_settings[extensionName]={}),extension_settings[extensionName][MODULE_NAME]=structuredClone(ragDefaultSettings),saveSettings()}function migrateLegacyRagSettings(){const legacy=extension_settings[MODULE_NAME];if(!legacy||"object"!=typeof legacy)return;extension_settings[extensionName]||(extension_settings[extensionName]={});const root=extension_settings[extensionName];if(root[MODULE_NAME]&&"object"==typeof root[MODULE_NAME]){const merged=root[MODULE_NAME],overlayLegacy=(src,dst)=>{for(const key of Object.keys(src)){const sv=src[key];sv&&"object"==typeof sv&&!Array.isArray(sv)&&dst[key]&&"object"==typeof dst[key]&&!Array.isArray(dst[key])?overlayLegacy(sv,dst[key]):dst[key]=sv}};overlayLegacy(legacy,merged),console.log(`[翰林院] 发现新旧两处配置；已将顶层 '${MODULE_NAME}' 深合并覆盖到 extension_settings['${extensionName}']。`)}else root[MODULE_NAME]=legacy,console.log(`[翰林院] 已迁移旧版 '${MODULE_NAME}' 设置到 extension_settings['${extensionName}']。`);delete extension_settings[MODULE_NAME],saveSettingsDebounced()}function sanitizeProfilePollution(){const s=getSettings();let cleaned=[];"••••••••"===s.retrieval?.apiKey&&(s.retrieval.apiKey="",cleaned.push("retrieval.apiKey 掩码")),"••••••••"===s.rerank?.apiKey&&(s.rerank.apiKey="",cleaned.push("rerank.apiKey 掩码"));s.retrieval&&!["custom","google_direct","local_proxy","openai","azure"].includes(s.retrieval.apiEndpoint)&&(cleaned.push(`retrieval.apiEndpoint 非法值 "${s.retrieval.apiEndpoint}"`),s.retrieval.apiEndpoint="custom");s.rerank&&!["custom","local_proxy"].includes(s.rerank.apiMode)&&(cleaned.push(`rerank.apiMode 非法值 "${s.rerank.apiMode}"`),s.rerank.apiMode="custom"),cleaned.length>0&&(console.warn(`[翰林院] 已清洗 profile-sync 历史污染字段: ${cleaned.join("、")}`),saveSettings())}function showNotification(message,type="info"){toastr[type](message)}function getTagForSource(source){switch(source){case"chat_history":return"聊天记录";case"lorebook":return"世界书";case"manual":return"手动录入";case"novel":return"小说录入";default:return"资料"}}function splitBySemanticBoundary(content,chunkSize,overlap){const pieces=[];if(!content||chunkSize<=0)return pieces;const minCut=Math.floor(.4*chunkSize),sentenceEndRegex=/[。！？!?…][”"』」)）】]?/g;let pos=0;for(;pos<content.length;){let end=Math.min(pos+chunkSize,content.length);if(end<content.length){const slice=content.substring(pos,end);let cut=slice.lastIndexOf("\n")+1;if(cut<=minCut){let m,lastSentenceEnd=-1;for(sentenceEndRegex.lastIndex=0;null!==(m=sentenceEndRegex.exec(slice));)lastSentenceEnd=m.index+m[0].length;lastSentenceEnd>minCut&&(cut=lastSentenceEnd)}cut>minCut&&(end=pos+cut)}const piece=content.substring(pos,end);if(piece.trim().length>0&&pieces.push(piece),end>=content.length)break;pos=Math.max(end-overlap,pos+1)}return pieces}function formatChunkTimeLabel(timestamp){const d=new Date(timestamp);if(isNaN(d.getTime()))return"";const pad=n=>String(n).padStart(2,"0");return`${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())} ${pad(d.getHours())}:${pad(d.getMinutes())}`}function splitIntoChunks(text,source,metadata={}){switch(source){case"novel":return _chunkForNovel(text,metadata);case"chat_history":return _chunkForChatHistory(text,metadata);case"lorebook":return _chunkForLorebook(text,metadata);case"manual":return _chunkForManual(text,metadata);default:return console.warn(`[翰林院-分块] 未知的来源类型 '${source}'，使用通用分块逻辑。`),_chunkForManual(text,{...metadata,sourceName:metadata.sourceName||"未知来源"})}}function _chunkForNovel(text,metadata){const{chunkSize:chunkSize,overlap:overlap}=settings.advanced,{sourceName:sourceName="小说"}=metadata,allChunks=[];if(!text||chunkSize<=0)return allChunks;const volumeRegex=/(第\s*[一二三四五六七八九十百千万零\d]+\s*卷)/gim,chapterRegex=/(第\s*[一二三四五六七八九十百千万零\d]+\s*[章回节部])|^(Chapter\s+\d+)/gim;let globalChunkIndex=0;const textLines=text.split("\n");let currentVolumeTitle="第1卷",currentChapterTitle="第1章",contentBuffer=[];function processBuffer(){if(0===contentBuffer.length)return;const content=contentBuffer.join("\n"),tagName=getTagForSource("novel");splitBySemanticBoundary(content,chunkSize,overlap).forEach((chunkText,idx)=>{const section=idx+1,chunkMetadata={source:"novel",sourceName:sourceName,timestamp:(new Date).toISOString(),globalIndex:globalChunkIndex++,volume:currentVolumeTitle,chapter:currentChapterTitle,section:section},wrappedText=`<${tagName}>\n${`[来源: ${sourceName}, ${currentVolumeTitle}, ${currentChapterTitle}, 第${section}节]`}\n${chunkText}\n</${tagName}>`;allChunks.push({text:wrappedText,metadata:chunkMetadata})}),contentBuffer=[]}for(const line of textLines){const trimmedLine=line.trim();volumeRegex.test(trimmedLine)?(processBuffer(),currentVolumeTitle=trimmedLine,currentChapterTitle="第1章"):chapterRegex.test(trimmedLine)?(processBuffer(),currentChapterTitle=trimmedLine):contentBuffer.push(line)}if(processBuffer(),0===allChunks.length&&text.length>0){const tagName=getTagForSource("novel");splitBySemanticBoundary(text,chunkSize,overlap).forEach((chunkText,idx)=>{const section=idx+1,chunkMetadata={source:"novel",sourceName:sourceName,timestamp:(new Date).toISOString(),globalIndex:allChunks.length,volume:"第1卷",chapter:"第1章",section:section},wrappedText=`<${tagName}>\n${`[来源: ${sourceName}, 第1卷, 第1章, 第${section}节]`}\n${chunkText}\n</${tagName}>`;allChunks.push({text:wrappedText,metadata:chunkMetadata})})}return allChunks}function _chunkForChatHistory(text,metadata){const{chunkSize:chunkSize,overlap:overlap}=settings.advanced,{floor:floor,is_user:is_user,timestamp:timestamp}=metadata,allChunks=[];if(!text||chunkSize<=0)return allChunks;const timeLabel=formatChunkTimeLabel(timestamp),tagName=getTagForSource("chat_history");return splitBySemanticBoundary(text,chunkSize,overlap).forEach((chunkText,idx)=>{const part=idx+1,wrappedText=`<${tagName}>\n${timeLabel?`[来源: 聊天记录, 楼层: #${floor}, 时间: ${timeLabel}, 第${part}部分]`:`[来源: 聊天记录, 楼层: #${floor}, 第${part}部分]`}\n${chunkText}\n</${tagName}>`;allChunks.push({text:wrappedText,metadata:{source:"chat_history",sourceName:`聊天记录 #${floor}`,floor:floor,part:part,is_user:is_user,timestamp:timestamp}})}),allChunks}function _chunkForLorebook(text,metadata){const{chunkSize:chunkSize,overlap:overlap}=settings.advanced,{bookName:bookName="世界书",entryName:entryName="世界书条目"}=metadata,allChunks=[];if(!text||chunkSize<=0)return allChunks;const tagName=getTagForSource("lorebook");return splitBySemanticBoundary(text,chunkSize,overlap).forEach((chunkText,idx)=>{const part=idx+1,wrappedText=`<${tagName}>\n${`[来源: ${bookName}, 条目: ${entryName}, 第${part}部分]`}\n${chunkText}\n</${tagName}>`;allChunks.push({text:wrappedText,metadata:{source:"lorebook",sourceName:`${bookName}: ${entryName}`,bookName:bookName,entryName:entryName,part:part,timestamp:(new Date).toISOString()}})}),allChunks}function _chunkForManual(text,metadata){const{chunkSize:chunkSize,overlap:overlap}=settings.advanced,{sourceName:sourceName="手动录入"}=metadata,allChunks=[];if(!text||chunkSize<=0)return allChunks;const timestamp=new Date,readableTime=formatChunkTimeLabel(timestamp),tagName=getTagForSource("manual");return splitBySemanticBoundary(text,chunkSize,overlap).forEach((chunkText,idx)=>{const part=idx+1,wrappedText=`<${tagName}>\n${`[来源: ${sourceName}, 向量化录入时间: ${readableTime}, 第${part}部分]`}\n${chunkText}\n</${tagName}>`;allChunks.push({text:wrappedText,metadata:{source:"manual",sourceName:sourceName,part:part,timestamp:timestamp.toISOString()}})}),allChunks}import{getCollectionId as getDynamicCollectionId,getCharacterName,getChatId}from"./utils/context-utils.js";async function getCollectionId(){if(lockedCollectionId)return lockedCollectionId;return settings.retrieval.independentChatMemoryEnabled?getChatId():await getDynamicCollectionId()}async function toggleSessionLock(){return lockedCollectionId?(lockedCollectionId=null,!1):(lockedCollectionId=await getDynamicCollectionId(),!0)}function isSessionLocked(){return null!==lockedCollectionId}function getLockedSessionInfo(){return lockedCollectionId?{id:lockedCollectionId,name:`(已锁定: ${lockedCollectionId.substring(0,8)}...)`}:null}function getLocalKnowledgeBases(){const charId=getCharacterStableId();return settings.knowledgeBases[charId]||(settings.knowledgeBases[charId]={}),settings.knowledgeBases[charId]}function getGlobalKnowledgeBases(){return settings.knowledgeBases._global||(settings.knowledgeBases._global={}),settings.knowledgeBases._global}function getKnowledgeBases(){const localBases=getLocalKnowledgeBases();return{...getGlobalKnowledgeBases(),...localBases}}function addKnowledgeBase(name,source="manual",chatId=null){if(!name||!name.trim())throw new Error("知识库名称不能为空");const charId=getCharacterStableId(),bases=getLocalKnowledgeBases(),taskId=`task_${Date.now()}_${Math.random().toString(36).substring(2,9)}`,newBase={id:taskId,name:name.trim(),enabled:!0,createdAt:(new Date).toISOString(),owner:charId,source:source,...chatId?{chatId:chatId}:{}};return bases[taskId]=newBase,saveSettings(),console.log(`[翰林院-核心] 已为角色 ${charId} 添加新知识库: ${name} (ID: ${taskId}${chatId?`, 聊天级: ${chatId}`:""})`),newBase}function getKbCollectionId(kb,scope="local"){return kb.chatId?`${kb.chatId}_${kb.id}`:"global"===scope?`${kb.owner||"_global"}_${kb.id}`:`${getCharacterStableId()}_${kb.id}`}async function removeKnowledgeBase(taskId,scope){getCharacterStableId();const bases="global"===scope?getGlobalKnowledgeBases():getLocalKnowledgeBases(),base=bases[taskId],baseName=base?.name||taskId;if(!base)return void console.warn(`[翰林院-核心] 尝试删除一个不存在的知识库: ${taskId} (范围: ${scope})`);const collectionIdToPurge=getKbCollectionId(base,scope);console.log(`[翰林院-核心] 准备删除知识库 ${taskId}，将清空集合: ${collectionIdToPurge}`);await purgeStorage(collectionIdToPurge)?(delete bases[taskId],saveSettings(),console.log(`[翰林院-核心] 成功删除知识库 ${taskId} 及其向量数据。`),toastr.success(`知识库 "${baseName}" 已删除。`)):(console.error(`[翰林院-核心] 清空向量集合 ${collectionIdToPurge} 失败，删除操作中止。`),toastr.error("删除知识库失败，未能清空后端数据。"))}function toggleKnowledgeBase(taskId,scope){const bases="global"===scope?getGlobalKnowledgeBases():getLocalKnowledgeBases();bases[taskId]&&(bases[taskId].enabled=!bases[taskId].enabled,saveSettings(),console.log(`[翰林院-核心] 知识库 ${taskId} (范围: ${scope}) 的状态已切换为: ${bases[taskId].enabled?"启用":"禁用"}`))}function generateHash(text){let hash=0;for(let i=0;i<text.length;i++){hash=(hash<<5)-hash+text.charCodeAt(i),hash&=hash}return Math.abs(hash).toString(36)}async function createQueryEmbedding(queryText){const queryEmbedding=(await getEmbeddings([queryText]))[0];if(!queryEmbedding)throw new Error("未能生成查询向量。");return queryEmbedding}async function queryVectors(queryText,options={}){const{includeBases:includeBases=null,queryEmbeddingPromise:queryEmbeddingPromise=null}=options;let basesToQuery=[];if(console.log(`[翰林院-日志] 开始向量查询... (目标: ${includeBases?"指定知识库":"所有启用库"})`),includeBases)basesToQuery=includeBases,console.log(`[翰林院-日志] 查询白名单已提供，将查询 ${basesToQuery.length} 个特定知识库。`);else if(settings.retrieval.independentChatMemoryEnabled){console.log("[翰林院-日志] 独立聊天记忆模式开启...");const chatId=getChatId();chatId||console.warn("[翰林院-日志] 无法获取当前聊天ID，聊天级知识库将被跳过。");const localBases=Object.values(getLocalKnowledgeBases()).filter(b=>b.enabled&&(!b.chatId||b.chatId===chatId));if(localBases.length>0){const chatScoped=localBases.filter(b=>b.chatId).length;console.log(`[翰林院-日志] 添加 ${localBases.length} 个本地知识库（其中 ${chatScoped} 个为当前聊天专属）。`),basesToQuery.push(...localBases.map(b=>({...b,scope:b.chatId?"chat":"local"})))}const enabledGlobalBases=Object.values(getGlobalKnowledgeBases()).filter(b=>b.enabled);enabledGlobalBases.length>0&&(console.log(`[翰林院-日志] 添加 ${enabledGlobalBases.length} 个已启用的全局知识库。`),basesToQuery.push(...enabledGlobalBases.map(b=>({...b,scope:"global"}))))}else{console.log("[翰林院-日志] 统一角色卡模式开启...");const localBases=getLocalKnowledgeBases(),globalBases=getGlobalKnowledgeBases(),enabledLocalBases=Object.values(localBases).filter(b=>b.enabled),enabledGlobalBases=Object.values(globalBases).filter(b=>b.enabled);if(basesToQuery.push(...enabledLocalBases.map(b=>({...b,scope:b.chatId?"chat":"local"}))),basesToQuery.push(...enabledGlobalBases.map(b=>({...b,scope:"global"}))),0===basesToQuery.length){console.log("[翰林院-日志] 没有启用的新知识库，尝试查询旧版单体宝库...");await getDynamicCollectionId()&&basesToQuery.push({id:null,name:"旧版宝库 (Legacy)",scope:"legacy"})}}if(0===basesToQuery.length)return console.log("[翰林院-日志] 没有可供查询的知识库，查询中止。"),[];const queryEmbedding=await(queryEmbeddingPromise||createQueryEmbedding(queryText)),queryPromises=basesToQuery.map(base=>_executeQueryForBase(base,queryText,queryEmbedding));let allResults=(await Promise.all(queryPromises)).flat();console.log(`[翰林院-日志] 所有知识库查询完毕，共获得 ${allResults.length} 条初步结果。`);const uniqueResults=[],seenTexts=new Set;for(const result of allResults)if(result&&"object"==typeof result&&result.text&&"string"==typeof result.text){const text=result.text.trim();text.length>0&&!seenTexts.has(text)&&(seenTexts.add(text),uniqueResults.push(result))}console.log(`[翰林院-日志] 去重后剩余 ${uniqueResults.length} 条结果。`),uniqueResults.sort((a,b)=>(b.score||0)-(a.score||0));const finalResults=[...uniqueResults];return console.log(`[翰林院-修复] 最终返回数组长度: ${finalResults.length}`),console.log("[翰林院-修复] 最终返回数组样本:",JSON.stringify(finalResults.slice(0,1),null,2)),finalResults}async function _executeQueryForBase(base,queryText,queryEmbedding=null){const charId=getCharacterStableId();let collectionId;switch(base.scope){case"legacy":collectionId=await getDynamicCollectionId();break;case"chat":collectionId=base.chatId?`${base.chatId}_${base.id}`:base.id;break;case"global":collectionId=`${base.owner||"_global"}_${base.id}`;break;default:collectionId=`${charId}_${base.id}`}if(!collectionId)return[];console.log(`[翰林院-日志] 正在查询知识库: ${base.name} (ID: ${collectionId})`);const finalQueryEmbedding=queryEmbedding||(await getEmbeddings([queryText]))[0];if(!finalQueryEmbedding)return console.error(`[翰林院-日志] 未能为知识库 ${collectionId} 生成查询向量。`),[];const requestBody={collectionId:collectionId,searchText:queryText,topK:settings.advanced.maxResults,threshold:settings.advanced.matchThreshold,source:"webllm",embeddings:{[queryText]:finalQueryEmbedding}};try{const response=await fetch("/api/vector/query",{method:"POST",headers:context.getRequestHeaders(),body:JSON.stringify(requestBody)});if(!response.ok){const errorText=await response.text();return console.error(`[翰林院-日志] 查询知识库 ${collectionId} 失败:`,errorText),[]}const result=await response.json();let rawData=[];Array.isArray(result)?rawData=result:result&&result.metadata&&Array.isArray(result.metadata)?rawData=result.metadata:result&&result.results&&Array.isArray(result.results)?rawData=result.results:result&&result.data&&Array.isArray(result.data)&&(rawData=result.data);const data=rawData.map(item=>{if(!item||"string"!=typeof item.text)return null;const newMetadata={source:"unknown",sourceName:"未知"},tagMatch=item.text.match(/^<([^>]+)>/);switch(tagMatch?tagMatch[1]:""){case"聊天记录":newMetadata.source="chat_history";const chatMatch=item.text.match(/楼层:\s*#(\d+)(?:,\s*时间:\s*([^,\]]+))?,\s*第(\d+)部分/);chatMatch&&chatMatch[1]&&chatMatch[3]&&(newMetadata.floor=parseInt(chatMatch[1],10),chatMatch[2]&&(newMetadata.timeLabel=chatMatch[2].trim()),newMetadata.part=parseInt(chatMatch[3],10),newMetadata.sourceName=`聊天记录 #${newMetadata.floor}`);break;case"世界书":newMetadata.source="lorebook";const loreMatch=item.text.match(/\[来源:\s*([^,]+),\s*条目:\s*([^,]+),\s*第(\d+)部分\]/);loreMatch&&loreMatch[1]&&loreMatch[2]&&loreMatch[3]&&(newMetadata.bookName=loreMatch[1].trim(),newMetadata.entryName=loreMatch[2].trim(),newMetadata.part=parseInt(loreMatch[3],10),newMetadata.sourceName=`${newMetadata.bookName}: ${newMetadata.entryName}`);break;case"手动录入":newMetadata.source="manual";const manualMatch=item.text.match(/\[来源:\s*([^,]+),.*第(\d+)部分\]/);manualMatch&&manualMatch[1]&&manualMatch[2]&&(newMetadata.sourceName=manualMatch[1].trim(),newMetadata.part=parseInt(manualMatch[2],10));break;case"小说录入":newMetadata.source="novel";const novelMatch=item.text.match(/\[来源:\s*([^,]+),\s*([^,]+),\s*([^,]+),\s*([^\]]+)\]/);novelMatch&&(newMetadata.sourceName=novelMatch[1].trim(),newMetadata.volume=novelMatch[2].trim(),newMetadata.chapter=novelMatch[3].trim(),newMetadata.section=novelMatch[4].trim())}return{...item,score:item.score||1,metadata:newMetadata}}).filter(Boolean);return console.log(`[翰林院-V13 修复] 重建元数据后，知识库 ${base.name} 返回 ${data.length} 条结果。`),data}catch(error){return console.error(`[翰林院-日志] 查询知识库 ${collectionId} 时发生网络错误:`,error),[]}}async function insertVectors(vectorItems,signal=null,collectionId){if(!collectionId)throw new Error("insertVectors 必须接收一个有效的 collectionId 参数。");if(0===vectorItems.length)return{success:!0,count:0};const items=vectorItems.map((item,index)=>({hash:generateHash(item.text+Date.now()+index),text:item.text,metadata:item.metadata||{source:"unknown",timestamp:(new Date).toISOString()}})),embeddingsMap=items.reduce((acc,item,index)=>(acc[item.text]=vectorItems[index].vector,acc),{}),requestBody={collectionId:collectionId,items:items,source:"webllm",embeddings:embeddingsMap},response=await fetch("/api/vector/insert",{method:"POST",headers:context.getRequestHeaders(),body:JSON.stringify(requestBody),signal:signal});if(!response.ok){const errorText=await response.text();throw console.error("[翰林院-日志] 忆识存入API错误:",errorText),new Error(`忆识存入API错误 ${response.status}: ${errorText}`)}return{success:!0,count:items.length}}async function getVectorCount(taskId=null,scope="local"){getCharacterStableId();if(taskId){const base=("global"===scope?getGlobalKnowledgeBases():getLocalKnowledgeBases())[taskId];return base?await countVectorsInCollection(getKbCollectionId(base,scope)):(console.warn(`[翰林院-计数] 在作用域 '${scope}' 中未找到ID为 ${taskId} 的知识库。`),0)}{const independent=settings.retrieval.independentChatMemoryEnabled,chatId=independent?getChatId():null;console.log(`[翰林院-日志] 开始获取${independent?"当前聊天可见的":"所有"}知识库向量总数...`);const localBases=Object.values(getLocalKnowledgeBases()).filter(base=>!independent||!base.chatId||base.chatId===chatId),globalBases=Object.values(getGlobalKnowledgeBases()),countPromises=[];if(localBases.forEach(base=>{countPromises.push(countVectorsInCollection(getKbCollectionId(base,"local")))}),globalBases.forEach(base=>{countPromises.push(countVectorsInCollection(getKbCollectionId(base,"global")))}),!independent){const legacyCollectionId=await getDynamicCollectionId();countPromises.push(countVectorsInCollection(legacyCollectionId))}const totalCount=(await Promise.all(countPromises)).reduce((total,count)=>total+count,0);return console.log(`[翰林院-日志] 知识库统计完成，总向量数: ${totalCount}`),totalCount}}async function countVectorsInCollection(collectionId){if(!collectionId)return 0;console.log(`[翰林院-日志] 统计目标集合ID: ${collectionId}`);const requestBody={collectionId:collectionId,source:"webllm",embeddings:{}};try{const response=await fetch("/api/vector/list",{method:"POST",headers:context.getRequestHeaders(),body:JSON.stringify(requestBody)});if(!response.ok){if(404===response.status)console.log(`[翰林院-日志] 集合 ${collectionId} 不存在，计为 0。`);else{const errorText=await response.text();console.warn(`[翰林院-日志] 获取集合 ${collectionId} 列表API时出现问题 (状态: ${response.status}):`,errorText)}return 0}const result=await response.json();let count=0;return Array.isArray(result)?count=result.length:result&&result.hashes&&(count=result.hashes.length),count}catch(error){return console.error(`[翰林院-日志] 统计集合 ${collectionId} 时发生网络错误:`,error),0}}async function purgeStorage(collectionIdOverride=null){console.log("[翰林院-日志] 开始清空宝库...");const collectionId=collectionIdOverride||await getCollectionId();if(!collectionId)return console.error("[翰林院-日志] 无法确定要清空的目标集合ID。"),toastr.error("无法确定要清空的目标宝库。"),!1;console.log(`[翰林院-日志] 清空目标集合ID: ${collectionId}`);const requestBody={collectionId:collectionId};console.log("[翰林院-日志] 发送到 /api/vector/purge 的请求体:",JSON.stringify(requestBody,null,2));const response=await fetch("/api/vector/purge",{method:"POST",headers:context.getRequestHeaders(),body:JSON.stringify(requestBody)});if(console.log(`[翰林院-日志] /api/vector/purge 响应状态: ${response.status}`),response.ok)console.log("[翰林院-日志] 清空宝库API调用成功。");else{const errorText=await response.text();console.error("[翰林院-日志] 清空宝库API错误:",errorText)}return response.ok}function getMessagesForCondensation(overrideMessageTypes=null){if(!settings.condensation.enabled)return showNotification("凝识之权未开启","warning"),[];const{layerStart:layerStart,layerEnd:layerEnd}=settings.condensation,messageTypes=overrideMessageTypes||settings.condensation.messageTypes,total=context.chat.length,startIndex=Math.max(0,layerStart-1),endIndex=0===layerEnd||layerEnd>total?total:Math.min(total,layerEnd);return context.chat.slice(startIndex,endIndex).filter(msg=>{const isUser=!0===msg.is_user,isAI=!1===msg.is_user;return!(!msg.mes||!msg.mes.trim())&&(messageTypes.user&&isUser||messageTypes.ai&&isAI)})}async function processCondensation(messages,logCallback=()=>{},range=null,kbNameOverride=null){if(!messages||0===messages.length)return{success:!1,error:"No messages to process."};try{let kbName,taskId;const charName=getCharacterName()||"未知角色";if(kbNameOverride)kbName=kbNameOverride;else if(range){const start=range.start??"?",end=0===range.end?"末":range.end??"?";kbName=`${charName}: ${start}楼-${end}楼`}else{const timestamp=(new Date).toLocaleString("zh-CN",{hour12:!1});kbName=`聊天记录: ${timestamp}`}const independentChatId=settings.retrieval.independentChatMemoryEnabled?getChatId():null,foundKb=Object.values(getLocalKnowledgeBases()).find(kb=>kb.name===kbName&&(kb.chatId??null)===independentChatId);if(foundKb)taskId=foundKb.id,logCallback(`[翰林院-核心] 检测到同名知识库 "${kbName}"，将数据合并入库。`,"info");else{logCallback(`[翰林院-核心] 准备为任务 "${kbName}" 创建专属知识库...`,"info");taskId=addKnowledgeBase(kbName,"chat_history",independentChatId).id}const charId=getCharacterStableId(),collectionId=independentChatId?`${independentChatId}_${taskId}`:`${charId}_${taskId}`;logCallback(`[翰林院-核心] 凝识任务已锁定知识库: ${kbName} (集合ID: ${collectionId})`,"success");const allChunks=[],fullChat=context.chat;for(const msg of messages){const text=(msg.mes||"").replace(/<[^>]*>/g,"").trim();if(0===text.length)continue;let floor;if(void 0!==msg.floor&&null!==msg.floor)floor=msg.floor;else{const floorIndex=fullChat.findIndex(chatMsg=>chatMsg===msg);floor=-1!==floorIndex?floorIndex+1:-1}const sendDate=new Date(msg.send_date),timestamp=isNaN(sendDate.getTime())?(new Date).toISOString():sendDate.toISOString(),msgChunks=splitIntoChunks(text,"chat_history",{floor:floor,is_user:msg.is_user,timestamp:timestamp});allChunks.push(...msgChunks)}if(0===allChunks.length)return{success:!0,count:0};logCallback(`[翰林院-核心] 已将 ${messages.length} 条消息分解为 ${allChunks.length} 个知识块，准备入库。`,"info");const batchSize=settings.retrieval.batchSize||5;let processedCount=0;for(let i=0;i<allChunks.length;i+=batchSize){const batchChunks=allChunks.slice(i,i+batchSize),batchTexts=batchChunks.map(c=>c.text),embeddings=await getEmbeddings(batchTexts);if(batchChunks.length!==embeddings.length)throw new Error("文本块和向量数量不匹配");const vectorItems=batchChunks.map((chunk,index)=>({...chunk,vector:embeddings[index]}));await insertVectors(vectorItems,null,collectionId),processedCount+=batchChunks.length}if(range){const finalEnd=0===range.end?context.chat.length:range.end,charId=getCharacterStableId();settings.condensationHistory[charId]||(settings.condensationHistory[charId]={}),settings.condensationHistory[charId][collectionId]={start:range.start,end:finalEnd,timestamp:(new Date).toISOString()},saveSettings(),logCallback(`[翰林院-核心] 已为宝库 ${collectionId} 记录凝识范围: ${range.start}-${finalEnd}`,"info")}logCallback(`[翰林院-核心] 聊天记录凝识完成，成功插入 ${processedCount} 个条目。`,"success");return{success:!0,count:processedCount,messages:messages.map(msg=>{const floorIndex=fullChat.findIndex(chatMsg=>chatMsg===msg),floor=-1!==floorIndex?floorIndex+1:-1;return`[${msg.is_user?"用户":getCharacterName()||"AI"} - 楼层 #${floor}] 的消息已成功凝识。`})}}catch(error){return console.error("[翰林院-核心] processCondensation 失败:",error),logCallback(`[翰林院-核心] 聊天记录凝识失败: ${error.message}`,"error"),{success:!1,error:error.message}}}async function handleAutoCondensation(){settings&&settings.condensation&&settings.condensation.enabled&&settings.condensation.autoCondense&&setTimeout(async()=>{try{const preserveFloors=settings.condensation.preserveFloors||0,totalMessages=context.chat.length,chatId=getChatId();if(!chatId)return void console.warn("[翰林院-自动凝识] 无法获取聊天ID，跳过。");settings.condensation.autoCondenseProgress||(settings.condensation.autoCondenseProgress={});const startFloor=(settings.condensation.autoCondenseProgress[chatId]||0)+1,endFloor=totalMessages-preserveFloors;if(startFloor>endFloor)return;const startIndex=startFloor-1,endIndex=endFloor,messagesToCondense=context.chat.slice(startIndex,endIndex);if(0===messagesToCondense.length)return;console.log(`[翰林院-自动凝识] 触发自动凝识: ${startFloor} - ${endFloor} 楼 (ChatID: ${chatId})`);const BUCKET_SIZE=100;let currentStart=startFloor;for(;currentStart<=endFloor;){const bucketIndex=Math.floor((currentStart-1)/BUCKET_SIZE),bucketStartFloor=bucketIndex*BUCKET_SIZE+1,bucketEndFloor=(bucketIndex+1)*BUCKET_SIZE,currentEnd=Math.min(endFloor,bucketEndFloor),sliceStart=currentStart-startFloor,sliceEnd=currentEnd-startFloor+1,batchMessages=messagesToCondense.slice(sliceStart,sliceEnd);if(batchMessages.length>0){const range={start:currentStart,end:currentEnd},kbName=`${getCharacterName()}: 自动凝识 (${bucketStartFloor}-${bucketEndFloor})`;console.log(`[翰林院-自动凝识] 处理分桶: ${currentStart}-${currentEnd} -> ${kbName}`);if(!(await processCondensation(batchMessages,(msg,type)=>{"error"===type?console.error(msg):console.log(msg)},range,kbName)).success){console.error(`[翰林院-自动凝识] 分桶 ${kbName} 处理失败，中止后续处理。`);break}settings.condensation.autoCondenseProgress[chatId]=currentEnd,saveSettings()}currentStart=currentEnd+1}}catch(error){console.error("[翰林院-自动凝识] 执行失败:",error)}},2e3)}async function rerankResults(allResults,queryText,settings){let processedResults=allResults,rerankedSuccessfully=!1;if(settings.rerank.enabled&&allResults.length>0){console.log("[翰林院-Rerank] 开始外部API重排序...");try{const documentsToRerank=allResults.map(res=>res.text),rerankConn=await getRerankSettings(),rerankedData=await executeRerank(queryText,documentsToRerank,rerankConn);processedResults=allResults.map((res,index)=>({...res,original_index:index})).map(result=>{const rerankedResult=rerankedData.results.find(r=>r.index===result.original_index),relevanceScore=rerankedResult?rerankedResult.relevance_score:0;return{...result,rerank_score:relevanceScore}}),rerankedSuccessfully=!0}catch(error){console.error("[翰林院-Rerank] 外部Rerank失败，将仅使用内部加权。",error),settings.rerank.notify&&showNotification(`Rerank失败: ${error.message}`,"error"),processedResults.forEach(res=>res.rerank_score=0)}}else processedResults.forEach(res=>res.rerank_score=0);console.log("[翰林院-Rerank] 开始元数据加权最终排序...");const totalMessages=context.chat.length,alpha=settings.rerank.hybrid_alpha,finalScoredResults=processedResults.map(result=>{let contextualWeight=1;const metadata=result.metadata||{};switch(metadata.source){case"lorebook":contextualWeight*=1.2;break;case"manual":contextualWeight*=1.1;break;case"chat_history":if(metadata.floor&&totalMessages>0){contextualWeight*=1+metadata.floor/totalMessages}}const finalScore=(result.rerank_score*alpha+(result.score||0)*(1-alpha))*contextualWeight;return{text:result.text,score:result.score,rerank_score:result.rerank_score,final_score:finalScore,metadata:result.metadata}});finalScoredResults.sort((a,b)=>(b.final_score||0)-(a.final_score||0)),console.log("[翰林院-Rerank] 元数据加权排序完成。");let finalResults=finalScoredResults.slice(0,settings.rerank.top_n);return settings.rerank.superSortEnabled&&(finalResults=superSort(finalResults)),{results:finalResults,reranked:rerankedSuccessfully}}function _parseOrdinal(value){if("number"==typeof value)return value;if(!value)return Number.MAX_SAFE_INTEGER;const str=String(value),arabic=str.match(/\d+/);if(arabic)return parseInt(arabic[0],10);const cnDigit={"零":0,"一":1,"二":2,"两":2,"三":3,"四":4,"五":5,"六":6,"七":7,"八":8,"九":9},m=str.match(/[零一二两三四五六七八九十百千万]+/);if(!m)return Number.MAX_SAFE_INTEGER;let total=0,current=0;for(const ch of m[0])void 0!==cnDigit[ch]?current=cnDigit[ch]:"十"===ch?(total+=10*(current||1),current=0):"百"===ch?(total+=100*(current||1),current=0):"千"===ch?(total+=1e3*(current||1),current=0):"万"===ch&&(total=1e4*(total+current),current=0);return total+current}function _composeInjectionText(source,results){const sorted=[...results],ord=v=>Number.isFinite(v)?v:Number.MAX_SAFE_INTEGER;if("chat_history"===source){sorted.sort((a,b)=>ord(a.metadata?.floor)-ord(b.metadata?.floor)||(a.metadata?.part??0)-(b.metadata?.part??0));const parts=[];let prevFloor=null;for(const r of sorted){const floor=r.metadata?.floor;null!==prevFloor&&Number.isFinite(floor)&&floor-prevFloor>1&&parts.push(`〔提示：以下内容与上文相隔约 ${floor-prevFloor} 楼，期间的剧情未被检索到，两段内容并非连续发生〕`),parts.push(r.text),Number.isFinite(floor)&&(prevFloor=floor)}return parts.join("\n\n")}return"novel"===source?(sorted.sort((a,b)=>_parseOrdinal(a.metadata?.volume)-_parseOrdinal(b.metadata?.volume)||_parseOrdinal(a.metadata?.chapter)-_parseOrdinal(b.metadata?.chapter)||_parseOrdinal(a.metadata?.section)-_parseOrdinal(b.metadata?.section)),sorted.map(r=>r.text).join("\n\n")):(sorted.sort((a,b)=>String(a.metadata?.sourceName??"").localeCompare(String(b.metadata?.sourceName??""),"zh")||(a.metadata?.part??0)-(b.metadata?.part??0)),sorted.map(r=>r.text).join("\n\n"))}async function rearrangeChat(chat,contextSize,abort,type){const injectionKeys={novel:"HANLINYUAN_RAG_NOVEL",chat_history:"HANLINYUAN_RAG_CHAT",lorebook:"HANLINYUAN_RAG_LOREBOOK",manual:"HANLINYUAN_RAG_MANUAL",graph:"HANLINYUAN_RAG_GRAPH"};if(Object.values(injectionKeys).forEach(key=>setExtensionPrompt(key,"",0,0,!1,0)),"quiet"===type||!settings.retrieval.enabled)return;const queryMessages=chat.slice(-settings.advanced.queryMessageCount);if(0===queryMessages.length)return;const queryPreprocessingSettings=resolveQueryPreprocessingRuleConfig(settings);let queryText="";const relevantTexts=[];for(const msg of queryMessages)if(msg.is_user)relevantTexts.push(msg.mes);else if(queryPreprocessingSettings.enabled&&queryPreprocessingSettings.tagExtractionEnabled){const tagsToExtract=(queryPreprocessingSettings.tags||"").split(",").map(t=>t.trim()).filter(Boolean);if(tagsToExtract.length>0){const blocks=extractBlocksByTags(msg.mes,tagsToExtract);if(blocks.length>0){const innerContents=blocks.map(block=>{const match=block.match(/<[^>]+>([\s\S]*?)<\/[^>]+>/);return match?match[1].trim():""});relevantTexts.push(innerContents.filter(Boolean).join("\n\n"))}}else relevantTexts.push(msg.mes)}else relevantTexts.push(msg.mes);if(queryText=relevantTexts.filter(Boolean).join("\n\n"),queryPreprocessingSettings.enabled&&(queryText=applyExclusionRules(queryText,queryPreprocessingSettings.exclusionRules)),queryText=queryText.trim(),!queryText)return void console.log("[翰林院] 经过预处理后，最终检索文本为空，注入中止。");const indexMatches=queryText.match(/(M\d+)/g);if(indexMatches){const indexQuery=[...new Set(indexMatches)].map(idx=>`[索引: ${idx}]`).join(" ");queryText+=`\n\n${indexQuery}`,console.log(`[翰林院] 检测到索引引用，已增强检索词: ${indexQuery}`)}console.log(`[翰林院-预处理] 最终用于检索的文本: "${queryText}"`);try{const graphContext=await executeGraphRetrieval(queryText);graphContext&&(console.log("[翰林院] 成功获取关系图谱上下文，准备注入。"),setExtensionPrompt(injectionKeys.graph,graphContext,settings.injection_lorebook?settings.injection_lorebook.position:0,settings.injection_lorebook?settings.injection_lorebook.depth:4,!1,0));const SETTINGS_VERSION=2,currentVersion=settings.settingsVersion||1;let settingsModified=!1;if(currentVersion<SETTINGS_VERSION){console.log(`[翰林院-户口普查] 检测到旧版设置 (V${currentVersion})，开始强制重分类所有知识库...`),toastr.info("检测到旧版数据，正在进行一次性户口普查...","翰林院通告");const allBasesObject=getKnowledgeBases();for(const kb of Object.values(allBasesObject)){const name=kb.name,oldSource=kb.source;name.startsWith("手动录入: ")?kb.source="manual":name.startsWith("小说:")?kb.source="novel":name.includes("楼-")&&name.includes("楼")&&name.includes(":")?kb.source="chat_history":kb.source="lorebook",oldSource!==kb.source&&console.log(`[翰林院-户口普查] 知识库 "${name}" 已从 [${oldSource||"无"}] 更正为 [${kb.source}]`)}settings.settingsVersion=SETTINGS_VERSION,settingsModified=!0}settingsModified&&(console.log("[翰林院-户口普查] 普查完成，正在保存更新后的户籍..."),saveSettings());let finalResults=[];const prioritySettings=settings.rerank.priorityRetrieval;if(prioritySettings.enabled){console.log("[翰林院] 进入多路并行独立检索流程...");const allEnabledBases=Object.values(getKnowledgeBases()).filter(b=>b.enabled),sharedQueryEmbeddingPromise=allEnabledBases.length>0?createQueryEmbedding(queryText):null,prioritySourceNames=Object.keys(prioritySettings.sources).filter(key=>prioritySettings.sources[key]&&prioritySettings.sources[key].enabled),queryPromises=[];let remainingBases=[...allEnabledBases];for(const sourceName of prioritySourceNames){const sourceSettings=prioritySettings.sources[sourceName],priorityGroup=remainingBases.filter(b=>b.source===sourceName);if(remainingBases=remainingBases.filter(b=>!priorityGroup.includes(b)),priorityGroup.length>0){console.log(`[翰林院] 创建优先查询组: ${sourceName} (${priorityGroup.length}个库)`);const promise=queryVectors(queryText,{includeBases:priorityGroup,queryEmbeddingPromise:sharedQueryEmbeddingPromise}).then(candidates=>{console.log(`[翰林院] 优先组 ${sourceName} 返回 ${candidates.length} 条结果。`);let processed=candidates.filter(r=>r.metadata?.source===sourceName);return processed=processed.slice(0,sourceSettings.count),console.log(`[翰林院] 已从 ${sourceName} 池精确提取 ${processed.length} 条结果。`),settings.rerank.superSortEnabled&&(processed=superSort(processed)),processed});queryPromises.push(promise)}}const normalBases=remainingBases;if(normalBases.length>0){console.log(`[翰林院] 创建常规查询组 (${normalBases.length}个库)`);const promise=queryVectors(queryText,{includeBases:normalBases,queryEmbeddingPromise:sharedQueryEmbeddingPromise}).then(async candidates=>{console.log(`[翰林院] 常规组返回 ${candidates.length} 条结果。`),console.log("[翰林院] 开始处理常规池...");const rerankOutput=await rerankResults(candidates,queryText,settings),normalResults=rerankOutput.results;return console.log(`[翰林院] 常规池处理完毕，产出 ${(normalResults||[]).length} 条结果。`),rerankOutput.reranked&&settings.rerank.notify&&showNotification("统一检索部分的Rerank已完成","success"),normalResults});queryPromises.push(promise)}finalResults=(await Promise.all(queryPromises)).flat()}else{console.log("[翰林院] 进入传统处理流程...");const allCandidates=await queryVectors(queryText),rerankOutput=await rerankResults(allCandidates,queryText,settings);finalResults=rerankOutput.results,rerankOutput.reranked&&settings.rerank.notify&&showNotification("外部Rerank完成","success")}if(!finalResults||0===finalResults.length)return void console.log("[翰林院] 最终无可用结果，注入中止。");console.log(`[翰林院] 最终准备注入 ${finalResults.length} 条结果。`);const resultsBySource={novel:[],chat_history:[],lorebook:[],manual:[]};finalResults.forEach(result=>{const source=result.metadata?.source;source&&resultsBySource.hasOwnProperty(source)&&resultsBySource[source].push(result)});for(const source in resultsBySource){const results=resultsBySource[source];if(0===results.length)continue;const injectionSettings=settings[`injection_${source.replace("_history","")}`];if(!injectionSettings){console.warn(`[翰林院] 未找到来源 '${source}' 的注入设置，跳过处理。`);continue}const formattedText=_composeInjectionText(source,results),placeholder=`{{${source.replace("_history","")}_text}}`;let injectionContent=injectionSettings.template.replace(placeholder,formattedText);injectionContent.trim()&&(injectionContent=`%%${injectionKeys[source]}%%${injectionContent}`),setExtensionPrompt(injectionKeys[source],injectionContent,injectionSettings.position,injectionSettings.depth,!1,injectionSettings.depth_role),console.log(`[翰林院] 已为来源 '${source}' 注入 ${results.length} 条内容。`)}}catch(error){console.error("[翰林院] 检索或注入时发生错误:",error),settings.retrieval.notify&&showNotification(`忆识检索失败: ${error.message}`,"error")}}async function moveKnowledgeBase(taskId,fromScope){const toScope="global"===fromScope?"local":"global",charId=getCharacterStableId();if(!charId&&"local"===toScope)return void toastr.error("移动失败：没有当前角色，无法移入局部知识库。");const sourceBases="global"===fromScope?getGlobalKnowledgeBases():getLocalKnowledgeBases(),targetBases="global"===toScope?getGlobalKnowledgeBases():getLocalKnowledgeBases(),kbData=sourceBases[taskId];if(!kbData){const errorMsg=`在源作用域 '${fromScope}' 中未找到ID为 ${taskId} 的知识库。`;return console.error(`[翰林院-配置] ${errorMsg}`),void toastr.error("移动失败：未找到源条目。")}if(kbData.chatId&&"global"===toScope)return void toastr.warning(`知识库【${kbData.name}】是聊天专属记忆，不能移动到全局。`);"local"!==fromScope||"global"!==toScope||kbData.owner||(console.log(`[翰林院-配置] 为旧版知识库 ${taskId} 补充所有者ID: ${charId}`),kbData.owner=charId),delete sourceBases[taskId],targetBases[taskId]=kbData,saveSettings();const message=`知识库【${kbData.name}】已成功移动到${"global"===toScope?"全局":"局部"}。`;console.log(`[翰林院-配置] ${message}`)}function renameKnowledgeBase(taskId,newName,scope){if(!newName||!newName.trim())throw toastr.error("知识库名称不能为空。"),new Error("知识库名称不能为空");const base=("global"===scope?getGlobalKnowledgeBases():getLocalKnowledgeBases())[taskId];if(!base){const errorMsg=`在作用域 '${scope}' 中未找到ID为 ${taskId} 的知识库。`;throw console.error(`[翰林院-配置] ${errorMsg}`),toastr.error("重命名失败：未找到知识库条目。"),new Error(errorMsg)}const oldName=base.name;base.name=newName.trim(),saveSettings();const message=`知识库 "${oldName}" 已成功重命名为 "${base.name}"。`;console.log(`[翰林院-配置] ${message}`),toastr.success(message)}