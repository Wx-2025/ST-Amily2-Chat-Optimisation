import{apiProfileManager}from"../../utils/config/ApiProfileManager.js";import{apiRequestRateLimiter,bindApiProfileRateLimit,getApiProfileRateLimitDescriptor}from"./api-request-rate-limiter.js";export function providerToApiMode(provider){return"sillytavern_preset"===provider?"sillytavern_preset":"openai_test"}export async function getSlotProfile(slot){try{return await apiProfileManager.getAssignedProfile(slot)}catch(e){return console.warn(`[ApiResolver] 读取槽位 "${slot}" 失败，降级到旧配置:`,e),null}}export function bindSlotProfileRateLimit(settings,profile){return bindApiProfileRateLimit(settings,profile)}export function acquireProfileRequestPermit(settings,signal=null){const descriptor=getApiProfileRateLimitDescriptor(settings);return descriptor?apiRequestRateLimiter.acquire(descriptor.profileId,descriptor.rpm,{signal:signal,getCurrentRpm:()=>{const current=apiProfileManager.getProfile(descriptor.profileId);return current?current.rpm:null}}):Promise.resolve()}