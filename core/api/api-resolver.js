import{apiProfileManager}from"../../utils/config/ApiProfileManager.js";import{apiRequestRateLimiter,bindApiProfileRateLimit,getApiProfileRateLimitDescriptor}from"./api-request-rate-limiter.js";export function providerToApiMode(provider){return provider==="sillytavern_preset"?"sillytavern_preset":"openai_test"}export async function getSlotProfile(slot){try{return await apiProfileManager.getAssignedProfile(slot)}catch(e){console.warn(`[ApiResolver] 读取槽位 "${slot}" 失败，降级到旧配置:`,e);return null}}export function bindSlotProfileRateLimit(settings,profile){return bindApiProfileRateLimit(settings,profile)}export function acquireProfileRequestPermit(settings,signal=null){const descriptor=getApiProfileRateLimitDescriptor(settings);if(!descriptor)return Promise.resolve();return apiRequestRateLimiter.acquire(descriptor.profileId,descriptor.rpm,{signal:signal,getCurrentRpm:()=>{const current=apiProfileManager.getProfile(descriptor.profileId);return current?current.rpm:null}})}