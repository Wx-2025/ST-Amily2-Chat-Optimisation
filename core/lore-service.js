import{registerInternalBusPlugin}from"../SL/bus/Amily2Bus.js";import{loadWorldInfo,createNewWorldInfo,saveWorldInfo}from"/scripts/world-info.js";let _writeLock=Promise.resolve();export function withLoreLock(label,fn){const result=_writeLock.then(()=>{console.log(`[LoreService] 写锁获取: ${label}`);return fn()});_writeLock=result.then(()=>{console.log(`[LoreService] 写锁释放: ${label}`)},()=>{console.warn(`[LoreService] 写锁释放（含错误）: ${label}`)});return result}export async function loadBook(bookName){return loadWorldInfo(bookName)}export async function ensureBook(bookName){return withLoreLock(`ensureBook(${bookName})`,async()=>{const existing=await loadWorldInfo(bookName);if(existing)return existing;console.log(`[LoreService] 世界书不存在，正在创建: ${bookName}`);return createNewWorldInfo(bookName)})}export async function saveBook(bookName,bookData,silent=true){return withLoreLock(`saveBook(${bookName})`,()=>saveWorldInfo(bookName,bookData,silent))}(()=>{try{const _ctx=registerInternalBusPlugin("LoreService");if(!_ctx){console.warn("[LoreService] Amily2Bus 尚未就绪，服务注册跳过。");return}_ctx.expose({getStatus:()=>Object.freeze({ready:true})});_ctx.log("LoreService","info","LoreService 已注册到 Bus。")}catch(e){console.error("[LoreService] Bus 注册失败:",e)}})();