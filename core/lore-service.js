import{registerInternalBusPlugin}from"../SL/bus/Amily2Bus.js";import{loadWorldInfo,createNewWorldInfo,saveWorldInfo}from"/scripts/world-info.js";let _writeLock=Promise.resolve();export function withLoreLock(label,fn){const result=_writeLock.then(()=>(console.log(`[LoreService] 写锁获取: ${label}`),fn()));return _writeLock=result.then(()=>{console.log(`[LoreService] 写锁释放: ${label}`)},()=>{console.warn(`[LoreService] 写锁释放（含错误）: ${label}`)}),result}export async function loadBook(bookName){return loadWorldInfo(bookName)}export async function ensureBook(bookName){return withLoreLock(`ensureBook(${bookName})`,async()=>{const existing=await loadWorldInfo(bookName);return existing||(console.log(`[LoreService] 世界书不存在，正在创建: ${bookName}`),createNewWorldInfo(bookName))})}export async function saveBook(bookName,bookData,silent=!0){return withLoreLock(`saveBook(${bookName})`,()=>saveWorldInfo(bookName,bookData,silent))}(()=>{try{const _ctx=registerInternalBusPlugin("LoreService");if(!_ctx)return void console.warn("[LoreService] Amily2Bus 尚未就绪，服务注册跳过。");_ctx.expose({getStatus:()=>Object.freeze({ready:!0})}),_ctx.log("LoreService","info","LoreService 已注册到 Bus。")}catch(e){console.error("[LoreService] Bus 注册失败:",e)}})();