import{getContext,extension_settings}from"/scripts/extensions.js";import{saveChatConditional,reloadCurrentChat}from"/script.js";import{extensionName}from"../utils/settings.js";import{SlashCommand}from"/scripts/slash-commands/SlashCommand.js";import{SlashCommandParser}from"/scripts/slash-commands/SlashCommandParser.js";import{checkAndFixWithAPI}from"./api.js";import{amilyHelper}from"./tavern-helper/main.js";async function checkLatestMessage(){const chat=getContext().chat||[];if(!chat||0===chat.length)return console.log("[Amily2-命令检查器] 没有聊天记录。"),{message:null,previousMessages:[]};const latestMessage=chat[chat.length-1];if(console.log("[Amily2-命令检查器] 正在侦测消息:",{isUser:latestMessage.is_user,messagePreview:latestMessage.mes?.substring(0,50)+"..."}),latestMessage.is_user)return console.log("[Amily2-命令检查器] 目标为用户消息，跳过。"),{message:latestMessage,previousMessages:[]};const contextCount=extension_settings[extensionName].contextMessages||2,startIndex=Math.max(0,chat.length-contextCount-1),previousMessages=chat.slice(startIndex,chat.length-1);return console.log("[Amily2-命令检查器] 已获取上下文消息:",{count:previousMessages.length}),{message:latestMessage,previousMessages:previousMessages}}async function checkCommand(){if(!extension_settings[extensionName].apiUrl)return toastr.error("请先配置API URL","命令检查器"),"";const checkResult=await checkLatestMessage();if(!checkResult.message||checkResult.message.is_user)return toastr.info("最新消息是用户消息，无需检查","命令检查器"),"";toastr.info("正在使用API检查回复...","命令检查器");const result=await checkAndFixWithAPI(checkResult.message,checkResult.previousMessages);return result&&result.optimizedContent&&result.optimizedContent!==checkResult.message.mes?toastr.warning("检测到问题，建议使用修复功能","命令检查器"):toastr.success("未检测到问题","命令检查器"),""}export async function fixCommand(){const settings=extension_settings[extensionName];if(!settings.apiUrl)return toastr.error("请先配置API URL","命令检查器"),"";const chat=getContext().chat;if(!chat||0===chat.length)return toastr.info("没有可修复的消息","命令检查器"),"";const latestMessage=chat[chat.length-1];if(latestMessage.is_user)return toastr.info("最新消息是用户消息，无需修复","命令检查器"),"";const contextCount=settings.contextMessages||2,startIndex=Math.max(0,chat.length-1-contextCount),previousMessages=chat.slice(startIndex,chat.length-1);toastr.info("正在检查并修复回复...","命令检查器");const result=await checkAndFixWithAPI(latestMessage,previousMessages);if(result&&result.optimizedContent&&result.optimizedContent!==latestMessage.mes){const messageId=chat.length-1;await amilyHelper.setChatMessage({message:result.optimizedContent},messageId,{refresh:"display_and_render_current"}),toastr.success("回复已修复","命令检查器")}else toastr.info("未检测到需要修复的问题","命令检查器");return""}export async function testReplyChecker(){const settings=extension_settings[extensionName];if(!settings.apiUrl)return toastr.error("请先配置API URL","命令检查器"),"";const chat=getContext().chat;if(!chat||chat.length<2)return toastr.warning("需要至少2条消息才能测试","命令检查器"),"";let testMessage=null;for(let i=chat.length-2;i>=0;i--)if(!chat[i].is_user){testMessage=chat[i].mes;break}if(!testMessage)return toastr.warning("没有找到可用于测试的AI消息","命令检查器"),"";const lastMessage=chat[chat.length-1];if(lastMessage.is_user)return toastr.warning("最后一条消息是用户消息，无法测试","命令检查器"),"";const originalMessage=lastMessage.mes;lastMessage.mes=testMessage+"\n\n"+testMessage,toastr.info("正在使用API测试检测功能...","命令检查器");const contextCount=settings.contextMessages||2,startIndex=Math.max(0,chat.length-contextCount-1),previousMessages=chat.slice(startIndex,chat.length-1),result=await checkAndFixWithAPI(lastMessage,previousMessages);return lastMessage.mes=originalMessage,result&&result.optimizedContent&&result.optimizedContent!==testMessage+"\n\n"+testMessage?toastr.success("测试成功！API检测到重复内容并提供了修复建议","命令检查器"):toastr.warning("测试结果：API未检测到问题，请检查API配置或提示词","命令检查器"),""}async function triggerSendButton(){const sendButton=document.getElementById("send_but");return sendButton?(sendButton.click(),console.log("[Amily2-触发器] 已触发发送按钮"),""):(console.warn("[Amily2-触发器] 未找到发送按钮"),toastr.warning("未找到发送按钮","触发器"),"")}export async function registerSlashCommands(){try{if(void 0===SlashCommand||void 0===SlashCommandParser)return void console.error("[Amily2] 致命错误：SlashCommand 或 SlashCommandParser 模块未能加载。");SlashCommandParser.addCommandObject(SlashCommand.fromProps({name:"check-reply",callback:checkCommand,helpString:"检查最新的AI回复是否有问题"})),console.log("[Amily2-新诏] /check-reply 命令已成功颁布。"),SlashCommandParser.addCommandObject(SlashCommand.fromProps({name:"fix-reply",callback:fixCommand,helpString:"修复最新的AI回复中的问题"})),console.log("[Amily2-新诏] /fix-reply 命令已成功颁布。"),SlashCommandParser.addCommandObject(SlashCommand.fromProps({name:"test-reply-checker",callback:testReplyChecker,helpString:"测试聊天回复检查器功能"})),console.log("[Amily2-新诏] /test-reply-checker 命令已成功颁布。"),SlashCommandParser.addCommandObject(SlashCommand.fromProps({name:"trigger",callback:triggerSendButton,helpString:"触发发送按钮 (用于自动发送消息)"})),console.log("[Amily2-新诏] /trigger 命令已成功颁布。")}catch(e){console.error("[Amily2] 命令注册过程中发生意外错误:",e)}}