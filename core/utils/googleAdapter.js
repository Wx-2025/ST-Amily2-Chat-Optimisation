const GOOGLE_DOMAINS=["generativelanguage.googleapis.com","ai.google.dev","us-central1-aiplatform.googleapis.com"];export function isGoogleEndpoint(url){try{if(!url||"string"!=typeof url)return!1;const parsedUrl=new URL(url),hostname=parsedUrl.hostname.toLowerCase();return!parsedUrl.pathname.toLowerCase().includes("/openai")&&GOOGLE_DOMAINS.some(domain=>hostname.includes(domain))}catch(error){return console.error("[GoogleAdapter] URL解析错误:",url,error),!1}}export function convertToGoogleRequest(openaiRequest){const{model:_,...rest}=openaiRequest;return{contents:openaiRequest.messages.map(message=>{let role;return role="assistant"===message.role?"model":"user",{role:role,parts:[{text:message.content}]}}),generationConfig:{maxOutputTokens:openaiRequest.max_tokens,temperature:openaiRequest.temperature||.7,topP:.95},safetySettings:[{category:"HARM_CATEGORY_HARASSMENT",threshold:"BLOCK_NONE"},{category:"HARM_CATEGORY_HATE_SPEECH",threshold:"BLOCK_NONE"},{category:"HARM_CATEGORY_SEXUALLY_EXPLICIT",threshold:"BLOCK_NONE"},{category:"HARM_CATEGORY_DANGEROUS_CONTENT",threshold:"BLOCK_NONE"}]}}export function parseGoogleResponse(googleResponse){try{if(googleResponse.error)throw new Error(`Google API错误: ${googleResponse.error.message||"未知错误"}\n代码: ${googleResponse.error.code}`);const candidate=googleResponse.candidates?.[0];if(!candidate||!candidate.content)throw new Error("无效的Google API响应: 未找到候选内容");return{choices:[{index:0,message:{role:"assistant",content:candidate.content.parts.map(part=>part.text||"").join("\n").trim()},finish_reason:candidate.finishReason||"stop"}]}}catch(error){throw console.error("[GoogleAdapter] 响应解析错误:",error),console.log("原始Google响应:",googleResponse),error}}export function buildGoogleApiUrl(baseUrl,modelName){try{const url=new URL(baseUrl),pathname=url.pathname.replace(/\/$/,"");if(pathname.endsWith("/v1beta")||pathname.endsWith("/v1")){if(!modelName)throw new Error("Google API需要模型名称");const apiVersion=pathname.endsWith("/v1beta")?"v1beta":"v1";return url.pathname=`/${apiVersion}/models/${modelName}:generateContent`,url.href}return url.href}catch(error){throw console.error("[GoogleAdapter] URL构建错误:",baseUrl,modelName,error),new Error(`无效的API地址: ${baseUrl}`)}}export function buildGoogleEmbeddingRequest(texts,modelName){return{requests:texts.map(text=>({model:`models/${modelName}`,content:{parts:[{text:text}]}}))}}export function parseGoogleEmbeddingResponse(googleResponse,originalTexts){if(!googleResponse||!Array.isArray(googleResponse.embeddings))throw console.error('[GoogleAdapter] Google向量API响应格式无效，缺少 "embeddings" 数组:',googleResponse),new Error("Google API返回的向量数据格式不正确。");return googleResponse.embeddings.map(emb=>emb.values)}export function buildGoogleEmbeddingApiUrl(baseUrl,modelName){const url=new URL(baseUrl),apiVersion=modelName.includes("gemini")?"v1beta":"v1";return url.pathname=`/${apiVersion}/models/${modelName}:batchEmbedContents`,url.href}function convertOaiToGoogleForPlotOptimization(messages){const contents=[];let lastRole="";for(const message of messages){if("user"===message.role&&"user"===lastRole){contents[contents.length-1].parts.push({text:`\n\n${message.content}`});continue}"assistant"===message.role?(contents.push({role:"model",parts:[{text:message.content}]}),lastRole="model"):(contents.push({role:"user",parts:[{text:message.content}]}),lastRole="user")}return{contents:contents,system_instruction:null}}export function buildPlotOptimizationGoogleRequest(messages,apiSettings){const{contents:contents}=convertOaiToGoogleForPlotOptimization(messages),generationConfig={temperature:apiSettings.temperature,topP:apiSettings.top_p,topK:apiSettings.top_k,maxOutputTokens:apiSettings.max_tokens};generationConfig.topK&&(generationConfig.topK=Math.round(generationConfig.topK)),Object.keys(generationConfig).forEach(key=>{void 0!==generationConfig[key]&&null!==generationConfig[key]||delete generationConfig[key]}),0===contents.length&&contents.push({role:"user",parts:[{text:"Hi"}]});return{contents:contents,generationConfig:generationConfig,safetySettings:[{category:"HARM_CATEGORY_HARASSMENT",threshold:"BLOCK_NONE"},{category:"HARM_CATEGORY_HATE_SPEECH",threshold:"BLOCK_NONE"},{category:"HARM_CATEGORY_SEXUALLY_EXPLICIT",threshold:"BLOCK_NONE"},{category:"HARM_CATEGORY_DANGEROUS_CONTENT",threshold:"BLOCK_NONE"}]}}export function parsePlotOptimizationGoogleResponse(googleResponse){try{const candidates=googleResponse?.candidates;if(!candidates||0===candidates.length){let message="Google API returned no candidates.";return googleResponse?.promptFeedback?.blockReason&&(message+=`\nPrompt was blocked due to: ${googleResponse.promptFeedback.blockReason}`,console.error(message,googleResponse.promptFeedback.safetyRatings)),{choices:[{message:{content:`Error: ${message}`}}]}}const responseContent=candidates[0].content,responseText=responseContent?.parts?.map(part=>part.text).join("")||"";if(!responseText){let message="Google API response text is empty.";return console.warn(message,googleResponse),{choices:[{message:{content:"Error: Received an empty response from the API."}}]}}return{choices:[{message:{content:responseText}}]}}catch(error){return console.error("Error parsing Google response:",error,googleResponse),{choices:[{message:{content:`Error: Failed to parse Google API response. Details: ${error.message}`}}]}}}