const ENCODING_LABELS=Object.freeze({"UTF-8":"utf-8",UTF8:"utf-8",GBK:"gbk",GB2312:"gbk",BIG5:"big5"});export function normalizeTextEncoding(value="UTF-8"){const normalized=String(value).trim().toUpperCase();const label=ENCODING_LABELS[normalized];if(!label){throw new RangeError(`不支持的文件编码: ${value}`)}return label}export function decodeTextBytes(bytes,encoding="UTF-8"){const label=normalizeTextEncoding(encoding);const view=bytes instanceof ArrayBuffer?new Uint8Array(bytes):ArrayBuffer.isView(bytes)?new Uint8Array(bytes.buffer,bytes.byteOffset,bytes.byteLength):new Uint8Array(bytes);return new TextDecoder(label,{fatal:false}).decode(view)}export async function readTextFile(file,encoding="UTF-8"){if(!file||typeof file.arrayBuffer!=="function"){throw new TypeError("请提供可读取的文本文件")}return decodeTextBytes(await file.arrayBuffer(),encoding)}