export function extractBlocksByTags(text,tagNames){if(!text||!tagNames||!Array.isArray(tagNames)||tagNames.length===0){return[]}const allBlocks=[];tagNames.forEach(tagName=>{const trimmedTag=tagName.trim();if(trimmedTag){const regex=new RegExp(`<${trimmedTag}[^>]*>[\\s\\S]*?<\\/${trimmedTag}>`,"g");const matches=text.match(regex);if(matches){allBlocks.push(...matches)}}});return allBlocks}export function applyExclusionRules(text,rules){if(!text||!rules||!Array.isArray(rules)||rules.length===0){return text}let processedText=text;rules.forEach(rule=>{if(rule.start&&rule.end){const start=rule.start.replace(/[.*+?^${}()|[\]\\]/g,"\\$&");const end=rule.end.replace(/[.*+?^${}()|[\]\\]/g,"\\$&");const regex=new RegExp(`${start}[\\s\\S]*?${end}`);while(regex.test(processedText)){processedText=processedText.replace(regex,"")}}});return processedText}