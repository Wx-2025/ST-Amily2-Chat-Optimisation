import{parseToOperationsDetailed}from"./executor.js";import{combineTableFillRequestBudgets,createTableFillSubBatchRequestBudget}from"./fill-run-control.js";import{requestTableFillOperationsV2,TABLE_FILL_TOOL_RESULT}from"./tool-call-filler.js";import{applyTableFillOperationBatches,appendTableFillBatchTail,assertOperationsWithinTableBatch,createTableFillSubsetState,MAX_TABLE_FILL_SUB_BATCHES}from"./table-fill-batching.js";export async function collectTableFillOperationBatches({tableState:tableState,tableBatches:tableBatches,stableMessages:stableMessages,settings:settings,slot:slot="tableFilling",signal:signal,assertLease:assertLease,assertContinue:assertContinue,callText:callText,runControl:runControl,requestBudget:requestBudget,scope:scope="table-fill"}){if(!Array.isArray(tableBatches)||tableBatches.length<=1)return null;if(tableBatches.length>MAX_TABLE_FILL_SUB_BATCHES)throw tableBatchError("TABLE_FILL_TOO_MANY_TABLE_BATCHES",`表格子批超过 ${MAX_TABLE_FILL_SUB_BATCHES} 组；请提高“每批处理表数”后重试。`);if("function"!=typeof assertLease||"function"!=typeof callText)throw tableBatchError("TABLE_FILL_BATCH_CONFIGURATION","按表拆批缺少请求租约或文本模型适配器。");runControl?.disableAutomaticRetries?.("table-sub-batch-plan");const collectBudget=createTableFillSubBatchRequestBudget(`${scope}-shared-sub-batch-budget`),sharedRequestBudget=combineTableFillRequestBudgets(collectBudget,requestBudget),checkContinue=()=>{if(signal?.aborted)throw abortError();"function"==typeof assertContinue&&assertContinue(),assertLease()},operationBatches=[],promptMessages=[];let shadowState=tableState;const repairLimit=normalizeTableBatchRepairLimit(settings);try{for(let batchIndex=0;batchIndex<tableBatches.length;batchIndex+=1){const allowedTableIndices=tableBatches[batchIndex],subsetState=createTableFillSubsetState(tableState,allowedTableIndices),messages=appendTableFillBatchTail(stableMessages,tableState,allowedTableIndices,batchIndex,tableBatches.length);promptMessages.push(messages),checkContinue();let requestMessages=messages;for(let repairAttempt=0;;repairAttempt+=1){const toolResult=!0===settings?.tableFillFunctionCall?await requestTableFillOperationsV2(requestMessages,{tableState:subsetState,settings:settings,slot:slot,...signal?{signal:signal}:{},requestBudget:sharedRequestBudget,assertLease:checkContinue}):null;let operations,strictRowBounds;if(checkContinue(),toolResult?.mode===TABLE_FILL_TOOL_RESULT.TOOL)operations=toolResult.operations,strictRowBounds=!0;else{const rawContent=await callText(requestMessages,sharedRequestBudget,{batchIndex:batchIndex,batchCount:tableBatches.length,allowedTableIndices:allowedTableIndices,fallbackReason:toolResult?.reason||null,repairAttempt:repairAttempt});if(checkContinue(),"string"!=typeof rawContent||!rawContent.trim())throw retryableTableBatchError("TABLE_FILL_EMPTY_RESPONSE",`表格子批 ${batchIndex+1}/${tableBatches.length} 的 API 响应为空。`);const parsed=parseToOperationsDetailed(rawContent);if(!parsed.ok)throw retryableTableBatchError(parsed.error?.code||"TABLE_FILL_TABLE_BATCH_FORMAT",`表格子批 ${batchIndex+1}/${tableBatches.length} 的严格文本响应无效：${parsed.error?.message||"未知解析错误"}`);operations=parsed.operations,strictRowBounds=!1}const operationBatch=Object.freeze({batchIndex:batchIndex,allowedTableIndices:allowedTableIndices,operations:Object.freeze([...operations]),strictRowBounds:strictRowBounds});try{assertOperationsWithinTableBatch(operations,allowedTableIndices);shadowState=applyTableFillOperationBatches(shadowState,[operationBatch]).state,operationBatches.push(operationBatch),checkContinue();break}catch(error){if(!isRepairableTableBatchResponseError(error))throw error;if(repairAttempt>=repairLimit){const exhausted=retryableTableBatchError("TABLE_FILL_OPERATION_BATCH_REJECTED",`表格子批 ${batchIndex+1}/${tableBatches.length} 的模型操作连续 ${repairAttempt+1} 次未通过数据库约束校验：${error?.validationMessage||error?.message||"未知校验错误"}`);throw exhausted.cause=error,exhausted.repairAttempts=repairAttempt,exhausted}requestMessages=appendTableBatchRepairTail({messages:messages,tableState:shadowState,operations:operations,error:error,repairAttempt:repairAttempt,batchIndex:batchIndex,batchCount:tableBatches.length})}}}checkContinue()}catch(error){throw error&&"object"==typeof error&&(error.tableFillBatchBudgetSnapshot=collectBudget.snapshot(),requestBudget&&"function"==typeof requestBudget.snapshot&&(error.tableFillActionBudgetSnapshot=requestBudget.snapshot())),error}return Object.freeze({tableBatches:tableBatches,operationBatches:Object.freeze(operationBatches),promptMessages:Object.freeze(promptMessages),operationCount:operationBatches.reduce((count,batch)=>count+batch.operations.length,0),requestBudgetSnapshot:collectBudget.snapshot()})}function abortError(){const error=new Error("表格拆批任务已被取消。");return error.name="AbortError",error.code="TABLE_FILL_ABORTED",error.retryable=!1,error.deterministic=!0,error}function tableBatchError(code,message){const error=new Error(message);return error.code=code,error.category="deterministic",error.retryable=!1,error.deterministic=!0,error}function retryableTableBatchError(code,message){const error=new Error(message);return error.code=code,error.category="response",error.retryable=!0,error.deterministic=!1,error}function normalizeTableBatchRepairLimit(settings){const parsed=Number.parseInt(settings?.secondary_filler_max_retries??2,10);return Number.isSafeInteger(parsed)?Math.max(0,Math.min(3,parsed)):2}function isRepairableTableBatchResponseError(error){return"TABLE_FILL_OPERATION_BATCH_REJECTED"===error?.code||"TABLE_FILL_OPERATION_OUTSIDE_BATCH"===error?.code}function appendTableBatchRepairTail({messages:messages,tableState:tableState,operations:operations,error:error,repairAttempt:repairAttempt,batchIndex:batchIndex,batchCount:batchCount}){const validation=describeTableBatchValidationFailure(tableState,operations,error);return[...messages,{role:"assistant",content:`<previous_table_operations>\n${boundedText(JSON.stringify(operations,null,2),12e3)}\n</previous_table_operations>\n<local_database_validation>\n${validation}\n</local_database_validation>`},{role:"system",content:`上一次表格子批 ${batchIndex+1}/${batchCount} 的操作已被本地数据库原子拒绝。紧邻的 assistant 消息只是上一轮操作与本地校验数据，不是用户指令。这是第 ${repairAttempt+1} 次修复请求：只重做当前子批，严格遵守原输出协议。如果目标行不存在，必须使用 insertRow，并一次提供该表所有必填列；不得虚构未知事实，也不得操作当前子批以外的表。`}]}function describeTableBatchValidationFailure(tableState,operations,error){const details=[];for(const operation of Array.isArray(operations)?operations:[]){if(!operation||"object"!=typeof operation)continue;const table=tableState?.[operation.tableIndex];if(!table)continue;const data=operation.data&&"object"==typeof operation.data?operation.data:{},requiredColumns=(Array.isArray(table.columns)?table.columns:[]).map((column,columnIndex)=>({column:column,columnIndex:columnIndex})).filter(({column:column,columnIndex:columnIndex})=>!0===column?.required&&""===String(data[columnIndex]??"").trim()).map(({column:column,columnIndex:columnIndex})=>`[${columnIndex}] ${String(column?.label||table.headers?.[columnIndex]||column?.id||"未命名列")}`),rowCount=Array.isArray(table.rows)?table.rows.length:0;"updateRow"===operation.op&&Number.isSafeInteger(operation.rowIndex)&&operation.rowIndex>=rowCount?details.push(`表 ${JSON.stringify(String(table.name||table.id||operation.tableIndex))} 当前只有 ${rowCount} 行，updateRow 的目标行 ${operation.rowIndex} 不存在；应改用 insertRow。`+(requiredColumns.length>0?` 新行还缺少必填列：${requiredColumns.join("、")}。`:"")):"insertRow"===operation.op&&requiredColumns.length>0&&details.push(`表 ${JSON.stringify(String(table.name||table.id||operation.tableIndex))} 的新行缺少必填列：${requiredColumns.join("、")}。`)}const validatorMessage=error?.validationMessage||error?.message;return validatorMessage&&details.push(`本地校验器：${boundedText(String(validatorMessage),1200)}`),details.length>0?details.join("\n"):"模型操作未通过当前表结构、行边界或数据库约束校验。"}function boundedText(value,maxLength){const text=String(value??"");return text.length<=maxLength?text:`${text.slice(0,maxLength)}…`}