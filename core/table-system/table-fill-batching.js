import{applyOperations}from"./actions/applyOperations.js";import{isAiFillableTable,assertAiFillOperationTargets}from"./module-tables.js";import{tablesToCsvForAiFill}from"./rendering.js";export const MAX_TABLE_FILL_TABLES_PER_REQUEST=128;export const MAX_TABLE_FILL_SUB_BATCHES=16;const DEFERRED_TABLE_DATA_NOTICE=["<当前表格数据>","当前请求启用了按表拆批；本批允许处理的完整表格快照位于消息链末尾。","只能修改末尾列出的表格，未列出的表格不属于本批任务。","</当前表格数据>"].join("\n");export function normalizeTablesPerFillRequest(value){let parsed;if("number"==typeof value)parsed=value;else{if("string"!=typeof value||!/^\d+$/u.test(value.trim()))return 0;parsed=Number(value.trim())}return!Number.isSafeInteger(parsed)||parsed<=0?0:Math.min(parsed,128)}export function planTableFillBatches(tableState,configuredSize){if(!Array.isArray(tableState))return Object.freeze([]);const tableIndices=tableState.map((table,tableIndex)=>isAiFillableTable(table)?tableIndex:null).filter(Number.isSafeInteger);if(0===tableIndices.length)return Object.freeze([]);const normalizedSize=normalizeTablesPerFillRequest(configuredSize);if(0===normalizedSize||tableIndices.length<=normalizedSize)return Object.freeze([Object.freeze(tableIndices)]);const relationGroups=buildRelationGroups(tableState,tableIndices),oversizedGroup=relationGroups.find(group=>group.length>128);if(oversizedGroup)throw tableBatchError("TABLE_FILL_RELATION_GROUP_TOO_LARGE",`一个外键连通表组包含 ${oversizedGroup.length} 张用户表，超过单次安全上限 128；该关系组不会被强行拆开。`);const batches=[];let currentBatch=[];for(const relationGroup of relationGroups)currentBatch.length>0&&currentBatch.length+relationGroup.length>normalizedSize&&(batches.push(Object.freeze([...currentBatch].sort((left,right)=>left-right))),currentBatch=[]),currentBatch.push(...relationGroup);if(currentBatch.length>0&&batches.push(Object.freeze([...currentBatch].sort((left,right)=>left-right))),batches.length>16)throw tableBatchError("TABLE_FILL_TOO_MANY_TABLE_BATCHES",`当前配置会产生 ${batches.length} 个表格子批，超过安全上限 16；请提高“每批处理表数”后重试。`);return Object.freeze(batches)}function buildRelationGroups(tableState,tableIndices){const allowed=new Set(tableIndices),tableIndexById=new Map(tableIndices.map(tableIndex=>[tableState[tableIndex]?.id,tableIndex])),parent=new Map(tableIndices.map(tableIndex=>[tableIndex,tableIndex])),find=value=>{let root=value;for(;parent.get(root)!==root;)root=parent.get(root);for(;parent.get(value)!==value;){const next=parent.get(value);parent.set(value,root),value=next}return root},unite=(left,right)=>{const leftRoot=find(left),rightRoot=find(right);leftRoot!==rightRoot&&parent.set(rightRoot,leftRoot)};for(const sourceIndex of tableIndices){const columns=Array.isArray(tableState[sourceIndex]?.columns)?tableState[sourceIndex].columns:[];for(const column of columns){const targetIndex=tableIndexById.get(column?.references?.tableId);allowed.has(targetIndex)&&unite(sourceIndex,targetIndex)}}const groups=new Map;for(const tableIndex of tableIndices){const root=find(tableIndex);groups.has(root)||groups.set(root,[]),groups.get(root).push(tableIndex)}return[...groups.values()].map(group=>group.sort((left,right)=>left-right)).sort((left,right)=>left[0]-right[0])}export function createTableFillSubsetState(tableState,allowedTableIndices){if(!Array.isArray(tableState))throw tableBatchError("TABLE_FILL_INVALID_STATE","表格快照无效，无法创建表格子批。");const subset=new Array(tableState.length);for(const tableIndex of normalizeAllowedIndices(allowedTableIndices,tableState.length)){const table=tableState[tableIndex];if(!isAiFillableTable(table))throw tableBatchError("TABLE_FILL_FORBIDDEN_TABLE_BATCH",`表格子批包含不可由 AI 填写的表格索引 ${tableIndex}。`);subset[tableIndex]=table}return Object.freeze(subset)}export function buildCacheStableFlowPrompt(flowTemplate){const template=String(flowTemplate??"");return template.includes("{{{Amily2TableData}}}")?template.split("{{{Amily2TableData}}}").join(DEFERRED_TABLE_DATA_NOTICE):template.trim()}export function appendTableFillBatchTail(stableMessages,tableState,allowedTableIndices,batchIndex,batchCount){const subsetState=createTableFillSubsetState(tableState,allowedTableIndices),snapshot=tablesToCsvForAiFill(subsetState);return[...stableMessages,{role:"system",content:[`# 当前表格子批 ${Number.isSafeInteger(batchIndex)?batchIndex+1:1}/${Number.isSafeInteger(batchCount)&&batchCount>0?batchCount:1}`,`允许修改的原始 tableIndex：${[...allowedTableIndices].join(", ")}`,"以下是本批唯一可读取、可操作的表格。不得猜测或修改未列出的表格。","<当前表格数据>",snapshot,"</当前表格数据>"].join("\n")}]}export function assertOperationsWithinTableBatch(operations,allowedTableIndices){if(!Array.isArray(operations))throw tableBatchError("TABLE_FILL_INVALID_OPERATION_BATCH","表格操作批次不是数组。");const allowed=new Set(allowedTableIndices);for(let index=0;index<operations.length;index+=1){const tableIndex=operations[index]?.tableIndex;if(!Number.isSafeInteger(tableIndex)||!allowed.has(tableIndex))throw tableBatchError("TABLE_FILL_OPERATION_OUTSIDE_BATCH",`第 ${index+1} 条操作指向未在本子批公开的表格索引 ${String(tableIndex)}。`)}return!0}export function applyTableFillOperationBatches(initialState,operationBatches){if(!Array.isArray(initialState))throw tableBatchError("TABLE_FILL_INVALID_STATE","表格快照无效，无法推演填表子批。");if(!Array.isArray(operationBatches))throw tableBatchError("TABLE_FILL_INVALID_OPERATION_BATCH","填表子批列表无效。");let state=initialState;const changes=[],operations=[];for(const batch of operationBatches){const batchOperations=Array.isArray(batch?.operations)?batch.operations:null;if(!batchOperations)throw tableBatchError("TABLE_FILL_INVALID_OPERATION_BATCH","某个填表子批缺少操作数组。");if(assertOperationsWithinTableBatch(batchOperations,batch.allowedTableIndices||[]),assertAiFillOperationTargets(state,batchOperations),0===batchOperations.length)continue;const applied=applyOperations(state,batchOperations,{strictRowBounds:!0===batch.strictRowBounds});if(!0!==applied.accepted){const error=tableBatchError("TABLE_FILL_OPERATION_BATCH_REJECTED",`表格子批 ${String(batch?.batchIndex??"?")} 的操作未通过数据库推演校验。`);throw applied.error&&(error.cause=applied.error,error.validationCode=applied.error.code||"TABLE_VALIDATION_FAILED",error.validationMessage=applied.error.message||String(applied.error)),error}state=applied.state,changes.push(...applied.changes),operations.push(...batchOperations)}return{state:state,changes:changes,operations:operations,accepted:!0}}function normalizeAllowedIndices(value,stateLength){if(!Array.isArray(value))throw tableBatchError("TABLE_FILL_INVALID_TABLE_BATCH","表格子批索引列表无效。");const seen=new Set,indices=[];for(const tableIndex of value){if(!Number.isSafeInteger(tableIndex)||tableIndex<0||tableIndex>=stateLength||seen.has(tableIndex))throw tableBatchError("TABLE_FILL_INVALID_TABLE_BATCH",`表格子批含无效或重复索引 ${String(tableIndex)}。`);seen.add(tableIndex),indices.push(tableIndex)}return indices}function tableBatchError(code,message){const error=new Error(message);return error.code=code,error.category="deterministic",error.retryable=!1,error.deterministic=!0,error}