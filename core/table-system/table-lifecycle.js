export const TABLE_CHAT_RELOAD_DELAY_MS=100;export function createTableLifecycleCoordinator(dependencies){const beginChatTransition=dependencies?.beginChatTransition;const loadCurrentTables=dependencies?.loadCurrentTables;const publishReady=dependencies?.publishReady||(()=>{});const onError=dependencies?.onError||((error,reason)=>{console.error(`[TableLifecycle] ${reason} 重载失败:`,error)});const setTimer=dependencies?.setTimer||globalThis.setTimeout;const clearTimer=dependencies?.clearTimer||globalThis.clearTimeout;if(typeof beginChatTransition!=="function"||typeof loadCurrentTables!=="function"){throw new TypeError("Table lifecycle requires transition and load callbacks.")}let ready=false;let chatSettling=false;let revision=0;let timer=null;let deferredPublish=false;function cancelTimer(){if(timer===null)return;clearTimer(timer);timer=null}function run(requestRevision,reason,shouldPublish){if(!ready||requestRevision!==revision)return false;timer=null;try{loadCurrentTables(reason);if(requestRevision!==revision)return false;chatSettling=false;if(shouldPublish||deferredPublish){deferredPublish=false;publishReady(Object.freeze({reason:reason,revision:requestRevision}))}return true}catch(error){chatSettling=false;onError(error,reason);return false}}function schedule(reason,{delayMs:delayMs=0,publish:publish=true}={}){revision+=1;const requestRevision=revision;cancelTimer();deferredPublish||=publish;if(!ready)return requestRevision;timer=setTimer(()=>run(requestRevision,reason,publish),Math.max(0,Number(delayMs)||0));return requestRevision}function reconcileNow(reason,{publish:publish=true}={}){revision+=1;const requestRevision=revision;cancelTimer();deferredPublish||=publish;return run(requestRevision,reason,publish)}return Object.freeze({handleChatChanged(){revision+=1;cancelTimer();chatSettling=true;deferredPublish=true;beginChatTransition();return schedule("chat-changed",{delayMs:TABLE_CHAT_RELOAD_DELAY_MS,publish:true})},handleChatLoaded(){chatSettling=false;return schedule("chat-loaded",{publish:true})},handleAuthorizationGranted(){deferredPublish=true;if(chatSettling)return revision;return schedule("authorization-granted",{publish:true})},markReady(){ready=true;if(chatSettling){return schedule("settings-ready-after-chat-change",{delayMs:TABLE_CHAT_RELOAD_DELAY_MS,publish:true})}return reconcileNow("settings-ready",{publish:false})},reconcile(reason="manual"){if(!ready){deferredPublish=true;return schedule(reason,{publish:true})}return reconcileNow(reason,{publish:true})},getState(){return Object.freeze({ready:ready,chatSettling:chatSettling,revision:revision,scheduled:timer!==null})}})}const readyListeners=new Set;let reconcileRequestHandler=null;export function publishTableLifecycleReady(detail){for(const listener of readyListeners){try{listener(detail)}catch(error){console.error("[TableLifecycle] 表格就绪监听器执行失败:",error)}}}export function subscribeTableLifecycleReady(listener){if(typeof listener!=="function")return()=>{};readyListeners.add(listener);return()=>readyListeners.delete(listener)}export function registerTableLifecycleReconcileRequest(handler){reconcileRequestHandler=typeof handler==="function"?handler:null;return()=>{if(reconcileRequestHandler===handler)reconcileRequestHandler=null}}export function requestTableLifecycleReconcile(reason="manual"){if(!reconcileRequestHandler)return false;return reconcileRequestHandler(reason)}