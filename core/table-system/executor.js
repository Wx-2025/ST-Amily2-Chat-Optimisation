import{log}from"./logger.js";import{applyOperations}from"./actions/applyOperations.js";const ALLOWED_FN_NAMES=new Set(["insertRow","updateRow","deleteRow"]);function parseFunctionCall(callString){const match=callString.trim().match(/^(\w+)\s*([(（])([\s\S]*)([)）])\s*[;；]?\s*$/u);if(!match)return log(`指令格式错误，无法解析: "${callString}"`,"error"),null;const functionName=match[1],openingParenthesis=match[2],argsString=match[3],closingParenthesis=match[4];if("("===openingParenthesis&&")"!==closingParenthesis||"（"===openingParenthesis&&"）"!==closingParenthesis)return log(`指令括号不匹配，无法解析: "${callString}"`,"error"),null;if(!ALLOWED_FN_NAMES.has(functionName))return log(`检测到非法函数调用: "${functionName}"。已阻止执行。`,"error"),null;try{const args=[];let currentArg="",inQuote=!1,quoteChar="";const braceStack=[];for(let i=0;i<argsString.length;i++){const char=argsString[i],escaped=isEscaped(argsString,i);if(inQuote){char!==quoteChar||escaped?currentArg+=char:(inQuote=!1,currentArg+=normalizedClosingQuote(char));continue}const openingQuote=getOpeningQuote(char);if(!openingQuote||escaped){if(isClosingCurlyQuote(char)&&!escaped)throw new Error(`孤立的右引号 "${char}"`);if("{"===char||"["===char)braceStack.push(char),currentArg+=char;else if("}"===char||"]"===char){const expectedOpening="}"===char?"{":"[";if(braceStack.pop()!==expectedOpening)throw new Error(`括号不匹配: "${char}"`);currentArg+=char}else if(","!==char&&"，"!==char&&";"!==char&&"；"!==char||0!==braceStack.length)currentArg+=normalizeStructuralPunctuation(char);else{if(!currentArg.trim())throw new Error("存在空参数");args.push(parseValue(currentArg)),currentArg=""}}else inQuote=!0,quoteChar=openingQuote.close,currentArg+=openingQuote.normalized}if(inQuote)throw new Error("字符串引号未闭合");if(braceStack.length>0)throw new Error("对象或数组括号未闭合");if(currentArg.trim())args.push(parseValue(currentArg));else if(argsString.trim()&&args.length>0)throw new Error("参数列表不能以分隔符结尾");return{name:functionName,args:args}}catch(e){return log(`解析函数 "${functionName}" 的参数时出错: ${e.message}`,"error"),null}}function getOpeningQuote(char){return'"'===char?{close:'"',normalized:'"'}:"'"===char?{close:"'",normalized:"'"}:"“"===char?{close:"”",normalized:'"'}:"‘"===char?{close:"’",normalized:'"'}:null}function isClosingCurlyQuote(char){return"”"===char||"’"===char}function normalizedClosingQuote(char){return"”"===char||"’"===char?'"':char}function isEscaped(text,index){let slashCount=0;for(let i=index-1;i>=0&&"\\"===text[i];i--)slashCount++;return slashCount%2==1}function normalizeStructuralPunctuation(char){return"，"===char?",":"："===char?":":"；"===char?";":char}function parseValue(val){if("true"===(val=val.trim()))return!0;if("false"===val)return!1;if("null"===val)return null;if("undefined"!==val){if(!isNaN(Number(val))&&""!==val)return Number(val);if(val.startsWith('"')&&val.endsWith('"'))try{return JSON.parse(val)}catch(e){return val.slice(1,-1)}if(val.startsWith("'")&&val.endsWith("'"))return val.slice(1,-1);if(val.startsWith("{")&&val.endsWith("}")||val.startsWith("[")&&val.endsWith("]"))try{return JSON.parse(val)}catch(e){const manualParsed=tryParseObject(val);if(manualParsed)return manualParsed;let fixedKeys=val.replace(/([{,]\s*)(\d+)(\s*:)/g,'$1"$2"$3');try{return JSON.parse(fixedKeys)}catch(e2){let fixedQuotes=fixedKeys.replace(/'/g,'"');try{return JSON.parse(fixedQuotes)}catch(e3){let fixedAllKeys=val.replace(/([{,]\s*)([a-zA-Z0-9_]+)(\s*:)/g,'$1"$2"$3');try{return JSON.parse(fixedAllKeys)}catch(e4){return val}}}}return val}}function tryParseObject(str){if(!str.startsWith("{")||!str.endsWith("}"))return null;let content=str.slice(1,-1);const result={};let hasMatch=!1;const strings=[];let placeholderIndex=0;content=content.replace(/"([^"\\]|\\.)*"|'([^'\\]|\\.)*'/g,match=>{const placeholder=`__STR_${placeholderIndex}__`;return strings.push(match),placeholderIndex++,placeholder});const keyRegex=/(?:^|[,;:]+\s*)(?:(\d+)|([a-zA-Z0-9_]+)|(__STR_\d+__))\s*:/g;let match,lastIndex=0,lastKey=null;for(;null!==(match=keyRegex.exec(content));){if(hasMatch=!0,null!==lastKey){let valStr=content.slice(lastIndex,match.index).trim();valStr=valStr.replace(/[,;:]+$/,"").trim(),result[restoreStrings(lastKey,strings)]=restoreStrings(valStr,strings)}lastKey=match[1]||match[2]||match[3],lastIndex=match.index+match[0].length}if(null!==lastKey){let valStr=content.slice(lastIndex).trim();valStr=valStr.replace(/[,;:]+$/,"").trim(),result[restoreStrings(lastKey,strings)]=restoreStrings(valStr,strings)}return hasMatch?result:null}function restoreStrings(str,strings){if(!str)return str;let restored=str;return restored=restored.replace(/__STR_(\d+)__/g,(match,index)=>strings[parseInt(index,10)]),cleanValueStr(restored)}function cleanValueStr(str){return str.startsWith('"')&&str.endsWith('"')||str.startsWith("'")&&str.endsWith("'")?str.slice(1,-1):str}function _argsToOperation(name,args){if("insertRow"===name){const tableIndex=normalizeLegacyIndex(args[0]);return 2===args.length&&null!==tableIndex&&isPlainDataObject(args[1])?{op:"insertRow",tableIndex:tableIndex,data:args[1]}:(log("insertRow 参数无效：需要 (非负整数 tableIndex, data 对象)。","error"),null)}if("updateRow"===name){const tableIndex=normalizeLegacyIndex(args[0]),rowIndex=normalizeLegacyIndex(args[1]);return 3===args.length&&null!==tableIndex&&null!==rowIndex&&isPlainDataObject(args[2])?{op:"updateRow",tableIndex:tableIndex,rowIndex:rowIndex,data:args[2]}:(log("updateRow 参数无效：需要 (非负整数 tableIndex, 非负整数 rowIndex, data 对象)。","error"),null)}if("deleteRow"===name){const tableIndex=normalizeLegacyIndex(args[0]),rowIndex=normalizeLegacyIndex(args[1]);return 2!==args.length||null===tableIndex||null===rowIndex?(log("deleteRow 参数无效：需要 (非负整数 tableIndex, 非负整数 rowIndex)。","error"),null):{op:"deleteRow",tableIndex:tableIndex,rowIndex:rowIndex}}return null}function normalizeLegacyIndex(value){if(Number.isSafeInteger(value)&&value>=0)return value;if("string"!=typeof value||!/^[0-9]+$/u.test(value))return null;const normalized=Number(value);return Number.isSafeInteger(normalized)?normalized:null}function isPlainDataObject(value){return Boolean(value)&&"object"==typeof value&&!Array.isArray(value)&&Object.getPrototypeOf(value)===Object.prototype}export function parseToOperations(aiResponseText){const match=(aiResponseText||"").match(/<Amily2Edit>([\s\S]*?)<\/Amily2Edit>/);if(!match)return[];const commandBlock=match[1].replace(/<!--|-->/g,"").trim();if(!commandBlock)return[];const commands=commandBlock.split("\n").filter(line=>""!==line.trim());if(0===commands.length)return[];const ops=[];for(const commandString of commands){const trimmed=commandString.trim();if(!/^(?:insertRow|updateRow|deleteRow)\b/u.test(trimmed))continue;const parsed=parseFunctionCall(trimmed);if(!parsed)return[];const op=_argsToOperation(parsed.name,parsed.args);if(!op)return[];ops.push(op)}return ops}export function parseToOperationsDetailed(aiResponseText){const source=String(aiResponseText??""),openTags=source.match(/<Amily2Edit>/gu)||[],closeTags=source.match(/<\/Amily2Edit>/gu)||[];if(1!==openTags.length||1!==closeTags.length)return strictParseFailure("TABLE_FILL_EDIT_BLOCK_COUNT","响应必须包含且只能包含一对完整的 <Amily2Edit> 标签。");const match=/<Amily2Edit>([\s\S]*?)<\/Amily2Edit>/u.exec(source);if(!match)return strictParseFailure("TABLE_FILL_EDIT_BLOCK_INCOMPLETE","响应中的 <Amily2Edit> 标签未完整闭合。");if(!isAllowedLegacyEditEnvelope(`${source.slice(0,match.index)}${source.slice(match.index+match[0].length)}`))return strictParseFailure("TABLE_FILL_EDIT_BLOCK_OUTSIDE_CONTENT","严格文本填表响应在 <Amily2Edit> 块外只能包含完整的 thinking/finish 兼容标签。");const block=match[1];if(!block.trim())return{ok:!0,empty:!0,operations:[],error:null};const commentBlock=extractCompleteCommentOnlyBlock(block);if(commentBlock.malformed)return strictParseFailure("TABLE_FILL_EDIT_BLOCK_MALFORMED_COMMENT","Amily2Edit 中的 HTML 注释未完整闭合或包含非法嵌套。");const commandBlock=commentBlock.matched?commentBlock.content.trim():block.trim();if(commentBlock.matched&&!commandBlock)return{ok:!0,empty:!0,operations:[],error:null};if(!commentBlock.matched&&/<!--|-->/u.test(commandBlock))return strictParseFailure("TABLE_FILL_EDIT_BLOCK_MALFORMED_COMMENT","Amily2Edit 中的 HTML 注释必须完整，且不能与裸文本混排。");const commands=commandBlock.split(/\r?\n/u),operations=[];let unknownCommentLine=!1;for(let index=0;index<commands.length;index+=1){const trimmed=commands[index].trim();if(!trimmed)continue;if(!/^(?:insertRow|updateRow|deleteRow)\b/u.test(trimmed)){if(commentBlock.matched){if(isExplicitLegacyNoopLine(trimmed))continue;unknownCommentLine=!0;continue}return strictParseFailure("TABLE_FILL_EDIT_BLOCK_UNKNOWN_LINE",`第 ${index+1} 行不是允许的表格操作。`,index+1)}const parsed=parseFunctionCall(trimmed);if(!parsed)return strictParseFailure("TABLE_FILL_EDIT_BLOCK_MALFORMED_COMMAND",`第 ${index+1} 行的表格操作无法解析。`,index+1);const operation=_argsToOperation(parsed.name,parsed.args);if(!operation)return strictParseFailure("TABLE_FILL_EDIT_BLOCK_INVALID_ARGUMENTS",`第 ${index+1} 行的表格操作参数无效。`,index+1);operations.push(operation)}return unknownCommentLine?strictParseFailure("TABLE_FILL_EDIT_BLOCK_UNKNOWN_LINE","包含表格操作的 HTML 注释中混入了未知文本。"):0===operations.length?commentBlock.matched?{ok:!0,empty:!0,operations:[],error:null}:strictParseFailure("TABLE_FILL_EDIT_BLOCK_NO_COMMANDS","非空的 <Amily2Edit> 块没有包含可执行的表格操作。"):{ok:!0,empty:!1,operations:operations,error:null}}function isAllowedLegacyEditEnvelope(outside){return/^(?:\s*<(thinking|finish|finsh)>[\s\S]*?<\/\1>\s*)*$/u.test(outside)}function isExplicitLegacyNoopLine(line){const source=String(line??"").trim();if(!source)return!0;if(/^(?:\/\/|#)/u.test(source))return!0;const normalized=source.replace(/[。.!！]+$/gu,"").trim();return/^(?:无(?:可靠)?(?:的)?表格(?:更新|变更|修改)|无需(?:进行)?(?:表格)?(?:更新|变更|修改)|没有(?:可靠)?(?:的)?(?:表格)?(?:更新|变更|修改)|本轮(?:无需|无)(?:进行)?(?:表格)?(?:更新|变更|修改)|本轮保持原表|保持原表|(?:no|without)\s+(?:(?:reliable|table)\s+)*(?:changes?|updates?|modifications?)|no[\s-]?op|none)$/iu.test(normalized)}function extractCompleteCommentOnlyBlock(block){const source=String(block??"");let cursor=0;const bodies=[];let matched=!1;for(;cursor<source.length;){if(cursor+=(/^\s*/u.exec(source.slice(cursor))?.[0]||"").length,cursor>=source.length)break;if(!source.startsWith("\x3c!--",cursor))return{matched:!1,malformed:matched||source.slice(cursor).includes("--\x3e"),content:""};matched=!0;const bodyStart=cursor+4,closeIndex=source.indexOf("--\x3e",bodyStart);if(closeIndex<0)return{matched:!1,malformed:!0,content:""};const body=source.slice(bodyStart,closeIndex);if(body.includes("\x3c!--")||body.includes("--"))return{matched:!1,malformed:!0,content:""};bodies.push(body),cursor=closeIndex+3}return{matched:matched,malformed:!1,content:bodies.join("\n")}}function strictParseFailure(code,message,line){return{ok:!1,empty:!1,operations:[],error:{code:code,message:message,...Number.isSafeInteger(line)?{line:line}:{}}}}export function executeCommands(aiResponseText,initialState){const ops=parseToOperations(aiResponseText);if(0===ops.length)return{finalState:initialState,hasChanges:!1,changes:[]};log(`检测到 ${ops.length} 条 AI 指令，开始推演...`,"info");const{state:state,changes:changes}=applyOperations(initialState,ops);return{finalState:state,hasChanges:changes.length>0,changes:changes}}