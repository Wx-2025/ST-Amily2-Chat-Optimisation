import{setExtensionPrompt}from"/script.js";import{extension_settings}from"/scripts/extensions.js";import{getBatchFillerFlowTemplate,convertTablesToCsvString,convertTablesToCsvStringForContentOnly,getMemoryState,setMemoryState,triggerSync}from"./manager.js";import{commitToLastMessageAsync}from"./infra/persistence.js";import{applyPendingRecordDeletions}from"./module-tables.js";import{tableSystemDefaultSettings}from"./settings.js";import{extensionName}from"../../utils/settings.js";import{log}from"./logger.js";import{renderTables}from"../../ui/table-bindings.js";import{updateOrInsertTableInChat}from"../../ui/message-table-renderer.js";const INJECTION_KEY="AMILY2_TABLE_SYSTEM";export function generateTableContent(){const settings=extension_settings[extensionName]||{};let injectionContent="";if(!1===settings.table_system_enabled||!settings.table_injection_enabled)return"";try{const fillingMode=settings.filling_mode||"main-api";if("secondary-api"===fillingMode){const contentOnlyTemplate="##以下内容是故事发生的剧情中提取出的内容，已经转化为表格形式呈现给你，请将以下内容作为后续剧情的一部分参考：\n{{{Amily2TableDataContent}}}",dataString=convertTablesToCsvStringForContentOnly();dataString.trim()&&(injectionContent=contentOnlyTemplate.replace("{{{Amily2TableDataContent}}}",dataString))}else if("optimized"===fillingMode){const contentOnlyTemplate="##以下内容是故事发生的剧情中提取出的内容，已经转化为表格形式呈现给你，请将以下内容作为后续剧情的一部分参考：\n{{{Amily2TableDataContent}}}",dataString=convertTablesToCsvStringForContentOnly();dataString.trim()&&(injectionContent=contentOnlyTemplate.replace("{{{Amily2TableDataContent}}}",dataString))}else{const flowTemplate=getBatchFillerFlowTemplate(),dataString=convertTablesToCsvString();flowTemplate&&dataString.trim()&&(injectionContent=flowTemplate.replace("{{{Amily2TableData}}}",dataString))}injectionContent.trim()&&window.MiZheSi_Global?.isEnabled()&&(injectionContent=`%%AMILY2_TABLE_INJECTION%%${injectionContent}`)}catch(error){return console.error("[Amily2-表格内容生成器] 生成表格内容时发生错误:",error),""}return injectionContent}export async function injectTableData(chat,contextSize,abort,type){if(!1===(extension_settings[extensionName]||{}).table_system_enabled)return void setExtensionPrompt(INJECTION_KEY,"",0,0,!1,"SYSTEM");try{const deletion=applyPendingRecordDeletions(getMemoryState()||[]);if(deletion.deletedCount>0){if(!await commitToLastMessageAsync(deletion.state))throw new Error("待删除行无法原子持久化，已保留原状态。");setMemoryState(deletion.state),triggerSync(),log(`【延迟删除】已在注入前提交 ${deletion.deletedCount} 条待删除行并永久保存状态。`,"info"),renderTables(),updateOrInsertTableInChat()}}catch(error){console.error("[Amily2-延迟删除] 在注入前提交待删除行时发生错误:",error)}if(!0===window.AMILY2_MACRO_REPLACED)return console.log("[Amily2-表格注入器] 检测到宏已替换，跳过传统注入。"),window.AMILY2_MACRO_REPLACED=!1,void setExtensionPrompt(INJECTION_KEY,"",0,0,!1,"SYSTEM");const settings=extension_settings[extensionName]||{};if("quiet"!==type)try{let injectionContent=generateTableContent();if(!settings.table_injection_enabled)return void setExtensionPrompt(INJECTION_KEY,"",0,0,!1,"SYSTEM");if(!injectionContent||""===injectionContent.trim())return void setExtensionPrompt(INJECTION_KEY,"",0,0,!1,"SYSTEM");const injectionSettings=settings.injection||tableSystemDefaultSettings.injection,position=parseInt(injectionSettings.position,10),depth=parseInt(injectionSettings.depth,10),role=parseInt(injectionSettings.role,10);setExtensionPrompt(INJECTION_KEY,injectionContent,position,depth,!1,role),console.log(`[Amily2-表格注入器] 已成功注入表格数据 (位置: ${position}, 深度: ${depth}, 角色: ${role})。`)}catch(error){console.error("[Amily2-表格注入器] 注入表格数据时发生错误:",error)}}