import{extension_settings}from"/scripts/extensions.js";import{buildGoogleEmbeddingRequest,parseGoogleEmbeddingResponse,buildGoogleEmbeddingApiUrl}from"./utils/googleAdapter.js";import{acquireProfileRequestPermit,bindSlotProfileRateLimit,getSlotProfile}from"./api/api-resolver.js";import{extensionName}from"../utils/settings.js";function sanitizeMaskedKey(key){return"••••••••"===key?"":key}function getSettings(){const root=extension_settings[extensionName],nested=root&&root["hanlinyuan-rag-core"];if(nested)return nested;const legacy=extension_settings["hanlinyuan-rag-core"];return legacy||(console.error("[翰林院-API] 无法获取设置，API调用可能失败。"),{retrieval:{},rerank:{}})}export async function getEmbedRetrievalSettings(){const profile=await getSlotProfile("ragEmbed");if(profile){const apiKey=sanitizeMaskedKey(profile.apiKey??"");return bindSlotProfileRateLimit({apiEndpoint:"google"===profile.provider?"google_direct":"custom",customApiUrl:profile.apiUrl,apiKey:apiKey,embeddingModel:profile.model,batchSize:getSettings().retrieval?.batchSize??5,_keyMissingFromProfile:!apiKey,_profileName:profile.name||profile.id},profile)}const fallback=getSettings().retrieval||{};return{...fallback,apiKey:sanitizeMaskedKey(fallback.apiKey??"")}}export function describeMissingKey(resolved,plainMessage){return resolved?._keyMissingFromProfile?`连接配置「${resolved._profileName}」在本设备上没有可用的 API Key。Key 仅保存在最初填写它的设备/浏览器上，不随云端设置同步。请在「API 连接配置」面板编辑该配置并重新填写 Key。`:plainMessage}export async function getRerankSettings(){const profile=await getSlotProfile("ragRerank");if(profile){const manualSettings=getSettings().rerank||{},apiKey=sanitizeMaskedKey(profile.apiKey??"");return bindSlotProfileRateLimit({url:profile.apiUrl,apiKey:apiKey,model:profile.model,top_n:manualSettings.top_n??10,apiMode:manualSettings.apiMode??"custom",_keyMissingFromProfile:!apiKey,_profileName:profile.name||profile.id},profile)}const fallback=getSettings().rerank||{};return{...fallback,apiKey:sanitizeMaskedKey(fallback.apiKey??"")}}export function getSanitizedBaseUrl(rawApiUrl){let baseUrl=rawApiUrl.trim();return baseUrl.endsWith("/")&&(baseUrl=baseUrl.slice(0,-1)),baseUrl.endsWith("/v1")&&(baseUrl=baseUrl.slice(0,-3)),baseUrl.endsWith("/embeddings")&&(baseUrl=baseUrl.slice(0,-11)),baseUrl}export async function fetchEmbeddingModels(overrideSettings=null){const settings=overrideSettings||await getEmbedRetrievalSettings(),{apiEndpoint:apiEndpoint,apiKey:apiKey,customApiUrl:customApiUrl}=settings;let modelsUrl,responseParser,headers={};switch(apiEndpoint){case"google_direct":if(!apiKey)throw new Error(describeMissingKey(settings,"Google直连模式需要API Key。"));const fetchGoogleModels=async version=>{const url=`https://generativelanguage.googleapis.com/${version}/models`;console.log(`[翰林院] 正在从 Google API (${version}) 获取模型列表: ${url}`);const response=await fetch(url,{headers:{"x-goog-api-key":apiKey}});if(!response.ok)return console.warn(`获取 Google API (${version}) 模型列表失败: ${response.status}`),[];const json=await response.json();return json.models&&Array.isArray(json.models)?json.models.filter(model=>model.supportedGenerationMethods?.includes("embedContent")||model.supportedGenerationMethods?.includes("batchEmbedContents")).map(model=>model.name.replace("models/","")):[]},[v1Models,v1betaModels]=await Promise.all([fetchGoogleModels("v1"),fetchGoogleModels("v1beta")]);return[...new Set([...v1Models,...v1betaModels])].sort();case"custom":if(!customApiUrl)throw new Error("自定义模式需要API URL。");if(!apiKey)throw new Error("自定义模式需要API Key。");modelsUrl=`${getSanitizedBaseUrl(customApiUrl)}/v1/models`,headers=getApiHeaders(settings),console.log(`[翰林院] 正在从 ${modelsUrl} 获取模型列表 (需要认证)...`),responseParser=json=>{if(!json.data||!Array.isArray(json.data))throw new Error("模型API的响应格式无效: 未找到 'data' 数组。");return json.data.map(m=>m.id).sort()};break;default:if(!customApiUrl)throw new Error("本地代理模式需要API URL。");modelsUrl=`${getSanitizedBaseUrl(customApiUrl)}/v1/models`,headers={"Content-Type":"application/json"},console.log(`[翰林院] 正在从 ${modelsUrl} 获取模型列表 (无需认证)...`),responseParser=json=>{if(!json.data||!Array.isArray(json.data))throw new Error("模型API的响应格式无效: 未找到 'data' 数组。");return json.data.map(m=>m.id).sort()}}if(!modelsUrl)throw new Error("无法确定获取模型的有效路径。");const response=await fetch(modelsUrl,{method:"GET",headers:headers});if(!response.ok){const errorBody=await response.text();throw new Error(`获取模型列表失败 (${response.status}): ${errorBody}`)}return responseParser(await response.json())}export function getRerankBaseUrl(rawApiUrl){let baseUrl=rawApiUrl.trim();return baseUrl.endsWith("/")&&(baseUrl=baseUrl.slice(0,-1)),baseUrl.endsWith("/v1")&&(baseUrl=baseUrl.slice(0,-3)),baseUrl.endsWith("/rerank")&&(baseUrl=baseUrl.slice(0,-7)),baseUrl}export async function fetchRerankModels(){const settings=await getRerankSettings(),{url:url,apiKey:apiKey,apiMode:apiMode="custom"}=settings;if(!url)throw new Error("Rerank API URL 未提供。");if("custom"===apiMode&&!apiKey)throw new Error(describeMissingKey(settings,"自定义模式下，Rerank API Key 未提供。"));const modelsUrl=`${getRerankBaseUrl(url)}/v1/models`,headers={"Content-Type":"application/json"};"custom"===apiMode&&(headers.Authorization=`Bearer ${apiKey}`),console.log(`[翰林院-Rerank] 正在从 ${modelsUrl} 获取模型列表 (模式: ${apiMode})...`);const response=await fetch(modelsUrl,{method:"GET",headers:headers});if(!response.ok){const errorBody=await response.text();throw new Error(`获取Rerank模型列表失败 (${response.status}): ${errorBody}`)}const data=await response.json();if(!data.data||!Array.isArray(data.data))throw new Error("Rerank模型API的响应格式无效: 未找到 'data' 数组。");return data.data.map(m=>m.id).sort()}export async function executeRerank(query,documents,rerankSettings=null){const resolved=rerankSettings||await getRerankSettings(),{url:url,apiKey:apiKey,model:model,top_n:top_n,apiMode:apiMode="custom"}=resolved;if(!url)throw new Error("Rerank API URL 未提供。");if("custom"===apiMode&&!apiKey)throw new Error(describeMissingKey(resolved,"自定义模式下，Rerank API Key 未提供。"));const rerankUrl=`${getRerankBaseUrl(url)}/v1/rerank`,headers={"Content-Type":"application/json"};"custom"===apiMode&&(headers.Authorization=`Bearer ${apiKey}`);const body=JSON.stringify({query:query,documents:documents,model:model,top_n:top_n});console.log(`[翰林院-Rerank] 正在向 ${rerankUrl} 发送请求 (模式: ${apiMode})...`),await acquireProfileRequestPermit(resolved);const response=await fetch(rerankUrl,{method:"POST",headers:headers,body:body});if(!response.ok)throw new Error(`Rerank API 请求失败 (${response.status}): ${await response.text()}`);return await response.json()}export function getApiEndpointUrl(raw=!1,overrideRetrieval=null){const{apiEndpoint:apiEndpoint,customApiUrl:customApiUrl}=overrideRetrieval||getSettings().retrieval;let url;switch(apiEndpoint){case"openai":url="https://api.openai.com";break;case"azure":case"custom":case"local_proxy":url=customApiUrl;break;default:url=customApiUrl||"https://api.openai.com"}return raw?url:getSanitizedBaseUrl(url)+"/v1/embeddings"}export function getApiHeaders(overrideRetrieval=null){const headers={"Content-Type":"application/json"},{apiKey:apiKey,apiEndpoint:apiEndpoint}=overrideRetrieval||getSettings().retrieval;switch(apiEndpoint){case"openai":case"custom":headers.Authorization=`Bearer ${apiKey}`;break;case"azure":headers["api-key"]=apiKey}return headers}export async function getEmbeddings(texts,signal=null,overrideSettings=null){const settings=overrideSettings||await getEmbedRetrievalSettings(),{apiEndpoint:apiEndpoint,customApiUrl:customApiUrl,apiKey:apiKey,embeddingModel:embeddingModel,batchSize:batchSize=5}=settings,allEmbeddings=[];for(let i=0;i<texts.length;i+=batchSize){if(signal?.aborted)throw new Error("AbortError");const batch=texts.slice(i,i+batchSize);let batchEmbeddings=[];if("google_direct"===apiEndpoint){if(console.log("[翰林院-API] 使用Google直连模式获取向量。"),!apiKey)throw new Error(describeMissingKey(settings,"Google直连模式需要API Key。"));const googleUrl=buildGoogleEmbeddingApiUrl("https://generativelanguage.googleapis.com",embeddingModel),googleBody=buildGoogleEmbeddingRequest(batch,embeddingModel);await acquireProfileRequestPermit(settings,signal);const googleResponse=await fetch(googleUrl,{method:"POST",headers:{"Content-Type":"application/json","x-goog-api-key":apiKey},body:JSON.stringify(googleBody),signal:signal});if(!googleResponse.ok)throw new Error(`Google embedding request failed (${googleResponse.status}).`);const googleData=await googleResponse.json();if(!googleData||!Array.isArray(googleData.embeddings))throw new Error("Google embedding response has an invalid shape.");batchEmbeddings=parseGoogleEmbeddingResponse(googleData,batch)}else{console.log(`[翰林院-API] 使用前端直连模式 (${apiEndpoint}) 获取向量。`);const url=getApiEndpointUrl(!1,settings),headers=getApiHeaders(settings);await acquireProfileRequestPermit(settings,signal);const response=await fetch(url,{method:"POST",headers:headers,body:JSON.stringify({input:batch,model:embeddingModel}),signal:signal});if(!response.ok)throw new Error(`Embedding request failed (${response.status}).`);const result=await response.json();if(!result.data||!Array.isArray(result.data))throw new Error("API返回的向量数据格式不正确。");batchEmbeddings=result.data.map(item=>item.embedding)}if(batchEmbeddings.length!==batch.length)throw new Error("获取到的向量数量与发送的文本数量不匹配。");allEmbeddings.push(...batchEmbeddings),i+batchSize<texts.length&&await new Promise(resolve=>setTimeout(resolve,200))}return allEmbeddings}export async function testApiConnection(){await getEmbeddings(["测试连接"])}