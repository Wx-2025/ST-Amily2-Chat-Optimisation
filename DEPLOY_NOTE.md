# 部署更新日志

每个版本块格式：`## v{version}`，Jenkins 构建时自动提取对应块作为 GitHub 提交说明。

---

## v2.3.1

### 表格填充可靠性修复

- **Function Call 严格校验**：只有结构完整且全部操作合法的 `{"operations": [...]}` 才会进入执行；空响应、损坏 JSON、多个根对象、缺失操作数组或部分非法操作会整批拒绝，不再被误判为“无需修改”并跳过
- **立即填表原批次重试**：模型响应无法解析时保持当前批次与楼层不变，按配置重试；达到上限后暂停并保留错误信息，不再静默推进下一块
- **分步自动填表空回修复**：空字符串、空 Function Call 和无效工具参数统一进入失败处理；只有明确的空操作数组才会标记目标楼层为已处理
- **提示词链自愈**：运行时补齐分步填表与批量填表所需的规则、流程、历史、世界书和核心内容段，既有但不完整的旧预设不会再造成空提示或缺失表格快照
- **操作原子性**：整批操作先完成表索引、模块归属、行索引和数据格式校验；任一操作失败时整批回滚，避免前半批已经写入、后半批失败形成半完成状态

### 自动重试与跨聊天安全

- **重试前可见提示**：每次分步自动重试前显示 `x/y` 次数与 3 秒等待提示
- **持久失败锁**：达到上限后按实际失败楼层及“内容长度 + 内容哈希”写入失败锁；后续消息事件不会重新从 0 开始调用，也不会绕过失败楼层继续处理后文
- **明确恢复方式**：编辑目标消息后内容指纹变化会自然解除锁；手动强制重填可旁路旧锁，成功保存后在同一事务中清除
- **目标指纹隔离**：重试次数与当前楼层/批次指纹绑定，等待期间出现新消息或批次变化时，新目标不会继承旧目标的重试次数
- **聊天作用域固定**：防抖、自动重试、弹窗补全与持久化始终绑定发起时的聊天；切换聊天后旧请求、旧计时器和旧结果不会写入新聊天
- **重复写入防护**：表格已经持久化后，即使日志、通知或界面刷新失败也不会再次调用模型；中断、重置和新任务间的计时器及控制器不会互相误清理

### 验证

- 表格专项 `61/61`、时间河 `102/102`、渐进记忆 `37/37` 全部通过
- 安全专项 `53` 项：`46` 项通过、`7` 项仅因缺少实际双产物环境按设计跳过、`0` 失败
- 语法检查与 `git diff --check` 通过

---

## v2.3.0

> 当前为预发布候选；真实 SillyTavern 候选 05 已完成时间河页面与本地 IndexedDB 的有限验收。以下内容只有在真实向量摄取、冻结提交生成的 `dev` / `dev-ori` 双产物、Jenkins BuildServer 全部门禁及 `dev` 到 `main` 的原样晋升通过后，才构成实际发布内容。

### 安全闭环

- **公共 Bus 权限继续收口**：核心服务与模块命名空间默认拒绝外部覆盖，模型调用、工具执行及敏感配置不再通过公共注册表暴露；启动阶段会确保使用当前受保护的 Bus 实例，避免延迟注册落到旧实例
- **内部表格事件隔离**：表格变更通知改走插件内部发布/订阅通道，发布者只提交表索引，事件内容统一从规范 store 读取、校验并冻结；不再信任页面可伪造的全局 DOM `CustomEvent` 或调用方自带 payload
- **模型连接不可重定向**：运行时调用参数只能调整有界的生成参数与取消信号，不能覆盖已配置的 API 地址、密钥、供应商、模型、消息、工具或路由字段
- **危险接口最小化**：公共能力按明确白名单开放，保留跨模块所需的只读与受约束操作；消息、世界书、表格、战斗和记忆等高风险写入只能走各自的内部授权路径
- **回归测试补强**：新增 Bus 启动、保留命名空间、模型接口、内部表格事件及表格生命周期安全测试
- **非聊天上下文降噪**：欢迎页或尚未选中聊天时不再输出角色/聊天 ID 获取失败；只有已进入真实聊天却仍无法解析上下文时才保留警告
- **非公开核心发布门禁**：内测功能、认证密钥与关键权限策略统一进入 JavaScript 混淆保护清单；本文所称“加密”即代码混淆，不表示密码学加密；保护目录新增源码但未登记时，测试与发布流水线都会拒绝继续
- **开发校验资产隔离**：安全回归测试、混淆/同步校验脚本、保护清单和 Jenkins 构建定义仅保留在开发仓库，发布同步会跳过并清理历史残留
- **模型 HTML 渲染默认关闭**：只有用户显式启用才处理聊天代码块；iframe 固定使用不含 `allow-same-origin` 的 opaque-origin 沙箱，旧版无来源校验的消息桥已删除，全部能力经登记来源和分级授权接口调用
- **凭证不再回填 DOM**：主 API、剧情并发、NGMS、NCCS、术语库、角色世界书、自动生卡、翰林院和图片生成等密码框只显示“已保存”状态；空白保持原值，主动编辑后清空才删除，实际请求直接从凭证存储读取
- **外部内容按文本渲染**：远端模型名、世界书选项、更新日志和关系图 tooltip 不再通过未经转义的 HTML 插值进入页面
- **更新与依赖链收口**：插件内更新器只做版本提醒并打开提交记录，不再直接拉取可变 `main`；文本解码与差异对比复用浏览器/SillyTavern 内置能力，保留的 ECharts 精确固定版本并启用 SRI
- **权限分级边界明确**：Type2 / Type3 都只是客户端功能开关，不作为安全认证或服务端授权依据；自行修改本地状态进入相应功能者自行承担测试风险

### 表格数据库化一期

- **版本化状态与稳定身份**：新增 `amily2.table-state@2` 聊天状态封包以及稳定的表、列、记录 ID；无消息的新聊天也会同步聊天 metadata，历史消息快照继续承担兼容与回滚用途
- **模块固定表与 CRUD**：模块可幂等注册固定 schema 的表，并通过 owner 隔离的增删改查接口维护数据；提供深拷贝快照和只读基础查询，调用方不能直接修改运行时 store
- **关系完整性**：支持业务主键、唯一列、`reference` 外键、跨 owner 显式授权，以及 `restrict` / 非必填 `setNull` 删除策略；传统填表、Function Call 与界面操作不能绕过模块表 schema 和约束
- **TableProfile 分层隔离**：加载顺序固定为“聊天快照 > 当前角色卡 > 全局档案 > 内置档案”，表结构与填表模板随聊天快照冻结，切换卡片或修改全局配置不会反向污染已有聊天
- **角色卡可移植档案**：表格面板可查看当前档案来源、导出不含运行时行数据的档案，并通过红色警示与二次确认手动写回或移除当前角色卡档案；不会自动改写角色卡
- **旧预设兼容与安全回退**：传统 v2/v3 表格预设导入时转换为 `TableProfile`；损坏、未知版本、超限或包含模块归属表的数据会被拒绝并安全回退，旧聊天仍保留原解析路径
- **持久化闭环**：填表、预设导入、延迟删除、分步填表 hash、历史版本恢复和消息删除后的回退统一采用“持久化成功后再发布状态”的补偿事务；导入只覆盖用户表，模块表保持独立
- **并发写入串行化**：模块 CRUD 的“读取—校验—保存—发布”完整临界区进入 FIFO 队列，修复战斗模块并发建表或同时更新双方记录时的覆盖丢失

### 时间河 Phase 1

- **独立管理页面**：Type2 及以上用户可在时间河页面选择聊天绑定、本地导入 TXT/MD、预览章节目录、提交语料并查看 Revision 与任务状态；入口、页面、service、后台任务与生成注入统一执行 Type2 门禁
- **可靠语料流水线**：语料、Revision、分块与任务账本使用独立 IndexedDB 等价事务；并发提交、不可变 staging、Manifest CAS、租约续期、暂停与失败重试均失败关闭
- **章节同轨检索**：每章使用独立 `story_track` 向量集合；用户手动设置当前章节和过去窗口，未来窗口固定为 0，查询只能访问显式白名单
- **生成期受控注入**：只有 active Revision、显式聊天绑定和开启注入后才会检索；聊天切换、静默生成、权限变化或清理失败会阻断旧内容跨会话残留
- **隐私与可解释性**：目录预览完全在本地完成，不触发 embedding 或向量查询；远端错误与任务诊断不保存响应正文、原文或凭证
- **保护期**：时间河全部运行时 JavaScript 及模块挂载文件在 `dev` / `dev-ori` 产物及由 `dev` 原样晋升的 `main` 中统一混淆；最低三个月从首个实际包含 Time River Phase 1 受保护代码的公开可下载产物 UTC 时间起算，已有但不含该代码的 `2.3.0` 标签或空壳产物不得回溯起算；实际发布前日期、提交和产物哈希均为 `TBD`，到期不自动解除

### 界面与配置体验

- **统一 UI Kit**：主面板、系统配置、API 配置、规则中心、总结、超级记忆、角色世界书、术语表、一键生卡与附加功能统一为深色半透明卡片、紫蓝强调色、统一间距和圆角体系
- **API 配置重构**：连接管理改为“连接 / 分配 / 更多”分段页面；各功能槽支持独立启停，NCCS 对外文案统一为“独立 API 填表”，常用配置路径增加白话说明
- **教程分层**：API、规则、正文优化、超级记忆、翰林院、内阁等面板补充入门与进阶教程，长表格和流程说明针对移动端收窄
- **时间河视觉适配**：时间河四阶段流程接入统一组件体系，保留顺序工作流与全部安全控件；修复局部样式覆盖导致窄栏中文按钮逐字换行的问题，390px 宽度下按钮保持横排，页面无整体横向溢出，目录表仅在自身容器滚动
- **入口与更新提示修复**：内测入口维持 2×2 布局，避免窄屏四按钮被压成竖排；更新提示复用唯一 DOM 节点，不再与版本更新器重复创建 ID 或互相覆盖

### 渐进记忆开发安全加固

- **稳定数据源约束**：数据源改用稳定表 ID 与允许列白名单，不再接受自由文本表名；输入体积、采样数量和 Token 均受显式硬上限控制
- **数据外发确认**：启用 AI 压缩前必须确认表格内容会发往所选模型；注入角色只允许用户或 AI，不允许伪装为 system
- **跨聊天失败关闭**：聊天切换、静默生成、上下文不唯一、权限变化或清理失败时清除旧状态并阻断注入；群聊无法得到唯一身份时不猜测绑定
- **版本边界不变**：本轮只完成开发中功能的安全收口和回归覆盖，渐进记忆正式验收仍不属于 v2.3.0 的公开承诺

### 发布流水线

- **双产物同源构建**：Jenkins 从一个不可变源码提交分别生成 compact `dev` 与保留普通格式的 `dev-ori`；两者共用保护清单并以一次原子推送发布
- **固定工具链与来源证明**：构建显式使用 Node.js 24.12.0、Terser 5.49.0 和 javascript-obfuscator 5.4.3；产物记录源码 SHA、分支、配置哈希与文件哈希
- **Node 20/24 测试发现兼容**：目录测试套件改由跨平台运行器显式递归枚举 `.test.mjs` 文件，避免 Node 24 将 `node --test tests/<suite>` 解释为单一模块路径而误报 `MODULE_NOT_FOUND`
- **BuildServer SSH 前置条件收敛**：GitHub checkout 与原子推送复用既有 `github-ssh-key` 发布凭据，并严格校验节点运行用户现有的 `known_hosts`；主机条目缺失时输出明确错误并失败关闭，不再额外依赖未配置的 Secret file 凭据
- **可读格式保留模型收敛**：只维护 `dev-ori` 作为查验用的 original-format 发布产物；它仍会排除内部文档、测试与构建脚本，并混淆保护清单，并非开发仓完整源码镜像；正式 `main` 由验证通过的 compact `dev` 原样晋升，不重新构建，也不再维护冗余的 `main-ori`
- **发布说明自动带入**：compact 与 original-format 两个提交均自动提取本文件对应版本块，避免产物提交只留下无内容的固定标题

### 本版本边界

- `TableProfile.views` 仅保留协议字段；视图执行器、联表/聚合运算、上下文宏和用户编辑器暂不开放
- 一键生卡的“不绑定 / 绑定现有档案 / 创建空档案”三选项推迟到 v2.3.2；现有角色更新仍保留未知扩展字段，不会主动覆盖卡内表格档案
- 第三方数据库脚本兼容迁移、渐进记忆正式验收、战斗系统产品化，以及时间河 Phase 2～4 的世界构建、偏离检测和推进建议不属于 v2.3.0

### 发布前验证

- QD 合并、Node 24 测试发现修复及时间河 Type2/按钮回归后的本地全量测试：281 项，273 项通过、0 失败、8 项按设计跳过；其中时间河 102/102、表格 42/42、渐进记忆 37/37、发布工具 1/1，安全专项 46 项通过、7 项实际产物模式检查跳过
- 历史候选 05 compact（早于最终 QD 合并提交）：精确同步 272 个部署、1 个未变、23 个忽略；Terser 221/221、混淆 51/51、单产物门禁 6/6、语法检查 221/221
- 真实 SillyTavern 已成功加载 TimeRiver，且无聊天上下文不再报错；已恢复 `smoke-novel` 的 `r1` staging Revision 与 5 个目录节点，选择清空/恢复、任务状态及安全控件通过
- 本轮未执行真实向量或付费摄取；active 发布、聊天绑定、游标、检索及生成注入仍待验收
- 候选 05 来自最终 QD 合并前的未提交工作树，归档 SHA-256 为 `7C29551E2304272E89C020034BF0D6CBAB299FB9555B6DDA84BCB5692994F411`；它不是公开发布产物，不能作为最终源码提交证据，也不启动三个月保护期
- 冻结提交的 `dev` / `dev-ori` 双产物、Jenkins BuildServer 与 `main` 正式晋升尚未完成，当前不得标记为已发布

---

## v2.2.2

### 新功能

- **Function Call 填表模式**：在填表设置中新增独立开关，启用后支持通过 OpenAI 兼容接口（DeepSeek / OpenRouter / 各类中转等）直接返回结构化操作列表，绕过 `<Amily2Edit>` 文本解析路径，填表更稳定
  - 遇到不支持 `tool_choice` 的接口时自动降级重试
  - 对思考模型注入强制调用指令，防止绕过工具直接输出文本
  - 全部走 ST 后端代理，修复 CSP 拦截直连外部 URL 的问题
- **主界面新增提示词链编辑器入口**，同时调换了记忆管理与角色世界书的按钮位置
- **规则中心**新增"自动排除用户楼层"选项

### 修复

- 提示词链按钮点击无响应（改为事件委托方式绑定）
- 拖拽组件微抖误触发（加 5px 移动阈值过滤）
- 填表检查窗若干问题修复；翰林院（批量回填）修复；防抖逻辑落地
- 角色世界书入口添加使用警告弹窗（强制 10 秒倒计时），提示该功能长期未维护
- ApiProfile `fakeStream` 字段保存丢失问题
- 正文优化默认改为关闭状态
- NGMS / NCCS API 配置槽位标签修正（NGMS→总结，NCCS→填表）
- API Profile 面板选择逻辑统一重构，修复多处旧字段覆盖新配置的问题
- 世界书控制参数兼容性修复（排除递归、插入位置、扫描深度等，适配 ST 1.17.0+）

---

## v2.2.3

### 新功能

- Function Call 填表开关下方新增公益站风险提示横幅：部分公益站会屏蔽 tools 参数，请确认支持情况避免被意外封禁

### 修复

- **Function Call 填表**：
  - 修复 ST 代理以 HTTP 200 + error body 形式返回错误、导致降级重试机制从未触发的问题
  - 修复思考模式模型（如 DeepSeek v4-flash）因 tool_choice 不兼容返回 Bad Request 后正确降级并重试
  - 重试时自动追加强制调用指令，防止思考模型绕过工具直接输出文本造成无效二次开销
- **超级记忆 / 翰林院**：
  - 修复 `getRagSettings()` 读写顶层路径而非嵌套路径，导致打开超级记忆面板后向量化、归档等开关在重载时被全默认值覆盖的问题
  - 修复自动归档失效问题
  - 修复归档管理器在同一事件中被三次触发的回归问题
  - 修复翰林院设置旧版迁移逻辑异常

---

## v2.2.4

### 新功能

- **Function Call 填表**：
  - FC 首次请求时对 DeepSeek 系模型自动附加 `thinking: { type: "disabled" }`，避免思考模式与 tool_choice 冲突
  - 操作列表为空时在日志面板输出原始响应 JSON，便于区分"AI 判断无需变更"、"格式校验全部不通过"和"JSON 解析失败"三种情况

### 修复

- **剧情优化**：移除剧情优化页面遗留的 Jqyh 直连配置字段（URL / Key / Model），统一走 API 连接配置功能分配槽位
- **表格**：
  - 补全 `batch-filling-threshold` 批处理阈值的持久化绑定（页面刷新后不再还原为默认值 30）
  - 修复分步填表并发锁与 async/await 时序问题
  - 修复外层多余 `try...finally` 导致的插件加载报错
- **Rerank**：
  - 修复选择连接配置后报"API Key 未配置"的问题（`apiMode` 现从设置读取而非硬编码 `custom`）
  - 补全 `hly-rerank-api-mode` 加载绑定及默认值
- **翰林院 RAG**：补全 `priorityRetrieval.sources` 各来源条目的缺失键，修复设置面板回填 TypeError

---

## v2.2.5

### 修复

- **翰林院（RAG）API Key 污染**：
  - 修复 `saveSettingsFromUI` 无差别遍历翰林院面板内全部 `[data-setting-key]` 输入（包含被 `profile-sync` 接管隐藏的字段），导致掩码占位符 `••••••••` 被当作真值写回 `settings.rerank.apiKey` / `settings.retrieval.apiKey`，URL / model 也被 Profile 值覆盖到 legacy 字段。修复后会跳过祖先带 `data-profile-hidden` 的输入
  - `getRerankSettings` / `getEmbedRetrievalSettings` 同时加入防御性还原：识别历史污染留下的 `••••••••` 时归为空字符串，避免取消 Profile 分配后实际请求带占位符 token 被 401
- **二次填表**：
  - 修复 `secondary-filler.js` 把哈希/重试次数写入非持久化的 `msg.metadata` 字段（ST 标准位是 `msg.extra`），导致刷新后去重与重试计数失效
  - 修复扫描深度重复计入 `bufferSize`（`contextLimit + buffer + batch + redundancy` → `contextLimit + batch + redundancy`），避免越过预期窗口
  - SWIPED 事件改走扫描路径，不再用 `targetMessage` bypass 强填最末条，`保留缓冲区(bufferSize)` 设置在滑动场景下正确生效（手动"回退重填"按钮仍保留 bypass，意图明确）
  - 修复 FC（Function Call）路径下成功填表与"AI 判断无需修改"两种结果均未写回 `amily2_process_hash` 与 `saveChat()` 的问题——之前导致 FC 模式去重完全失效，最旧的未处理楼层会被每次扫描重复发给 AI；现统一回写路径为 `markTargetsProcessed`
  - FC 空操作时同步输出原始响应 JSON 到控制台（与批量回填日志面板保持一致），便于区分"无需变更"/"格式校验失败"/"JSON 解析失败"
  - 修复 `fillWithSecondaryApi` 入口处过早设置 `secondaryFillerRunning = true`，导致防抖/总开关关闭/聊天过短/非分步模式/系统瘫痪五条早返路径均不解锁的死锁问题（特别是防抖路径——锁住后 setTimeout 回调撞上自己的锁，永久跳过后续触发）。锁的获取已挪到所有早返检查之后、`try` 块之前
- **填表设置面板**：新增"手动解除填表锁"按钮（位于触发延迟下方），用于兜底应急——若仍遇到"分步填表正在进行中，跳过本次触发"反复刷屏，可手动点击释放
- **API 调用层全面支持 AbortController**（`callAI` / `callAIForTools` / `callNccsAI` 及其全部下游 provider）：
  - 新增 `options.signal` 透传，OpenAI 兼容 / OpenAI(测试) / Google 直连 / ST 后端 / FC 等所有 `fetch` 调用均接受 `AbortSignal`
  - `callSillyTavernBackend` 由 `$.ajax` 改写为 `fetch`，以原生支持 signal
  - `callSillyTavernPreset` / `callNccsSillyTavernPreset` 通过 `raceAgainstSignal` 兜底，外部不可终止的 `ConnectionManagerRequestService.sendRequest` 也能在 signal 触发时即时返回 AbortError
  - 全部 catch 块识别 `AbortError`，rethrow 而不弹错误 toast；FC 重试逻辑识别中断后跳过重试
- **填表设置面板**：在"手动解除填表锁"旁新增"强制中断当前填表"按钮——通过 AbortController 真正掐断 fetch 连接（fetch 立即抛错），结果会被丢弃，不会污染表格 / hash / `saveChat`

---

## v2.2.8

### 新功能

- **填表记录 · 版本恢复**（填表设置面板「回退重填」旁新增「填表记录」按钮）：针对"模型填表前把整张表删空 / 误删大量内容"的反馈，提供一键找回。
  - **零新存储**：直接复用各楼层 `extra.amily2_tables_data` 里逐轮继承的表格快照——历史本就在聊天中，无需另建存储或元数据
  - 点开列出所有带快照的楼层（最新在上），可**展开预览**每一版的表格内容（CSV）后再决定
  - **恢复某版本**：把该楼层快照设为当前状态，并清除其**之后**所有楼层的快照与填表标记 hash——使该版本成为最新有效状态，后续楼层下轮自动重填会从恢复点往前重建（赌模型不再抽风）
  - 「回退重填」按钮保留，但版本恢复是更安全的找回路径

### 修复

- **角色世界书请求绕过所选 API 配置**：角色世界书实际请求（`callCustomOpenAI`）的配置来源仍读历史旧字段，导致"已在 API 连接配置里选好全兼容槽位，生成时却仍用旧配置"。现统一改走 `getCwbApiSettings()`（优先 slot profile、回退旧配置），并新增轻量 `isCwbApiConfigured()` 替换 4 处只看旧字段的"是否已配置"判断（含正确识别 ST 预设模式）；自动更新/批量更新的配置守卫一并修正
- **超级记忆面板内容不随切聊天刷新**：在 A 聊天打开超级记忆面板后切到 B 聊天，面板仍显示 A 的内容。现改为打开时即刷新，且面板可见时收到 `CHAT_CHANGED` 也同步刷新
- **填表记录等弹窗按钮中文竖排**：「恢复到此版本」等按钮文字逐字竖排显示。根因是弹窗 `appendTo('body')` 逃出了插件 `#amily2_chat_optimiser` 的 CSS 作用域，`menu_button` 退回默认窄宽度被挤竖；改为对 `.amily2-modal .menu_button` 加全局兜底（`white-space:nowrap; width:auto`），系统性修复所有同类弹窗按钮

### 重构

- 抽出 `_normalizeTableState` 共用旧存档字段归一逻辑，`loadTables` 与"恢复快照"复用，消除重复

---

## v2.2.7

### 修复

- **分步填表 · 保留楼层场景下 swipe 最新楼会回退掉已填内容**：开启「保留楼层(bufferSize)」后，分步填表处理的是较早楼层、状态本应绑定到「被填楼层里最后一条」(E)。但 `updateTableFromText` / `updateTableFromOps` 在应用完操作后会**统一把表格状态写到聊天最新楼 L**，覆盖了随后 `markTargetsProcessed` 写到 E 的快照（`loadTables` 从尾部回溯先命中 L）。结果状态实际落在 L 上，**滑动/重新生成最新楼时 `rollbackState` 回退到上一轮快照，把本轮填入的内容一起丢掉、且因较早楼层的 hash 仍在而不再重填**。
  - 修复：给 `updateTableFromText` / `updateTableFromOps` 增加 `skipPersist` 选项；分步填表（文本 / Function Call / 手动应用三条 commit 路径）统一传入，跳过"写最新楼"，改由 `markTargetsProcessed` 把状态保存到 E。
  - `bufferSize=0`（默认）时 E 即最新楼，行为与旧版一致；仅 `bufferSize>0` 的保留楼层场景受影响并被修复。

---

## v2.2.6

### 新功能

- **翰林院向量化质量升级**：
  - **边界感知切块**：替换四个来源（聊天记录/小说/世界书/手动）的纯字符硬切——优先在段落边界断开，其次句末标点（含中文引号闭合），极端长串才硬切；句子/对话不再被拦腰截断，embedding 质量同步受益。仅影响新录入，已有向量无需重建
  - **注入时序重排**：检索结果注入提示词前按时序重排（聊天记录按楼层、小说按卷/章/节——中文数字章节号可解析），rerank 只决定"选哪些块"，不再决定呈现顺序；修复"不打不相识的剧情之后紧跟关系亲密"这类因按相关度排序导致的认知时间错乱
  - **断层提示**：聊天记录相邻块楼层跳跃时自动插入"与上文相隔约 N 楼，并非连续发生"提示行，消除中间剧情缺失造成的割裂感
  - **时间标识**：新录入的聊天记录块在来源标识中带上消息发送时间（ST 向量存储不持久化元数据，时间必须写入块文本才能在检索后取回；旧格式块兼容解析）
- **记忆块工作流（memory-blocks）**：剧情优化新增"自定义记忆块"体系——占位符驱动的并发工作流框架
  - 在剧情优化面板「匹配替换 (sulv)」下方可增删自定义块：每个块定义一个占位符，执行剧情优化时主/拦截提示词中的占位符会被块的产出替换
  - **静态块**：直接输出固定内容；**AI 调用块**：用所选 API 功能槽独立请求一次，把回复（或其中指定 `<标签>` 的内容）作为替换值
  - 原有 sulv1-4 速率占位符迁入同一框架，行为与旧版逐字节一致
  - 块定义为纯 JSON、随设置持久化，为后续导入导出与战斗系统接入预留扩展点
  - 框架层新增**顺序拼接式 Chain**（`composeChain`）：与占位符替换并列的第二种组合范式——同链的块并发执行后按 `order` 排序、以 `separator` 拼接并可选 `header/footer` 包裹，产出一个完整注入块；为记忆注入合成块与战斗系统"底部战报块"预留的承载结构，本版本暂无 UI 入口
- **渐进记忆（开发中功能，暂未对外开放）**：主菜单新增独立入口（点击提示"开发中，未来版本开放"），后续完善后放出。当前已落地的设计：
  - 按"近期完整、远期摘要"的时间梯度，从指定表格（默认总结表，行序旧→新）采样历史并注入上下文：最新 X 行全量保留 + 其余历史对半拆分，较近一半等距取 Y 行、较远一半等距取 Z 行（中心对齐等距采样，不随机、不首尾加权，避免内容扎堆或事件结局被规律性忽略）
  - 经 `setExtensionPrompt` 直接注入当回合上下文——内容独立、不写世界书、不随聊天/角色卡导出，生命周期天然跟随会话（区别于超级记忆的世界书条目路线）
  - 注入位置 / 深度 / 角色 / 模板（含 `{{progressive_memory}}` 占位符）均可在面板配置；采样参数 X/Y/Z 默认 5/5/3，全部纯 JSON 持久化
  - 采样器 `sampler.js` 为纯函数，参数结构与 memory-blocks 工作链对齐，后续可平移为 `progressive_sample` 节点
- **超级记忆 · 首行常驻**（表格专属配置新增开关，默认关闭）：表格第一行通常是总调/全局定义行（基调、主线目标等），原先与普通行一样走绿灯——没人提到主键就永远不注入；开启后该行详情条目升为蓝灯常驻，切换即时生效
- **API 连接配置**：
  - 角色世界书（cwb）与一键生卡（autoCharCard）纳入旧配置自动迁移：老用户首次加载会把旧 URL / Key / 模型自动迁移为连接配置并分配槽位（一键生卡仅在规划者与执行者配置一致或规划者为空时迁移，避免悄悄改变行为）
  - **profile 已分配时参数控件 informational 化**：主面板 / 并发剧情优化 / 角色世界书 / 术语表的温度、maxTokens 控件在槽位分配 profile 后自动禁用并显示"由连接配置控制"提示，消除"改了没效果"的用户陷阱
  - **profile 状态卡新增"本设备无 Key"警示**：API Key 仅保存在最初填写它的设备/浏览器上（安全设计，不随云端设置同步），换设备后状态卡会直接亮出警示徽标，不必等到调用报错才发现

### 修复

- **独立聊天记忆从摆设变真功能**：此前向量数据"随卡不随聊天"——开启"独立聊天记忆"后录入仍存进角色库、查询却去查一个从未被写入过的聊天集合、计数恒为 0，整体静默失效。现已重构为聊天级分桶：
  - 独立模式下，聊天记录类向量按当前聊天隔离存储与检索，同一张卡开多个聊天（不同剧情线）的记忆互不污染
  - 小说 / 世界书 / 手动录入属于"知识"，仍随角色卡跨聊天共享；全局库不受影响
  - 知识管理列表为聊天专属库显示"聊天级"徽标；聊天级库禁止移动到全局
  - 统一模式（默认关闭独立记忆）的存量数据与行为完全不变
  - 已知限制：聊天专属记忆跟随聊天文件，重命名聊天文件会使其失联（与 ST 官方向量扩展同等限制）
- **超级排序截断顺序修正**：开启"超级排序"时，时序重排发生在 top_n 截断之前，导致保留的是"时序最早"而非"最相关"的块，检索结果长期偏向最旧的聊天记录。现改为先按相关度截取 top_n、再做时序排序
- **翰林院向量化失败（"向量化块数量不识别"反馈）**：
  - 一次性清洗 profile-sync 历史污染：`retrieval/rerank.apiKey` 中的掩码占位符在持久层根治（此前仅读取侧防御）；`apiEndpoint` / `rerank.apiMode` 的非法值（如被旧版写入的空字符串）归一化为 `custom`
  - 修复 `apiEndpoint` 为空/非法时请求被硬定向到 `api.openai.com`、无视用户自定义 URL 的问题（CSP 拦截 / 401 的元凶）
  - 修复**本地代理（LM Studio/Ollama）模式**自始就缺少 URL 分支、同样被错误定向到 openai.com 的问题
  - API 模式下拉补全 `OpenAI 官方` / `Azure` 选项；默认 API 模式改为 `custom`（与默认 URL 配套），新用户不再因选项缺失导致首次保存写入空值
  - profile-sync 给下拉框赋不存在选项值的污染源头修复（影响所有模块面板，不止翰林院）
- **Rerank "测试成功但实际请求报 API Key 未提供"（路径分叉根因）**：实际重排调用 `executeRerank(query, docs, settings.rerank)` 直接把 legacy 嵌套设置当连接传入，绕过了 `getRerankSettings()` 的 profile 解析；而「测试连接」传 `null` 会正常解析 profile——于是用 API Profile 配 rerank 的用户测试通过、实际生成时却拿到空 apiKey/stale url 报错。现实际调用点统一走 `getRerankSettings()`（profile 优先、legacy 兜底），与测试路径一致；`enabled / notify / hybrid_alpha` 等行为开关仍读 legacy 设置
- **Rerank "API Key 未提供"报错升级**：当原因是"连接配置在本设备没有可用 Key"时，报错会直接说明 Key 的设备本地性并指引到 API 连接配置重新填写（向量化 Google 直连、获取模型列表同步处理）
- **旧配置迁移**：一键生卡迁移时排除掩码占位符，避免把历史污染的假 Key 迁入新连接配置
- **超级记忆稳定性专项**（针对"工作不大稳定"反馈，4 处根因一次修复）：
  - **切聊天竞态污染**：CHAT_CHANGED 时超级记忆立即全量同步，而表格系统延迟 100ms 才加载新聊天的表格，导致【旧聊天】的表格内容被写进【新角色】的记忆世界书；两边表名不同时旧表条目无 GC 兜底会**永久残留**（"记忆串台"元凶）。现 CHAT_CHANGED 只确保世界书存在，新状态同步交由 `loadTables()` 完成后的自动推送，单次且时序正确
  - **死代码双轨存储拆除**：`saveStateToMetadata` / `tryRestoreStateFromMetadata` 把表格状态写到 `msg.metadata`——该字段非 ST 持久化位（同 v2.2.5 二次填表修过的坑），写入即蒸发、恢复永远为空，且每次同步还白调一次 `saveChat()`。整条链路删除，表格状态唯一信源为表格系统的 `msg.extra.amily2_tables_data`
  - **`awaitSync()` 穿透**：同步队列正忙时 `pushUpdate` 会用一个立即 resolve 的空 Promise 覆盖 `_syncPromise`，Pipeline Stage 4 等待形同虚设、后续阶段在同步未完成时被放行。现忙时不覆盖，正在运行的 drain 循环自然吃掉新入队项
  - **开关打开不生效**：启动时若总开关为关，初始化早退且不注册监听器；此后在 UI 勾选开关只写设置，超级记忆直到刷新页面前都是死的。现勾选即触发初始化（幂等）
  - 附带：`forceSyncAll` 的表格角色推断改为复用 `events-schema.inferTableRole`，消除两处重复逻辑漂移风险；每次切聊天的双倍全量同步（restore 路径一次 + 显式一次）随死代码移除归一

### 重构

- 表格核心 `manager.js` 瘦身（约 1050 → 600 行）：19 个 UI 突变操作拆分至 `actions/ui-mutations.js`，SuperMemory 事件分发拆分至 `events-dispatch.js`；全部经 re-export 保持兼容，外部调用路径零改动
- 角色世界书最后 2 处散乱的厂商 URL 判断迁移至 `detectVendor` 统一入口，业务路径上不再有硬编码的 URL substring 判断

---

## v2.2.9

### 新功能

- **Bus 工具调用框架（底层能力，面向后续功能）**：任何内部模块现在可以声明自己的 Function Call 工具集并跑多步工具调用循环（模型调工具 → 结果回喂 → 继续，直到给出答复；handler 报错会回喂模型自纠）。最重要的一点——**双运输自动降级**：原生 tools 请求被接口拒绝时（部分中转站屏蔽 tools 参数），自动切换为"工具说明写进提示词、模型按约定 JSON 回复"的兼容模式继续跑完，功能不再因中转站限制而中断。本版本为框架落地，现有填表 Function Call 行为不变，后续功能将逐步接入
- **渐进记忆（开发中功能，内测推进）**：注入内容接入密折司预览（沙漏图标可辨识、可看到本回合实际采样内容与 token 量）；新增最低触发阈值（源内容太少时不注入，随总结模块自动隐藏阈值走）、Token 上限（超预算从最旧行开始丢弃）、可见窗口行数（排除还在视野内楼层的摘要，消除重复注入）；与超级记忆同表同开时给出提示建议二选一

### 修复

- **角色世界书"选了 API 连接配置但实际仍用旧配置"（全兼容模式反馈）**：实际生成走的请求函数直接读旧配置字段、完全绕过了所选的连接配置——测试连接正常（走对的路径）、真正生成却用历史配置。现统一走连接配置优先、旧配置兜底的解析；4 处"是否已配置"判定同步修正（此前清除旧配置残留后会误判未配置拦死功能，ST 预设模式判定也已纠正）
- **宏史卷向量化数据丢失风险**：两处加固——① 向量化"成功但零条目入库"（如分块结果为空）此前也会移除宏史卷原文，现按失败处理、原文保留；② 向量化成功后原文不再直接丢弃，转存为同世界书的**禁用存档条目**（不触发、零 token 占用）——向量库是设备本地存储，清理向量库/换设备后正本仍在世界书里可找回
- **超级记忆面板切换聊天不刷新**：同卡切换聊天后，面板中的表格列表与清空记忆页面仍显示旧聊天内容；现打开面板与切换聊天时自动刷新
- **弹窗按钮中文竖排**：逃出插件 CSS 作用域的系统性兜底修复
- **清理无效事件监听**（经 SillyTavern 1.13.5–1.18.0 逐版本核实）：移除多处监听/发射 ST 根本不存在的事件名的死代码（角色世界书冗余监听、世界书更新通知重复发射、剧情优化三个无效监听），行为零变化、消除误导

---

## v2.2.10

### 新功能

- **渐进记忆 · 真压缩远带**（默认关闭）：可直接读取史官金账中已重铸的「宏史卷」，作为早期剧情的高密度摘要注入，不再额外发起 AI 压缩请求。远带文本会明确标注为早期存档，避免被误当作当前剧情续写
  - 提供「关闭 / 自动 / 始终注入」三种模式；自动模式会在翰林院 RAG 检索已启用时自动让位，避免同一宏史卷被重复注入
  - 支持独立 Token 上限，超出预算时优先保留更接近当前剧情的后段内容；金账不存在、尚未重铸或读取失败时静默跳过，不影响原有渐进记忆
- **确定性战斗系统基础（开发中）**：新增可复现的种子随机源、战斗档案解析与结构化战报；已实现 LoR 风格骰子拼点和 LC 风格硬币拼点两套纯代码结算模式，支持等级优势、状态、HP 与 LC 理智/SP 变化。模型只负责为角色选择技能，数值结算不参与模型推理
  - 战斗模块现可创建持久 `combat.state` / `combat.rounds` 表，在建局时登记参战者、在结算后回写状态与回合摘要
  - 尚未开放战斗 UI、表格视图宏或从持久表恢复战局，当前接口供后续记忆管理、自定义模块与开发调试调用
- **表格数据库底座（开发中）**：表格状态升级为聊天级稳定记录，兼容既有聊天快照；模块可通过 AmilyBus 注册归属表，并仅对自己归属表的数据执行新增、更新与删除，表结构不会开放给模块修改
  - 模块表现可声明业务主键、唯一列和 `reference` 外键；内核统一校验必填、基础类型、唯一性与引用目标，删除按 `restrict` 或非必填 `setNull` 处理
  - `TableSystem.queryRecords()` 提供跨模块只读的单表筛选、排序、投影和限制结果；不暴露裸 state，不提供跨模块写入、联表或任意脚本执行
  - 角色卡可保存不含行数据的表结构档案，并在新聊天中作为初始状态来源；运行时行数据仍只属于具体聊天，不会随卡导出
  - 为后续跨模块表读写、视图计算、`{{AM2T.视图名.属性}}` 宏映射及一键生卡数据库设计提供基础；这些用户侧能力尚未开放
- **一键生卡凭证存储迁移**：遗留模式下的规划者/执行者 API Key 会按需迁移到插件统一密钥存储，配置对象不再保留明文 Key；原有连接配置优先级与旧配置兼容逻辑保持不变

### 修复

- **内容预览与超级记忆表名的 HTML 注入风险**：预优化差异预览和超级记忆表格设置现按纯文本转义显示，不再把聊天内容或表名直接拼入 HTML
- **一键生卡连接配置迁移**：自动迁移会正确读取已迁入统一密钥存储的旧凭证，并在清理旧配置时一并移除对应凭证，避免迁移后出现“测试可用、实际无 Key”或残留密钥
- **前端渲染开关风险提示**：在设置中明确标注该开关只适用于来源可信、明确允许执行前端代码的角色卡或预设
- **插件启动被一键生卡设置回调阻断**：保存 API 配置时新增的异步密钥写入位于非异步回调中，浏览器会在解析阶段直接报错，导致整套插件不加载；现已修正回调声明
- **战斗模块启动竞态**：`CombatSystem` 早于 `TableSystem` 服务就绪时，表定义注册异常会中断公共 API 暴露，表现为 Bus 显示已注册但无法查询战斗模块；现改为先暴露 API，并对表服务就绪短暂重试
- **表格编辑器错误加载根目录样式**：`Memorisation-forms.html` 被注入酒馆页面后，其相对 `table.css` 会解析为 `/table.css`，返回 HTML 并触发样式 MIME 错误；入口已正确加载插件资产样式，现移除该重复引用

### 发布

- 真压缩远带所需的金账解析模块已纳入混淆与发布产物清单，避免开发版功能在正式插件包中缺失
- 云测试环境已更新至 `91abf38`；通过真实持久聊天验证了 `combat.state` / `combat.rounds` 的建局、结算写入、刷新后重新打开同一聊天恢复，以及 owner CRUD 隔离与只读快照隔离
- **部署环境待处理**：云测酒馆的扩展更新接口响应包含带嵌入式凭据的远端 URL。该问题不在插件代码中，应由酒馆服务端移除敏感字段并轮换已暴露凭据

---

## v2.2.11

### 安全修复

- **前端 HTML 自动渲染安全边界重构**：自动渲染保持启用，但执行环境统一改为不具备同源权限的沙箱 iframe；渲染内容无法再直接访问酒馆父页面 DOM、表单、弹窗或浏览器同源存储
  - 聊天读取、聊天写入、世界书读取、世界书写入和酒馆命令分别作为独立能力；卡片首次调用对应桥接能力时由父页面原生确认框授权，授权或拒绝结果仅在该 iframe 生命周期内缓存
  - 父页面只接受由插件登记的 iframe 消息，并校验消息来源；普通 UI 能力保留无提示调用，避免影响纯展示卡片
  - iframe 高度更新增加上限，移除沙箱内不再适用的外部模块脚本注入
- **一键生卡内容渲染加固**：Markdown、模型回复、工具名称与参数默认按不可信文本转义；只有插件内部构造且数据已转义的静态模板可显式进入可信 HTML 路径；世界书和角色下拉项改用 DOM 文本接口创建
- **表格规则编辑器注入修复**：角色卡携带的规则文本不再拼入 textarea HTML，统一使用 `.val()` 回填；行数、简化阈值、列索引和字符限制同步进行整数范围归一化
- **公告与教程渲染收口**：移除运行时下载 DOMPurify、Showdown 的 CDN 依赖；公告优先使用酒馆已有 DOMPurify，后备清洗器增加标签、属性和链接协议白名单；教程 Markdown 改用酒馆自身格式化器
- **AmilyBus 内部注册表封装**：服务、工具、模块注册表以及日志、文件、模型调用器改为私有字段，外部模块不再能直接取得或覆盖内部 handler 与 capability context
- **表格模块权限与角色卡档案校验**：
  - 已注册模块表再次声明时必须与原 schema 一致，否则拒绝启动并报告 `TABLE_SCHEMA_MISMATCH`
  - 跨 owner 外键默认拒绝，目标表需通过 `referenceGrants` 明确授权来源 owner；删除时的 `restrict` / `setNull` 副作用同样执行授权校验
  - 随卡表结构档案限制总体积、表数、列数和规则长度，拒绝运行时行数据、模块归属表以及覆盖已注册模块表

### 开发中

- **时间河规划与纯逻辑基础**：新增小说语料接收、分层弹性切块、世界构建与故事轨向量任务规划文档及初始纯函数；功能定位为 Type3，尚未接入用户 UI、后台任务或上下文注入

### 测试

- 表格数据库权限、外键与随卡档案测试：6/6 通过
- iframe 能力授权与生命周期测试：4/4 通过
- 时间河语料规划与弹性切块测试：6/6 通过
- 本版本仍需在云测试酒馆验证：可信卡片 HTML 展示、桥接能力首次授权/拒绝、刷新后重新授权，以及旧卡片是否依赖已禁止的 `parent.document` 同源访问
