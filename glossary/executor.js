import{callSybdAI}from"../core/api/SybdApi.js";import{getDataBankAttachments,getDataBankAttachmentsForSource,getFileAttachment}from"/scripts/chats.js";import{extensionName}from"../utils/settings.js";import{getPresetPrompts,getMixedOrder}from"../PresetSettings/index.js";import{generateRandomSeed}from"../core/api.js";import{safeLorebookEntries,safeUpdateLorebookEntries,compatibleWriteToLorebook}from"../core/tavernhelper-compatibility.js";import{loadWorldInfo,saveWorldInfo,createWorldInfoEntry}from"/scripts/world-info.js";import{escapeHTML}from"../utils/utils.js";export async function executeNovelProcessing(processingState,updateStatusCallback){const{chunks:recognizedChapters,batchSize:batchSize,selectedWorldBook:selectedWorldBook}=processingState;if(0===recognizedChapters.length)throw updateStatusCallback("没有可处理的章节。","error"),new Error("没有可处理的章节。");updateStatusCallback("开始处理小说...","info");try{const bookName=selectedWorldBook;if(!bookName)throw new Error("请先在设置中选择一个目标世界书。");let previousBatchAIResponse="";if(processingState.currentIndex>0){const allEntries=await safeLorebookEntries(bookName)||[],previousBatchIndex=processingState.currentIndex,targetComment=`[Amily2小说处理] 链式生成-第${previousBatchIndex}部分`,previousEntry=allEntries.find(e=>e.comment===targetComment);if(!previousEntry)throw new Error(`无法找到衔接批次 ${previousBatchIndex} 的世界书条目，请从 1 开始处理。`);previousBatchAIResponse=previousEntry.content,updateStatusCallback(`已加载批次 ${previousBatchIndex} 的内容作为上下文。`,"info")}for(let i=processingState.currentIndex;i<recognizedChapters.length;i+=batchSize){if(processingState.isAborted)return updateStatusCallback(`处理已中止。当前进度: ${i}/${recognizedChapters.length}`,"info"),"paused";processingState.currentIndex=i;const currentBatchNumber=i+1,batch=recognizedChapters.slice(i,i+batchSize);updateStatusCallback(`正在处理批次 ${currentBatchNumber}... ${`(${currentBatchNumber}/${recognizedChapters.length})`}`,"info");const chapterContent=batch.map(c=>`## ${c.title}\n${c.content}`).join("\n\n---\n\n"),order=getMixedOrder("novel_processor")||[],presetPrompts=await getPresetPrompts("novel_processor"),messages=[{role:"system",content:generateRandomSeed()}];let promptCounter=0;for(const item of order)if("prompt"===item.type)presetPrompts&&presetPrompts[promptCounter]&&(messages.push(presetPrompts[promptCounter]),promptCounter++);else if("conditional"===item.type)if("existingLore"===item.id){const contextContent=previousBatchAIResponse?`# 上一章节的剧情发展概要\n\n${previousBatchAIResponse}`:"这是小说的第一部分，请开始生成剧情发展概要。";messages.push({role:"user",content:contextContent})}else"chapterContent"===item.id&&messages.push({role:"user",content:`# 最新章节内容\n\n${chapterContent}\n\n请根据以上信息，分析并输出当前章节的剧情发展概要。`});if(messages.length<=1)throw new Error("未能根据预设构建有效的API请求。");const response=await callSybdAI(messages);if(!response||0===response.trim().length)throw new Error(`API调用失败，批次 ${currentBatchNumber} 未收到有效响应。`);const contentMatch=response.match(/\[--START_TABLE--\]([\s\S]*?)\[--END_TABLE--\]/);if(!contentMatch||!contentMatch[1])throw new Error(`API响应格式不正确，未找到被 '[--START_TABLE--]' 和 '[--END_TABLE--]' 包裹的内容，批次 ${currentBatchNumber}。`);const aiContent=contentMatch[1].trim(),newEntryData={comment:`[Amily2小说处理] 链式生成-第${currentBatchNumber}部分`,content:aiContent,keys:[`小说处理链式生成第${currentBatchNumber}部分`],enabled:!0,order:2e3+currentBatchNumber,position:"before_char"};await compatibleWriteToLorebook(bookName,newEntryData.comment,()=>newEntryData.content,{keys:newEntryData.keys,isConstant:!1,insertion_position:newEntryData.position,order:newEntryData.order}),updateStatusCallback(`批次 ${currentBatchNumber} 处理完成，已创建新条目。`,"success"),previousBatchAIResponse=aiContent}return updateStatusCallback("小说处理完成！","success"),"success"}catch(error){throw console.error("处理小说时发生严重错误:",error),updateStatusCallback(`处理失败: ${error.message}`,"error"),error}}export async function reorganizeEntriesByHeadings(bookName,headingsToProcess,updateStatusCallback){try{updateStatusCallback("开始重组...","info");const bookData=await loadWorldInfo(bookName);if(!bookData||!bookData.entries)throw new Error(`无法加载世界书 "${bookName}" 的数据。`);const allEntries=Object.values(bookData.entries);updateStatusCallback(`已获取 ${allEntries.length} 个条目，正在根据您提供的 ${headingsToProcess.length} 个标题进行解析...`,"info");const headingsMap=new Map;headingsToProcess.forEach(h=>headingsMap.set(h,[]));const finalEntries={},userTitlesSet=new Set(headingsToProcess);for(const entry of allEntries){const lines=entry.content.split(/\r?\n/);let currentCaptureTitle=null,currentCaptureContent=[];const remainingLines=[],endCapture=()=>{currentCaptureTitle&&currentCaptureContent.length>0&&headingsMap.get(currentCaptureTitle).push(currentCaptureContent.join("\n")),currentCaptureTitle=null,currentCaptureContent=[]};for(const line of lines){const trimmedLine=line.trim();if(trimmedLine.startsWith("#")&&!trimmedLine.startsWith("##")){endCapture();const potentialTitleFromFile=trimmedLine.substring(1).trim();let matchedUserTitle=null;for(const userTitle of userTitlesSet)if(potentialTitleFromFile.startsWith(userTitle)){matchedUserTitle=userTitle;break}matchedUserTitle?currentCaptureTitle=matchedUserTitle:remainingLines.push(line)}else currentCaptureTitle?currentCaptureContent.push(line):remainingLines.push(line)}endCapture();const remainingContent=remainingLines.join("\n").trim();remainingContent&&(finalEntries[entry.uid]={...entry,content:remainingContent})}let foundHeadingsCount=0;for(const contentBlocks of headingsMap.values())contentBlocks.length>0&&foundHeadingsCount++;if(0===foundHeadingsCount)return void updateStatusCallback("在任何条目中都未找到您指定的标题，无需操作。","info");updateStatusCallback(`解析完成，找到 ${foundHeadingsCount} 个匹配的标题类别。正在合并内容并创建新条目...`,"info");for(const[title,contentBlocks]of headingsMap.entries())if(contentBlocks.length>0){const mergedContent=contentBlocks.map((block,index)=>`# ${title} - 第${index+1}部分\n${block.trim()}`).join("\n\n"),newEntry=createWorldInfoEntry(bookName,bookData);Object.assign(newEntry,{comment:`[Amily2重组] ${title}`,content:mergedContent,key:[title],disable:!1,constant:!1,position:0,order:2100}),finalEntries[newEntry.uid]=newEntry}bookData.entries=finalEntries,await saveWorldInfo(bookName,bookData,!0),updateStatusCallback(`成功！已重组 ${foundHeadingsCount} 个标题。`,"success"),toastr.success(`世界书 "${bookName}" 已成功按标题重组。`)}catch(error){throw console.error("重组世界书条目时发生错误:",error),updateStatusCallback(`错误: ${error.message}`,"error"),error}}export async function loadDatabaseFiles(){const fileMap=new Map;try{getDataBankAttachments().forEach(file=>{file&&file.url&&fileMap.set(file.url,file)}),getDataBankAttachmentsForSource("global").forEach(file=>{file&&file.url&&fileMap.set(file.url,file)}),getDataBankAttachmentsForSource("character").forEach(file=>{file&&file.url&&fileMap.set(file.url,file)}),getDataBankAttachmentsForSource("chat").forEach(file=>{file&&file.url&&fileMap.set(file.url,file)})}catch(error){return console.error("Error getting database files:",error),void toastr.error("读取数据库文件失败。")}const container=document.getElementById("database-file-list-container");if(container.innerHTML="",0===fileMap.size)return container.innerHTML="<small>未找到数据库文件。</small>",void(container.style.display="block");Array.from(fileMap.values()).forEach(file=>{const fileElement=document.createElement("div");fileElement.classList.add("database-file-item","menu_button","secondary","interactable"),fileElement.textContent=file.name,fileElement.dataset.url=file.url,fileElement.addEventListener("click",async()=>{try{const text=await getFileAttachment(file.url);console.log(`Loaded file content from ${file.name}`);const event=new CustomEvent("novel-file-loaded",{detail:{content:text,fileName:file.name}});document.dispatchEvent(event),container.style.display="none",document.getElementById("select-from-database-button").innerHTML=`<i class="fas fa-check"></i> 已选择: ${escapeHTML(file.name)}`}catch(error){console.error(`Error processing file ${file.name}:`,error),toastr.error(`处理文件 ${file.name} 失败。`)}}),container.appendChild(fileElement)}),container.style.display="block"}