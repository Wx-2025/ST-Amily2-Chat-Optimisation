# 数据库兼容：第一次怎么开

> 适用版本：Amily 2.3.1
> 权限要求：有效 Type2 及以上授权

这项功能用于让依赖 shujuku / AutoCardUpdaterAPI 的角色卡迁移到 Amily 表格。它默认关闭，也不会执行卡内脚本。只给来源可信的卡开启兼容和聊天授权。

## 最短配置流程

1. 打开角色卡对应的聊天，再进入 **表格 → 数据库兼容**。
2. 开启 **启用全局兼容层**。
3. 点击 **扫描并建立当前聊天兼容档案**，认真核对摘要后确认。
4. 只勾选这张卡实际请求的功能；不确定时先不勾。
5. 先选择 **授权当前聊天：只读**。只有确认卡片必须改表时，才使用读写或精确扩展授权。
6. 按页面提示刷新当前聊天，确认状态显示兼容桥已激活。

如果希望同一张卡的新聊天沿用选择，可在配置正确后点击 **记住当前授权为本卡默认**。这只保存在本机，不会写回 PNG/JSON，也不等于替新聊天自动授权。

## 表格模板和剧情预设

- **查看内容**只打开静态预览，不执行其中脚本。
- **绑定到本聊天**只绑定迁移档案已经登记的资源。
- 新聊天仍按聊天隔离；角色卡默认只负责预选。

## 什么时候需要 Agent 兼容

普通表格、B3 模板或剧情预设不需要 Agent。只有卡片明确依赖 Agent 接口时，才进入右侧 **Agent 兼容**：

1. 开启卡片请求的 Agent 功能并确认本聊天授权；
2. 给“shujuku Agent 原生任务”分配独立 API Profile；
3. 依次执行 **准备影子档案 → 构建索引 → 激活**。

不要跳过状态检查。按钮不可用时，先看页面上的就绪度、错误码和专用 Profile 状态。

## 出问题先检查

| 现象 | 先做什么 |
| --- | --- |
| 看不到数据库兼容分页 | 确认授权有效且至少为 Type2，然后完整刷新页面 |
| 扫描后仍显示无档案 | 重新扫描并完成确认；旧版档案不能直接沿用新授权 |
| 新聊天全部关闭 | 开启自动检测，或保存本卡默认；聊天授权仍需确认 |
| 功能已勾选但不生效 | 同时检查全局功能开关与“本聊天”授权 |
| Agent 卡在未准备 | 检查专用 Profile、旧伴侣脚本是否停用，以及是否完成整页刷新 |

需要完整的权限层级、B3/B4、Agent、恢复和排错说明，请打开下方的“补充进阶教程”。
