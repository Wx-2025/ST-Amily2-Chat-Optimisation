import{getRequestHeaders}from"/script.js";import{getContext,extension_settings}from"/scripts/extensions.js";import{amilyHelper}from"../../../core/tavern-helper/main.js";import Options from"./Options.js";import RequestBody from"./RequestBody.js";export default class ModelCaller{constructor(loggerDelegate){this.logger=loggerDelegate,this.defaultHeaders={"Content-Type":"application/json"}}async call(callerName,messages,options){if(!(options instanceof Options)){throw new TypeError("[ModelCaller] Options must be instance of Options class.")}this._log("info",`API Request [${options.mode}] StreamMode: ${options.fakeStream}`,callerName);try{const requestBody=new RequestBody(messages,options);let result;return result="preset"===options.mode?await this._callPreset(callerName,requestBody,options):await this._callDirect(callerName,requestBody,options),this._normalize(result,options.fakeStream)}catch(error){throw this._log("error",`Request Failed: ${error.message}`,callerName),error}}_log(level,msg,plugin){this.logger?.log&&this.logger.log(level,msg,"ModelCaller",plugin)}_extractMessage(data){if("string"==typeof data)try{data=JSON.parse(data)}catch{return{role:"assistant",content:data}}return data?.choices?.[0]?.message??null}async callWithTools(callerName,messages,options,loop={}){if(!(options instanceof Options))throw new TypeError("[ModelCaller] callWithTools: options must be instance of Options class.");const{getToolDefs:getToolDefs=()=>[],dispatch:dispatch,maxSteps:maxSteps=8,onToolError:onToolError="feedback",transport:transport="auto",tools:extraTools=[],toolChoice:toolChoice="auto"}=loop;if("function"!=typeof dispatch)throw new TypeError("[ModelCaller] callWithTools: loop.dispatch is required.");if("preset"===options.mode)throw new Error("[ModelCaller] callWithTools 暂不支持 ST 预设模式（preset），请使用 direct 连接配置。");const byName=new Map;for(const def of[...extraTools||[],...getToolDefs()||[]]){const name=def?.function?.name;name&&!byName.has(name)&&byName.set(name,def)}const toolDefs=Array.from(byName.values());if(0===toolDefs.length)throw new Error("[ModelCaller] callWithTools: no tools supplied by the internal task scope.");const convo=Array.isArray(messages)?[...messages]:[],allToolCalls=[];let activeTransport="json"===transport?"json":"tools",jsonPromptInjected=!1;const enterJsonMode=()=>{activeTransport="json",jsonPromptInjected||(convo.unshift({role:"system",content:buildJsonToolPrompt(toolDefs)}),jsonPromptInjected=!0)};"json"===activeTransport&&enterJsonMode();const runHandler=async(name,args)=>{try{return await dispatch(name,args)}catch(err){if("throw"===onToolError)throw err;return this._log("warn",`tool "${name}" 出错，回喂模型自纠: ${err.message}`,callerName),{error:String(err.message||err)}}};for(let step=1;step<=maxSteps;step++){if(this._log("info",`callWithTools step ${step}/${maxSteps} [${activeTransport}] (tools:${toolDefs.length})`,callerName),"tools"===activeTransport){let message=null;try{message=await this._requestToolsMessage(callerName,convo,options,toolDefs,toolChoice)}catch(reqErr){if("auto"!==transport)throw new Error(`${reqErr.message} （带工具调用的请求失败——部分中转站禁用了 tools 参数；请更换支持 function calling 的接口，或关闭工具调用模式）`);this._log("warn",`tools 运输失败，自动降级 JSON 模式: ${reqErr.message}`,callerName),enterJsonMode()}if(message){const toolCalls=Array.isArray(message.tool_calls)?message.tool_calls:[];if(0===toolCalls.length)return{content:(message.content||"").trim(),steps:step,finishReason:"stop",messages:convo,toolCalls:allToolCalls,transport:"tools"};convo.push(message);for(const tc of toolCalls){const name=tc?.function?.name;let result,args={};try{args=tc?.function?.arguments?JSON.parse(tc.function.arguments):{},result=await runHandler(name,args)}catch(parseErr){if("throw"===onToolError)throw parseErr;result={error:`arguments JSON 解析失败: ${parseErr.message}`}}allToolCalls.push({name:name,args:args,result:result}),convo.push({role:"tool",tool_call_id:tc.id,name:name,content:"string"==typeof result?result:JSON.stringify(result??null)})}continue}}const stepOptions=new Options({mode:"direct",fakeStream:!1,apiUrl:options.apiUrl,apiKey:options.apiKey,model:options.model,maxTokens:options.maxTokens,temperature:options.temperature,params:options.params}),raw=await this._callDirect(callerName,new RequestBody(convo,stepOptions),stepOptions);if(raw&&raw.error){const msg=raw.error?.message||("string"==typeof raw.error?raw.error:JSON.stringify(raw.error));throw new Error(`接口拒绝了请求: ${msg}`)}const jsonMessage=this._extractMessage(raw),text=(jsonMessage?.content||"").trim();if(!text)throw new Error("callWithTools(json): 未取到有效响应文本");const parsed=parseJsonToolResponse(text);if("final"===parsed.type)return{content:parsed.content,steps:step,finishReason:"stop",messages:convo,toolCalls:allToolCalls,transport:"json"};if(convo.push({role:"assistant",content:text}),"invalid"===parsed.type){this._log("warn",`JSON 协议解析失败(${parsed.reason})，回喂纠错`,callerName),convo.push(buildJsonRetryMessage(parsed.reason));continue}const result=await runHandler(parsed.name,parsed.arguments);allToolCalls.push({name:parsed.name,args:parsed.arguments,result:result}),convo.push(buildJsonToolResultMessage(parsed.name,result))}return this._log("warn",`callWithTools 触顶 maxSteps(${maxSteps})，强制结束`,callerName),{content:"",steps:maxSteps,finishReason:"maxSteps",messages:convo,toolCalls:allToolCalls,transport:activeTransport}}async _requestToolsMessage(callerName,convo,options,toolDefs,toolChoice){const stepOptions=new Options({mode:"direct",fakeStream:!1,apiUrl:options.apiUrl,apiKey:options.apiKey,model:options.model,maxTokens:options.maxTokens,temperature:options.temperature,params:options.params,tools:toolDefs,toolChoice:toolChoice}),raw=await this._callDirect(callerName,new RequestBody(convo,stepOptions),stepOptions);if(raw&&raw.error){const msg=raw.error?.message||("string"==typeof raw.error?raw.error:JSON.stringify(raw.error));throw new Error(`接口拒绝了请求: ${msg}`)}const message=this._extractMessage(raw);if(!message)throw new Error("未取到有效响应消息");return message}async _callDirect(callerName,requestBody,options){const url="/api/backends/chat-completions/generate",payload=requestBody.toPayload(),fetchOpts={method:"POST",headers:{...getRequestHeaders(),...this.defaultHeaders},body:JSON.stringify(payload)};return options.fakeStream?this._fetchFakeStream(url,fetchOpts):this._fetchStandard(url,fetchOpts)}async _callPreset(callerName,requestBody,options){const context=getContext(),originalProfile=await amilyHelper.triggerSlash("/profile"),targetProfile=context.extensionSettings?.connectionManager?.profiles?.find(p=>p.id===options.presetId);if(!targetProfile)throw new Error(`Preset ID ${options.presetId} not found`);if(originalProfile!==targetProfile.name){this._log("info",`Switching profile: ${originalProfile} -> ${targetProfile.name}`,callerName);const escapedName=targetProfile.name.replace(/"/g,'\\"');await amilyHelper.triggerSlash(`/profile await=true "${escapedName}"`)}try{if(options.fakeStream){const url="/api/backends/chat-completions/generate",profilePayload=this._buildProfilePayload(targetProfile),minimal=requestBody.toMinimalPayload(),finalPayload={...profilePayload,...minimal,...options.params},fetchOpts={method:"POST",headers:{...getRequestHeaders(),...this.defaultHeaders},body:JSON.stringify(finalPayload)};return await this._fetchFakeStream(url,fetchOpts)}if(!context.ConnectionManagerRequestService)throw new Error("ST Request Service unavailable");return await context.ConnectionManagerRequestService.sendRequest(targetProfile.id,requestBody.messages,options.maxTokens)}finally{if(originalProfile)try{if(originalProfile!==await amilyHelper.triggerSlash("/profile")){const escapedOriginal=originalProfile.replace(/"/g,'\\"');await amilyHelper.triggerSlash(`/profile await=true "${escapedOriginal}"`)}}catch(e){this._log("warn",`Failed to restore profile: ${e.message}`,callerName)}}}async _fetchStandard(url,opts){const res=await fetch(url,opts);if(!res.ok)throw new Error(`HTTP ${res.status}`);return await res.json()}async _fetchFakeStream(url,opts){const res=await fetch(url,opts);if(!res.ok)throw new Error(`Stream HTTP ${res.status}`);const reader=res.body.getReader(),decoder=new TextDecoder;let fullContent="",buffer="";try{for(;;){const{done:done,value:value}=await reader.read();if(done)break;buffer+=decoder.decode(value,{stream:!0});const lines=buffer.split("\n");buffer=lines.pop();for(const line of lines){const trimmed=line.trim();if(trimmed&&"data: [DONE]"!==trimmed&&trimmed.startsWith("data: "))try{const jsonStr=trimmed.substring(6),json=JSON.parse(jsonStr),delta=json.choices?.[0]?.delta?.content;delta&&(fullContent+=delta)}catch(e){console.warn("[ModelCaller] SSE Parse Error:",e)}}}}finally{reader.releaseLock()}if(!fullContent&&buffer)try{return JSON.parse(buffer)}catch{return buffer}return fullContent}_normalize(data,isFromStream=!1){if(isFromStream&&"string"==typeof data)return data;if("string"==typeof data)try{data=JSON.parse(data)}catch(e){return data}return data?.choices?.[0]?.message?.content?data.choices[0].message.content.trim():data?.content?data.content.trim():"object"==typeof data?JSON.stringify(data):data}_buildProfilePayload(targetProfile){const context=getContext(),payload={...targetProfile};payload.user_name=context.name1||"User",payload.char_name=context.name2||"AI",payload.group_names=[],payload.use_sysprompt=!0,payload.type="quiet",payload.custom_prompt_post_processing=payload.custom_prompt_post_processing||"strict",payload.model||(payload.model=payload.openai_model||payload.claude_model||payload.mistral_model||"");const rawUrl=payload["api-url"]||payload.api_url||payload.custom_url||payload.url;rawUrl&&("custom"===payload.chat_completion_source?(payload.custom_url=rawUrl,payload.reverse_proxy=payload.reverse_proxy||""):(payload.reverse_proxy=rawUrl,payload.custom_url=rawUrl),payload.zai_endpoint=rawUrl,payload.vertexai_region=rawUrl);const globalGenSettings=extension_settings.text_generation||{};return["temperature","max_tokens","top_p","top_k","min_p","frequency_penalty","presence_penalty","repetition_penalty"].forEach(field=>{void 0===payload[field]&&(payload[field]=globalGenSettings[field]??("temperature"===field?1:0))}),payload.chat_completion_source||(payload.chat_completion_source="openai"),payload}}