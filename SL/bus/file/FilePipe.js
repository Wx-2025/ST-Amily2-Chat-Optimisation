const DB_NAME="Amily2_FilePipe";const DB_VERSION=1;const STORE_NAME="files";let _dbPromise=null;function _openDB(){if(_dbPromise)return _dbPromise;_dbPromise=new Promise((resolve,reject)=>{const req=indexedDB.open(DB_NAME,DB_VERSION);req.onupgradeneeded=event=>{const db=event.target.result;if(!db.objectStoreNames.contains(STORE_NAME)){const store=db.createObjectStore(STORE_NAME,{keyPath:["plugin","path"]});store.createIndex("by_plugin","plugin",{unique:false})}};req.onsuccess=e=>resolve(e.target.result);req.onerror=e=>{_dbPromise=null;reject(new Error(`[FilePipe] IndexedDB 打开失败: ${e.target.error}`))}});return _dbPromise}function _tx(db,mode){return db.transaction(STORE_NAME,mode).objectStore(STORE_NAME)}function _idbRequest(req){return new Promise((resolve,reject)=>{req.onsuccess=e=>resolve(e.target.result);req.onerror=e=>reject(e.target.error)})}class FilePipe{constructor(){this.name="FilePipe"}_safePath(plugin,path){if(!plugin||typeof plugin!=="string"){console.error("[FilePipe] 无效的插件标识。");return null}if(!path||typeof path!=="string"){console.error("[FilePipe] 无效的路径。");return null}if(path.includes("..")){console.error(`[FilePipe] 安全拦截：插件 "${plugin}" 尝试目录穿越，路径: ${path}`);return null}return path.replace(/^\/+/,"")}async read(plugin,path){const safePath=this._safePath(plugin,path);if(!safePath)return null;try{const db=await _openDB();const result=await _idbRequest(_tx(db,"readonly").get([plugin,safePath]));return result?.data??null}catch(e){console.error(`[FilePipe] read 失败 (${plugin}/${path}):`,e);return null}}async write(plugin,path,data){const safePath=this._safePath(plugin,path);if(!safePath)return false;try{const db=await _openDB();await _idbRequest(_tx(db,"readwrite").put({plugin:plugin,path:safePath,data:data,updatedAt:(new Date).toISOString()}));return true}catch(e){console.error(`[FilePipe] write 失败 (${plugin}/${path}):`,e);return false}}async delete(plugin,path){const safePath=this._safePath(plugin,path);if(!safePath)return false;try{const db=await _openDB();await _idbRequest(_tx(db,"readwrite").delete([plugin,safePath]));return true}catch(e){console.error(`[FilePipe] delete 失败 (${plugin}/${path}):`,e);return false}}async list(plugin,prefix=""){if(!plugin)return[];try{const db=await _openDB();const store=_tx(db,"readonly");const index=store.index("by_plugin");const range=IDBKeyRange.only(plugin);return new Promise((resolve,reject)=>{const paths=[];const req=index.openCursor(range);req.onsuccess=e=>{const cursor=e.target.result;if(!cursor){resolve(paths);return}if(!prefix||cursor.value.path.startsWith(prefix)){paths.push(cursor.value.path)}cursor.continue()};req.onerror=e=>reject(e.target.error)})}catch(e){console.error(`[FilePipe] list 失败 (${plugin}):`,e);return[]}}async clearAll(plugin){const paths=await this.list(plugin);let count=0;for(const path of paths){if(await this.delete(plugin,path))count++}console.info(`[FilePipe] 已清除插件 "${plugin}" 的 ${count} 个文件。`);return count}async stat(plugin,path){const safePath=this._safePath(plugin,path);if(!safePath)return null;try{const db=await _openDB();const result=await _idbRequest(_tx(db,"readonly").get([plugin,safePath]));if(!result)return null;return{path:result.path,updatedAt:result.updatedAt}}catch(e){return null}}forPlugin(plugin){return{read:path=>this.read(plugin,path),write:(path,data)=>this.write(plugin,path,data),delete:path=>this.delete(plugin,path),list:prefix=>this.list(plugin,prefix),clearAll:()=>this.clearAll(plugin),stat:path=>this.stat(plugin,path)}}}export default FilePipe;