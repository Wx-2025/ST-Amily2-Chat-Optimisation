# API 连接（照着点就行）

## 认货

| 用途 | 认法 / 用在哪 |
|---|---|
| **对话** | Gemini、DeepSeek、GPT、Claude、Qwen → 总结、表格填表、独立API填表、记忆管理… |
| **向量** | 名字带 `embedding` / `embed` / `bge` → **仅**智能检索 |
| **重排** | 名字带 `rerank` → 智能检索更准（可选） |

> 聊天模型别挂向量/重排；向量别挂对话槽。

## 怎么配

```
连接 → 添加 → 选用途 → 填地址密钥模型 → 保存
     → 分配：开关打开 + 下拉选这条连接
     → 完成
```

| 页 | 干啥 |
|---|---|
| **连接** | 建/改接口，可点测试 |
| **分配** | 开关=开功能；下拉=用哪条连接 |
| **更多** | 选择密钥存储方式、导出或导入私钥 |

## 翻车

| 现象 | 你做 |
|---|---|
| 没反应 | 分配有没有选连接 + 开关开没开 |
| 填表/总结仍没反应 | 看对应页 **提取规则** 是不是「未分配」（规则配置中心建好再选） |
| 类型不匹配 | 对话挂对话、向量挂向量 |
| Type2+ 换电脑没密钥 | 更多 → 存储方式 → 授权码云同步；插件会用当前授权码恢复私钥 |
| 手动云同步换电脑没密钥 | 更多 → 导出/导入私钥包 |

> “授权码云同步”备份的是解密私钥；API Key 的加密密文本身仍随当前 SillyTavern 设置同步。只复制授权码、却没有同步同一份酒馆设置时，不会凭空出现原来的 API Key。

> 从本机切到加密云同步时，插件会先检查当前酒馆设置中的共享公钥和密文；检测到已有备份时不会自动生成新密钥或覆盖。切回本机只退出当前设备，共享备份会保留，其他设备不受影响。

> 安全边界：私钥包使用服务器授权码派生密钥加密，而授权服务仍需保存该授权码用于校验。因此它不是零知识或端到端保险箱，不能防护授权数据库与私钥包被同时读取的整库泄露。请勿使用短码、可预测码或人工口令作为云同步授权码；平台生成码应使用至少 128 bit 的密码学安全随机量。
