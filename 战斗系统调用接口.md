# CombatSystem 调用接口

> 状态：v0.1 内部接口（2026-07-15）
> 实现：[core/combat/CombatService.js](core/combat/CombatService.js)
> 设计源（开发仓）：`docs/fight/战斗数据层架构.md` · `docs/fight/rule.md`

本文定义 Amily2 内部模块调用 `CombatSystem` 的方式与当前能力边界。它是运行在同一个 SillyTavern 页面中的 Bus 服务，不是 HTTP API，也不是已经承诺兼容性的第三方插件 SDK。

## 1. 获取服务

`CombatSystem` 由 `imports.js` 副作用加载，并在下一轮事件循环注册到 `Amily2Bus`。调用方必须使用 `query()` 查询，不能重复 `register('CombatSystem')`。

```javascript
function getCombatSystem() {
    return window.Amily2Bus?.query('CombatSystem') ?? null;
}

const combat = getCombatSystem();
if (!combat) {
    throw new Error('CombatSystem is not ready. Retry after extension initialization.');
}
```

可在控制台检查服务是否已暴露：

```javascript
window.Amily2Bus?.query('PUBLIC')?.getAvailableModules();
// 包含 "CombatSystem" 即可调用
```

禁止事项：

```javascript
window.Amily2Bus.register('CombatSystem'); // 错误：名称已被严格锁定，会抛异常
window.Amily2Bus.get('CombatSystem');      // 错误：Bus 没有 get() 方法
```

外部扩展在同一页面上也可以查询该全局服务，但当前接口仅按本插件内部演进，不保证跨插件版本兼容；外部调用方应固定版本并做好 `null` 与异常处理。

## 1.1 表格数据库接入

战斗模块启动时会经受限 Bus 通道注册 `combat.state` 与 `combat.rounds` 两张表的 schema；**注册不会自动建表**。后续回合编排层应由 `CombatSystem` 自己调用 `ensureRegisteredTable`，再对这两张 owner 为 `CombatSystem` 的表写入记录。

其他 Amily 服务可只读查询当前全部表：

```javascript
const tables = window.Amily2Bus.query('TableSystem');
const allTables = tables?.listTables() ?? [];
const combatState = tables?.getTableSnapshot('combat.state');
```

`getMemoryState()`、`listTables()` 与 `getTableSnapshot()` 都返回深拷贝。跨服务写入不能通过 `query()` 完成；它必须由注册服务持有的 `context.callService()` 发起，Bus 会注入真实调用方身份。

## 2. 已暴露方法

| 方法 | 用途 | 当前状态 |
|------|------|----------|
| `createEncounterFromSeeds(seedText, options)` | 将 L2 战斗档案文本解析为参战者和两张初始表 | 可用 |
| `startEncounterFromSeeds(seedText, options)` | 初始化 `combat.state` / `combat.rounds` 并写入参战者 | 可用 |
| `ensureCombatTables()` | 幂等创建战斗 owner 表 | 可用 |
| `persistCombatResolution(result)` | 回写双方状态并追加回合摘要 | 可用 |
| `createRandom(seed)` | 创建确定性随机源 | 可用 |
| `createReport(round)` | 创建空战报对象 | 可用 |
| `resolveLoR(input)` | 结算一次 LoR 行动 | 可用，首版 |
| `createCombatTables(combatants)` | 创建表 A/表 B 的内存结构 | 可用 |
| `createCombatStateUpdateOperations(tableState, combatants)` | 从角色新状态生成表 A 更新操作 | 可用，未落库 |
| `createRoundSummaryOperation(tableState, summary)` | 生成表 B 追加摘要操作 | 可用，未落库 |

`LC`、`DND`、战斗 UI、回合调度、资源消耗、战报注入、真实表格持久化均未接入，不能假定存在对应方法。

## 3. 建立战斗

输入为一个或多个 L2 档案块；完整的开发期格式规范位于 `docs/fight/战斗数据层架构.md` 的“L2 种子序列化格式”章节。

```javascript
const combat = window.Amily2Bus.query('CombatSystem');

const encounter = combat.createEncounterFromSeeds(`
【战斗档案·浮士德】
阵营: 我方
模式: LoR
HP: 80
攻击等级: 9
防御等级: 6
资源: 光芒:10
技能:
- 裂伤 | 攻击 | [3-7][2-5][1-4] | 2光芒 | -
`, {
    seed: 'chat-42:battle-1',
    defaultFaction: '中立',
    defaultMode: 'LC',
    defaultHp: 10,
    instance: 1,
});
```

需要立即建立持久战斗时，改用异步入口：

```javascript
const encounter = await combat.startEncounterFromSeeds(seedText, {
    seed: 'chat-42:battle-1',
});
// encounter.combatants[*].recordId 已绑定到 combat.state 的稳定记录 ID
```

返回值：

```javascript
{
    schemaVersion: 1,
    seed: 'chat-42:battle-1',
    combatants: [/* 标准角色对象 */],
    tableRows: [/* 表 A 的行数组 */],
    tables: [/* 表 A「战斗角色状态」、表 B「战斗回合摘要」 */],
}
```

缺少档案字段时会以默认值降级；无法找到任何 `【战斗档案·名称】` 块时返回空参战者数组，不会自动创建角色。

## 4. 确定性随机与 LoR 结算

随机源由调用方持有。`encounter.seed` 只负责记录种子，`resolveLoR()` 不会自动读取它。

```javascript
const rng = combat.createRandom(encounter.seed);
const [attacker, defender] = encounter.combatants;

const result = combat.resolveLoR({
    round: 1,
    attacker,
    attackerSkill: attacker.skills[0],
    defender,
    defenderSkill: defender.skills[0], // 可省略，省略时按无阻拦攻击结算
    rng,
});

await combat.persistCombatResolution(result);
```

`resolveLoR()` 参数要求：

- `rng` 必须是 `createRandom()` 返回的对象，或兼容 `int(min, max, label)` 的确定性实现；禁止传入 `Math.random()`。
- `attackerSkill.formula` 必须含至少一个 LoR 骰子区间，例如 `[3-7][2-5]`。
- 当前调用方应自行保证参战双方都处于 `LoR` 模式；首版不会替调用方进行跨体系调度。
- 结算不会修改传入的 `attacker` 或 `defender`；必须使用返回的角色状态进入下一次结算。

返回值关键字段：

```javascript
{
    mode: 'LoR',
    round: 1,
    attacker: { /* 结算后的攻击方 */ },
    defender: { /* 结算后的防守方 */ },
    clashes: [/* 每枚骰子的原始结果 */],
    damage: { toAttacker: 0, toDefender: 12, reflection: 0 },
    flags: { attackerCritical: false, perfectBlock: false },
    report: CombatReport,
    reportText: 'R1-1: ...',
}
```

同一战斗应复用同一个 `rng` 实例以保留抽取序列；若需要按回合独立重放，则自行约定稳定派生种子，例如 `${battleId}:round:${round}`，并将该约定随战斗存档保存。

## 5. 战报

`result.reportText` 是引擎原始结算战报，供调试、复盘和后续渲染层转换使用：

```javascript
console.log(result.reportText);
// R1-1: 浮士德:[6+0+1] - 敌人:[4+0+1] (Aw) ⟦浮士德压制⟧ // 7 vs 5
// S1-2: 敌人:(HP-2) ⟦轻伤⟧
```

原始战报含骰子和机制数值，**当前不能直接自动注入模型上下文**。后续将增加“引擎战报 → 无机制词渲染视图”的转换层，再接入记忆管理的当回合瞬态注入；在此之前，调用方若自行注入，需要承担模型把骰子/点数误写进剧情的风险。

## 6. 表格适配

```javascript
const tables = combat.createCombatTables(encounter.combatants);

const stateOperations = combat.createCombatStateUpdateOperations(
    tables,
    [result.attacker, result.defender],
);

const summaryOperation = combat.createRoundSummaryOperation(tables, {
    round: result.round,
    actor: result.attacker.name,
    skill: result.attackerSkill,
    target: result.defender.name,
    result: result.damage.toDefender > 0 ? '拼点胜' : '未造成伤害',
    damage: result.damage.toDefender,
    statusChange: '-',
    resourceChange: '-',
});
```

生成的操作符合表格执行器的形状：

```javascript
{
    op: 'updateRow' | 'insertRow',
    tableIndex: 0,
    rowIndex: 0,             // updateRow 才有
    data: { '0': '...', '1': '...' },
}
```

注意：`startEncounterFromSeeds()` 与 `persistCombatResolution()` 已走模块表服务，负责持久化当前角色状态和回合摘要。旧的 `createCombatTables()` 与操作适配器仍只生成内存结构，保留给预览与兼容调用；新的回合 UI、从持久表恢复战斗对象、战报瞬态注入仍未实现。

此外，表 A 的角色名目前视为主键；`createCombatTables()` 会拒绝同名角色，群战需先在 L2 档案中使用唯一名称或后缀。

## 7. 错误处理与调用边界

调用方应处理下列异常：

| 场景 | 行为 |
|------|------|
| 服务未注册 | `query()` 返回 `null` |
| 档案没有角色名 | `createCombatant()` 抛错 |
| 骰子格式无效或区间反转 | `resolveLoR()` 抛错 |
| 缺少 RNG、攻击方或防守方 | `resolveLoR()` 抛错 |
| 表不存在、角色不在表 A、角色同名 | 表格适配器抛错 |

本接口不负责授权、玩家选卡、敌方选技、目标选择、回合排序或用户确认。调用层在进入 `resolveLoR()` 前完成这些决策；引擎只接收确定的输入并给出可重放的结果。

## 8. 版本策略

- Bus 服务名固定为 `CombatSystem`。
- `schemaVersion` 当前为 `1`，只描述初始表结构，不能视为完整战斗存档版本。
- 新增模式、字段或战报语义前，先更新开发仓的 `docs/fight/战斗数据层架构.md`，再扩展服务接口。
- 任何破坏性改动应新增方法名或显式版本字段，不静默改变既有输入/输出含义。
