# CombatSystem 调用接口

> 状态：v0.4 内部接口（2026-07-26）；已集成到 `2608` 开发分支，未公开发布
> 实现：[core/combat/CombatService.js](core/combat/CombatService.js)
> 规则契约：[docs/fight/战斗规则配置档案.md](docs/fight/战斗规则配置档案.md)
> 设计源：[docs/fight/战斗数据层架构.md](docs/fight/战斗数据层架构.md) · [docs/fight/rule.md](docs/fight/rule.md)

本文描述合入 `2608` 后的当前代码边界。`CombatSystem` 是同一 SillyTavern 页面中的 Amily2Bus 服务，不是 HTTP API，也不是已承诺跨版本兼容的第三方 SDK。

当前已经提供规则档案 v2、单次 LoR / LC 结算、目标槽调度和 Guard 的可组合 API，并有三表持久生命周期编排。完整回合 controller、玩家选槽、敌方决策、浮窗、DND、战报注入，以及 counter / evade / LoR 抗性 / status / 乘算正式结算仍未完成。

## 0. Type3 与数据边界

- 战斗系统只对 **Type3** 开放。设置管理器、Bus 公开方法、预览、恢复及持久写入都执行运行时门禁；权限在异步操作期间变化时，旧访问租约失效并失败关闭。
- `combat.state`、`combat.rounds`、`combat.meta` 是 owner 为 `CombatSystem` 的模块私有表。它们不出现在通用表格 UI，不接受手工表格编辑、CSV、传统 AI 填表或 Function Call 填表，也不会被通用表格上下文注入读取。
- `combat.meta` 永不进入模型上下文。未来战报与状态联动必须由 `CombatSystem` 生成有界、字段白名单化的安全投影，不能直接公开模块表快照。
- Type3 是客户端功能开放条件，不应被描述成服务端认证或抵御同页面恶意脚本的安全沙箱。

## 1. 获取服务

`CombatSystem` 由 `imports.js` 副作用加载。调用方应查询现有服务，不能重复注册：

```javascript
const combat = window.Amily2Bus?.query('CombatSystem') ?? null;
if (!combat) {
    throw new Error('CombatSystem is not ready.');
}
```

`window.Amily2Bus.query('PUBLIC').getAvailableModules()` 返回值包含 `CombatSystem` 时，公开方法已可查询。`Amily2Bus` 没有 `get()` 方法，核心服务名也不能由外部脚本重新 `register()`。

公开 Bus 仍处于同页面 JavaScript 环境，因此只暴露经过 Type3 门禁的纯结算、预览和只读恢复。持久写入口保留为模块内部导出；Type3 门禁不能替代对不可信同页脚本的隔离。

## 2. 当前公开方法

### 2.1 规则、随机与基础对象

| 方法 | 用途 |
|---|---|
| `createRandom(seed)` | 创建确定性随机源 |
| `createReport(round)` | 创建战报对象 |
| `createRuleProfile(input)` | 规范化并校验 schema v2 规则档案；兼容显式 schema v1 迁移 |
| `getDefaultRuleProfile()` | 返回内置默认档案副本 |
| `getActiveRuleProfile()` | 返回设置中当前启用档案副本 |
| `createEncounterFromSeeds(seedText, options)` | 创建不写表的 encounter 预览及完整规则快照 |
| `restoreEncounter(options)` | 从 `combat.state` / `combat.rounds` / `combat.meta` 严格恢复 |

配置档案的保存、删除、导入、导出和启用由高级设置中的战斗规则配置管理器负责。

### 2.2 目标槽调度

| 方法 | 用途 |
|---|---|
| `createActionSchedule(input)` | 校验 `speedScheduler` 策略、槽位与链，生成确定性 JSON 调度表 |
| `createActionScheduleState(schedule, options)` | 创建可序列化游标 |
| `nextScheduledAction(schedule, state)` | 取得下一条行动指令 |
| `applyScheduledActionResult(schedule, state, result)` | 把一次结算结果写入新游标 |
| `simulateActionSchedule(schedule, options)` | 测试和重放用便捷执行器 |

这些方法只排顺序和维护 capability，不替调用方选技能，也不自动调用 `resolveLoR()` / `resolveLC()`。

### 2.3 Guard

| 方法 | 用途 |
|---|---|
| `inspectDefenseKind(value)` | 归一化 Guard / counter / evade 名称并返回支持状态 |
| `beginRoundShieldLedger(round)` | 创建本轮护盾账本 |
| `clearRoundShieldLedger(ledger, nextRound)` | 清空护盾并可推进轮次 |
| `getRoundShield(ledger, combatantId)` | 查询剩余护盾 |
| `resolveDefenseAction(input)` | 校验 `guard` 策略，把本次 Guard 最终威力加入护盾 |
| `absorbFinalDamage(input)` | 用护盾逐击吸收已经完成模式和等级结算的最终伤害 |

`counter` 与 `evade` 只会被识别并返回显式“不支持”，不会降级成 Guard 或普通攻击。

### 2.4 模式结算与兼容适配

| 方法 | 用途 | 当前边界 |
|---|---|---|
| `resolveLoR(input)` | 结算一次 LoR 行动 | 首版单次行动 |
| `resolveLC(input)` | 结算一次 LC 拼点与 S 阶段 | 普通、减算、不可破坏硬币 |
| `createCombatTables(combatants)` | 创建旧式内存表结构 | 兼容层 |
| `createCombatStateUpdateOperations(...)` | 生成旧表状态操作 | 兼容层 |
| `createRoundSummaryOperation(...)` | 生成旧表摘要操作 | 兼容层 |

## 3. 规则档案 v2

本期 profile 的核心形状如下：

```json
{
  "format": "amily2.combat-rule-profile",
  "schemaVersion": 2,
  "id": "amily2-default-combat-rules",
  "rulesetId": "amily2-core",
  "rulesetVersion": 1,
  "engineApiVersion": 1,
  "features": {
    "speedScheduler": { "strategy": "target-chain-v1" },
    "spSettlement": {
      "strategy": "total-subround-step-v1",
      "winner": [10, 12, 13, 14, 15],
      "loser": [5, 7, 8, 9, 10]
    },
    "guard": { "strategy": "shield-v1" },
    "counter": { "strategy": "off" },
    "evade": { "strategy": "off" },
    "lorResistance": { "strategy": "off" },
    "statusEngine": { "strategy": "off" },
    "multiplySettlement": { "strategy": "off" }
  }
}
```

`id` 是用户配置档案身份；`rulesetId` 是引擎规则族，本期只支持 `amily2-core`。克隆自定义配置时应产生新 `id`，不能自行改出未知 `rulesetId`。

每个 feature 都是带 `strategy` 的对象，不是布尔值或裸字符串。未知顶层字段、feature、策略、规则集版本或引擎 API 版本会硬失败。

encounter 锁定的不是当前活动 profile 引用，而是完整快照包装：

```javascript
{
    format: 'amily2.combat-rule-snapshot',
    snapshotVersion: 1,
    profileHash: 'fnv1a32:........',
    profile: { /* 完整、规范化、不可变的 schema v2 profile */ },
}
```

`profileHash` 当前采用 FNV-1a，只用于非对抗性的格式漂移、误写和调用链一致性检查。它不是密码学哈希，不提供来源认证、抗碰撞或面对恶意输入的防篡改保证。

## 4. 创建、持久化与恢复

### 4.1 预览创建

`createEncounterFromSeeds()` 是纯创建入口，不写表：

```javascript
const encounter = combat.createEncounterFromSeeds(`
【战斗档案·浮士德】
阵营: 我方
模式: LC
HP: 80
攻击等级: 9
防御等级: 6
技能:
- 表示放出 | 攻击 | (18+3*(+3)) | 3资源 | -
`, {
    encounterId: 'chat-42:battle-1',
    seed: 'chat-42:battle-1',
    rulesProfile: combat.getActiveRuleProfile(),
});
```

关键返回字段：

```javascript
{
    schemaVersion: 2,
    encounterId: 'chat-42:battle-1',
    seed: 'chat-42:battle-1',
    rulesSnapshot: { /* 完整快照包装 */ },
    rulesProfile: { /* rulesSnapshot.profile 兼容别名 */ },
    rulesHash: 'fnv1a32:........',
    hasRulesSnapshot: true,
    combatants: [],
    tableRows: [],
    tables: [],
}
```

L2 LC 技能只接受严格短式 `(18+3*(+3))`；减算示例为 `(31+3*(-12))`。直接调用 resolver 时可传已经归一化的技能对象，但这不等于 L2 文本允许旧式或含糊写法。

### 4.2 三表持久生命周期

模块内部导出而未列入公开 Bus 白名单的方法：

| 方法 | 行为 |
|---|---|
| `startEncounterFromSeeds(seedText, options)` | 校验并构建新 encounter，随后原子清空并替换三张战斗表；返回 revision 0 的活动身份 |
| `persistCombatResolution(result)` | 强制校验 `encounterId`、`revision`、`rulesHash`，原子写状态、摘要并推进 meta revision |

持久结果不是匿名结算对象。调用方必须从 `startEncounterFromSeeds()` 或 `restoreEncounter()` 的返回值取得三项活动身份，并附加到 resolver 结果：

```javascript
const committed = await persistCombatResolution({
    ...result,
    encounterId: active.encounterId,
    revision: active.revision,
    rulesHash: active.rulesHash,
});
active = committed; // 后续行动沿用 committed.revision，不能继续使用旧 revision
```

`rulesSnapshot` / `rulesProfile` 可以作为额外一致性证据一并传入，但不能替代必填的 `encounterId`、`revision` 和 `rulesHash`。任一必填字段缺失或与 `combat.meta` 不一致都拒绝写入。成功返回值带锁定的 `encounterId` / `rulesHash`、最新 `currentRound` 和 `revision = 旧值 + 1`；下一次持久化必须使用这份新 revision。

三表职责：

| 表 | 内容 |
|---|---|
| `combat.state` | 当前参战者运行状态 |
| `combat.rounds` | 回合摘要 |
| `combat.meta` | 唯一活动记录：`encounterId`、`active`、`round`、`revision`、`seed`、完整 `rulesSnapshot` |

生命周期写入在模块内串行化。新战只有整批删除/插入全部成功后才替换旧三表；失败时旧 encounter 不变。持久回合结果缺少活动身份、revision 过期、encounter 不同或规则哈希不同都会被拒绝。

三张表虽由 TableSystem 持久化，仍保持模块私有；上述“状态”和“摘要”不是对通用表格查询、AI 填表或上下文注入的授权。`combat.meta` 只用于恢复与并发控制，禁止外发给模型。

这两个写方法没有暴露给 `window.Amily2Bus.query('CombatSystem')`。当前也没有一个公开 Bus 方法能够独立跑完整战斗。

### 4.3 严格恢复

`restoreEncounter()` 始终从 `combat.meta` 读取 seed 和完整规则快照：

```javascript
const restored = combat.restoreEncounter({
    encounterId: 'chat-42:battle-1', // 可选：作为期望值校验
    seedText,                        // 可选：补回技能、被动与 metadata
    rulesHash: encounter.rulesHash,  // 可选：作为期望值校验
});
```

以下情况会显式失败：

- `combat.meta` 缺失、不是恰好一条活动记录，或必要字段无效；
- `rulesSnapshot` JSON、格式、版本或 `profileHash` 无效；
- 显式传入的 encounterId、seed、rulesHash、rulesSnapshot 或 rulesProfile 与活动 meta 不一致；
- `combat.state` / `combat.rounds` 缺失或 schema 漂移。

恢复不会读取当前活动规则作兼容回退，成功返回时 `hasRulesSnapshot` 恒为 `true`。`combat.state` 不保存技能与被动，完整续跑仍应提供原 L2 `seedText`；当前也尚未持久化 RNG 抽取游标。

## 5. 目标槽原子链

最小调度输入：

```javascript
const schedule = combat.createActionSchedule({
    slots: [
        { slotId: 'A:8', actorId: 'A', campId: 'ally', speed: 8, targetSlotId: 'E:2' },
        { slotId: 'A:4', actorId: 'A', campId: 'ally', speed: 4, targetSlotId: 'E:2' },
        { slotId: 'A:3', actorId: 'A', campId: 'ally', speed: 3, targetSlotId: 'E:2' },
        { slotId: 'E:2', actorId: 'E', campId: 'enemy', speed: 2, targetSlotId: 'A:4' }
    ],
    chains: [{
        chainId: 'chain-e2',
        anchorSlotId: 'E:2',
        orderedSlotIds: ['A:8', 'A:4', 'A:3'],
        clashSlotId: 'A:8'
    }]
});

let state = combat.createActionScheduleState(schedule);
let next = combat.nextScheduledAction(schedule, state);
```

独立链按 anchor initiative 排序；链内严格遵守 `orderedSlotIds`，不重新按速度排序。只有实际 `clash` 指令的 `producesSp` 为 `true`；单方面攻击为 `false`。anchor 被击败后，该原子链剩余行动全部取消。

调度器只描述执行顺序。`[8,4,3] vs Guard [2]` 的数值吸收由 Guard API 完成。公共入口未传 `rulesProfile` 时只为一次性预览读取当前活动档案；进行中的 encounter 必须显式传 `encounter.rulesProfile`。`features.speedScheduler.strategy === "off"` 或不是 `target-chain-v1` 时硬失败。

## 6. Guard 结算

`generatedShield` 必须是模式适配器已经算好的最终 Guard 威力；Guard 层不会重复套等级、拼点、SP、反伤或状态：

```javascript
let ledger = combat.beginRoundShieldLedger(1);
ledger = combat.resolveDefenseAction({
    ledger,
    combatantId: 'E',
    defenseKind: 'guard',
    generatedShield: 2,
}).ledger;

const absorbed = combat.absorbFinalDamage({
    ledger,
    combatantId: 'E',
    hp: 100,
    hits: [8, 4, 3],
});
```

结果等价于：第一击 8 先消耗 2 点 Guard，HP 承受 6；Guard 耗尽后 4 和 3 无阻拦，总 HP 损失 `6 + 4 + 3 = 13`。每轮结束调用 `clearRoundShieldLedger()`，不能把旧轮护盾带到下一轮。进行中的 encounter 必须显式传 `encounter.rulesProfile`；未传只适合按当前活动档案预览。`features.guard.strategy === "off"` 或不是 `shield-v1` 时入口硬失败。

一期不自动产生“破防”。`features.statusEngine.strategy === "off"` 时，伤势词只能作为叙事标签。

## 7. LoR / LC 单次结算

随机源由调用方持有；`encounter.seed` 只是记录，resolver 不会自动取得或推进存档游标：

```javascript
const rng = combat.createRandom(encounter.seed);
const [attacker, defender] = encounter.combatants;

const result = combat.resolveLC({
    round: 1,
    attacker,
    attackerSkill: attacker.skills[0],
    defender,
    defenderSkill: defender.skills[0],
    rng,
    rulesProfile: encounter.rulesSnapshot.profile,
});
```

LC 当前规则边界：

- R 每个子轮双方重投全部仍有效硬币；同点继续重投，不产生最终平局。
- SP 按本次 R 总子轮数一次结算，默认胜方 `10/12/13/14/15`、败方 `5/7/8/9/10`；单方面攻击 SP 为 0。
- S 使用更新后的 SP 重新投币；普通与减算逐击结算，原始威力先和 0 取最大值，再套等级倍率与每击最低 1 伤害。
- 拼输方的不可破坏硬币会在胜方 S 完成后触发 LC 固有后续；若持有者或原目标在轮到它前已经死亡或无效，则取消。
- 该不可破坏后续不等于通用 counter；`features.counter.strategy` 仍为 `off`。
- 乘算仍是隔离实验路径，不能当成正式平衡规则。

`resolveLoR()` / `resolveLC()` 都返回新角色状态，不修改传入对象。调用方必须把返回值继续传入后续结算。

## 8. 错误处理与未完成边界

调用方应处理服务未就绪、档案解析失败、未知规则策略、损坏快照、表 schema 漂移、持久结果缺少 encounterId / revision / rulesHash、过期 revision、无效槽链、缺失 RNG 和非法骰子/硬币公式等异常。规则与恢复路径采用硬失败，不猜测、不静默降级。

当前明确未完成：

- 将 scheduler、Guard、模式 resolver 和持久写入串成完整回合 controller；
- 玩家/敌方选技、目标重定向、资源支付、结束战斗；
- counter、evade、LoR 三类攻击及双抗性、可执行 status、乘算正式结算、暴击；
- DND；
- 战报清洗后的瞬态模型注入；
- 战斗悬浮 UI 与真实 SillyTavern 全流程验收。

## 9. 版本约束

- Bus 服务名固定为 `CombatSystem`。
- encounter 返回的 `schemaVersion: 2` 与三张表各自的 `schemaVersion: 1` 是不同层级。
- Combat Rules Profile 当前为 schema v2，规则快照包装当前为 snapshotVersion 1。
- 破坏性接口变化必须新增显式版本字段或方法名，不能静默改变既有输入/输出语义。
