# 规则配置（照着点就行）

## 先看消息长什么样

点酒馆 AI 消息旁的 **小铅笔（编辑）**，看正文被什么标签包着。

示例：

```text
<time>2026/7/25</time>

<content>
<!--我今天不高兴-->
我今天真高兴。
</content>
```

## 怎么配

| 想做 | 怎么写 |
|---|---|
| 只要标签里的内容 | 开「只提取指定标签」→ 写入 `time,content`（逗号分隔，不要尖括号） |
| 去掉 CoT / 注释 | 排除段落：左边写 `<!--`，右边写 `-->` |
| 去掉思考块 | 左边 `<think>`，右边 `</think>`（以你消息里真实标签为准） |

上面例子：

- 标签填：`time,content` → 会取时间和正文
- 排除：`<!--` … `-->` → 丢掉 `我今天不高兴` 那段注释

## 怎么用

```
规则配置：新建 → 起名 → 开标签提取 / 加排除 → 保存
各功能页：总结 / 表格 / 检索 的「提取规则」下拉选这条
```

## 翻车

| 现象 | 你做 |
|---|---|
| 提取空 | 标签名写错了；用小铅笔对照真实标签 |
| 还带思考 | 排除起止标记和消息里不一致 |
| 功能没反应 | 对应页规则下拉还是「未分配」 |

## 仍解决不了

回首页 **作者留言 / 公告板**，点 **【01】群** 或 **【02】群** 加群求助。
